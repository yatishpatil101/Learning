# PuneNest E2E — Coverage Traceability Matrix

Audit of every app route / feature (`frontend/src/App.jsx` + `docs/flows/`) against the
Playwright specs in `tests/`. Status legend:

- ✅ **Covered** — a dedicated spec exercises the feature's key buttons/functions.
- 🟡 **Partial** — touched incidentally or only one surface; needs a dedicated/deeper spec.
- ❌ **Missing** — no spec; **coverage gap to close**.

Suite size: **228 spec files**, grouped audience → feature area under `tests/`:

| Folder | Specs | | Folder | Specs |
|---|---:|---|---|---:|
| `consumer/flatmates` | 34 | | `consumer/property` | 18 |
| `mobile` | 28 | | `consumer/list-property` | 18 |
| `admin` | 26 | | `consumer/services` | 13 |
| `consumer/account` | 22 | | `ops` | 12 |
| `platform` (+`auth`, `help`) | 20 | | `consumer/society` | 9 |
| `consumer/search` | 19 | | `consumer/home` | 6 |

The five loose specs at `tests/` and `tests/consumer/` are cross-cutting and belong to no
one area. Counts are re-derived from the tree, not maintained by hand.

Spec citations below are paths under `tests/`, minus the `.spec.js`.
`scripts/check-coverage-citations.mjs` fails if one of them stops existing — a stale
citation reads as coverage that is not there.

---

## Viewport projects

The suite is **folder-routed** across viewport projects in *both* configs, because the
mobile-first overhaul made several behaviours viewport-dependent and a desktop-only run
would silently pass against a broken phone layout. A spec's folder is therefore a
functional choice, not just filing — it decides which viewports ever exercise it.

`tests/mobile/**` moved wholesale to the **live** suite in P5b wave 3, so the three
projects that matter now live in `playwright.live.config.js`:

| Project (live config) | Device | Runs |
|---|---|---|
| `chromium` | Desktop Chrome | every `live-*` spec **except** `tests/mobile/` |
| `mobile` | Pixel 7 (412×915) | `tests/mobile/` + the converted cross-viewport specs |
| `mobile-small` | 360×640, `hasTouch` | `tests/mobile/` only |

The mock config (`playwright.config.js`) keeps a `chromium` and a `mobile` project. Its
`mobile` project now carries only `CROSS_VIEWPORT`; its `mobile-small` project was
removed in the same wave, because with the folder gone it matched zero specs — and a
project that matches nothing reports nothing.

`CROSS_VIEWPORT` is the explicit opt-in for specs that assert something genuinely
viewport-dependent (`consumer/flatmates/owner-split`,
`consumer/flatmates/posting`, `consumer/services/referral-rewards`). Adding a spec there is a
deliberate act — it doubles that spec's runtime, and a spec that only ever needed one
viewport should not be on the list. Converting a cross-viewport spec means **moving**
its entry to the live config's `mobile` project, never dropping it: a stale path matches
nothing and reports nothing, so halved coverage is silent.

`mobile-small` is deliberately narrower than a feature sweep: it exists to stress the
bottom chrome, tap targets and label wrapping at the cramped width where they break
first, not to run the whole suite a third time.

**Two traps this layout has already caught, both worth remembering when writing a
cross-viewport spec:**

- *Collapsed chrome.* `Footer.jsx` renders each column as an accordion that starts
  **closed** below `sm`. A cross-viewport spec that clicks a footer link blind passes
  on desktop and fails on a phone against a footer that is behaving correctly — see
  `revealFooterLink()` in `platform/help/live-i18n-urls.spec.js`.
- *Unpainted tap targets.* Controls carrying `.tap-extend` are drawn smaller than 44px
  on purpose and restore the touch floor with a transparent 44px `::before`
  (`index.css`). `boundingBox()` measures the painted box, so asserting on it demands a
  44px *pill* and fails a compliant control. `mobile/live-tap-targets.spec.js` used to
  exempt `.tap-extend` wholesale; it now **unions the element box with its
  `::before`/`::after` geometry** instead, which is strictly stronger — deleting the
  pseudo-element from `index.css` fails the sweep, where the class name alone used to
  buy a pass. A single-element spec can still take the cheap route (see
  `mobile/live-home-taps.spec.js`), but never assert on `boundingBox()` alone.
- *Sweeps that measure a blank page.* Until D19x, `tap-targets.spec.js` swept at
  `networkidle`, which on this app fires ~350ms **before** React paints — six of seven
  routes were being measured with `innerText.length === 0` and passing vacuously. The
  sweep now gates on `appReady()` plus a `MIN_CANDIDATES` floor that is asserted again
  at measurement time. Any new sweep-style spec must do the same: a sweep that can
  silently measure an empty document is worse than no sweep.

---

## Consumer

| Route / Feature | Flow doc | Spec(s) | Status |
|---|---|---|---|
| `/` Home (search, featured, popular, property-types, flatmates rail) | search-listings | consumer/home/live-entity-search, consumer/home/live-featured, consumer/home/live-popular-places, consumer/home/live-property-types, consumer/home/live-search-combobox, consumer/home/live-flatmates-rail | ✅ |
| The home Flatmates rail's two CTAs fork on the **session**: "Find a flatmate" opens the Team-up view (`?view=team-up`, the browse-people question, not `move-in`), while "Post your requirement" sends a guest to `/signin` and a signed-in account straight to the post form. The retired mock twin could not prove the second fork — it wrote `puneNestUser` into localStorage, a key the live app never reads, so against a real API that user does not exist and the CTA would take the *guest* path while the test claimed to assert the signed-in one. The account here is a fresh registration with no listings, no badge and no flatmate history, which is the adversarial case: a form that opened only for an established user fails | flatmates, search-listings | consumer/home/live-flatmates-rail | ✅ |
| `/help` help centre (+ `/hi/help`, `/mr/help`, `/docs`, `/help-center`) | — | platform/help/live-centre, platform/help/live-i18n-urls, platform/help/live-article-feedback (all `--config=playwright.live.config.js`) | ✅ |
| Footer help links on a phone stay tappable and keep the `/hi`/`/mr` language prefix even with the cookie banner showing; the page reserves the banner's height instead of letting fixed bottom chrome cover the footer (D189) | — | platform/help/live-i18n-urls (`--config=playwright.live.config.js`, `--project=mobile`) | ✅ |
| `/listings` search + filters + map | search-listings | consumer/search/live-listings-locality-filter(-registry), consumer/search/live-type-aware-filters, consumer/search/live-search-property-types, consumer/search/live-commercial-type-filter, consumer/search/live-filter-slider-manual-entry, `consumer/search/live-near-a-place-*`, consumer/search/live-location-recovery, consumer/search/live-location-search-oracle, consumer/search/live-map-popup, consumer/search/live-map-panel-contact, consumer/search/live-boost-ranking, consumer/search/live-listings-responsive-controls, consumer/search/live-listing-attributes, consumer/search/live-server-side-search (all `--config=playwright.live.config.js`) | ✅ |
| Photoless property cards hit during the seeded search-property-types journey render **no `<img src="">` at all**, so `image: ''` rows stay visible without the browser re-requesting the whole page or logging React warnings (D188) | search-listings | consumer/search/live-search-property-types | ✅ |
| `/property/:id` detail | property-detail | consumer/property/live-detail, consumer/property/live-detail-improvements, consumer/property/live-detail-sale, consumer/property/live-infotips, consumer/property/live-detail-counts, consumer/property/live-chat-owner, consumer/property/live-dedup, consumer/property/live-dup-modal | ✅ |
| The detail heading is **composed** around fields the listing may not have, and a missing one leaves no trace — an Open Plot has a null `bhk`, so the heading reads "Plot for Sale in Wagholi" with no orphan "BHK", no `0 BHK`, and no `null`/`undefined`. The derived title is also the breadcrumb's and the gallery's label, so a throw here white-screens the route rather than degrading. The retired mock twin aimed at the right seam and the wrong fields: it seeded `type: undefined` and `createdAt: undefined`, both of which are `NOT NULL` in Postgres (V3, V1), so neither test could ever have failed against a real database — `bhk` is the one null the live catalogue can actually produce | property-detail | consumer/property/live-detail | ✅ |
| The rating block's average, review count, star distribution and per-aspect averages come from the seam's `getPropertyReviewSummary` (`GET /properties/{propId}/reviews/summary`, D79) rather than a `reduce` over the rendered list; `categoryAverages` stays sparse, per-aspect row order is the mapper's (the server sorts `order by c.key`, the mock inserts in product order), and "% would recommend" — which has no server aggregate — is still derived from the list | property-detail | consumer/property/live-reviews-summary | ✅ |
| A failed summary read leaves the reviews themselves rendered — the section draws the aggregate grid only when the summary arrived, so losing one of the two reads shows cards without stars, and names the missing rating, instead of claiming the listing has no reviews. **Unreachable in mock mode**: both providers read the same localStorage rows, so neither read is a request and `page.route(...).abort()` intercepts nothing — so this is asserted live, aborting only `/reviews/summary` and checking the list read still returned 200 with rows in it before asserting the cards rendered, `reviews-aggregate` is absent, and `property-rating-unavailable` is shown | property-detail | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| Every star in the property review composer carries an accessible name — "3 star" on the overall strip, "3 star for Value" on each aspect row — so the six `StarInput` strips are not thirty identically-announced buttons in a dialog (D198). Now proved behind a real gate: the resident claims a stay, the owner confirms it over HTTP, and the composer opens because the server says it may. The retired mock twin bought its eligibility by writing the `pnTenancies:` localStorage bucket whose dead read was D194 — a door that does not exist in production, and one whose bricking-up the spec could not have noticed, since every assertion was scoped to a dialog that would simply never appear | property-detail | consumer/property/live-review-composer | ✅ |
| A former resident can earn reviewer standing without a booked visit, and **only** with the landlord's agreement (D194). The tenancy half of eligibility used to be a `localStorage` read nothing on the live path wrote, so it was unconditionally false against the API and residents were told to "book a visit first" for a flat they had lived in. Both halves now go through the seam: a brokered tenancy (`/me/tenancies`) and an owner-confirmed declaration. The load-bearing assertions are the negative ones — a **pending** claim does not open the composer, and a **withdrawn** confirmation closes it again — because making residents eligible is easy and doing it without minting a self-service review button is the actual problem. Server-side twin: `TenancyDeclarationFlowTest` (422 for pending, 404 for a non-owner confirming) | property-detail | consumer/property/live-tenancy-declaration | ✅ |
| The ownership-verified badge describes what was actually checked — documents sighted by the platform — and makes **no** land-registry claim (D190). The copy is the assertion here rather than the state: the badge is now earnable from `property_ownership_evidence`, and the previous wording promised a registry match the platform has never performed, which is the kind of overclaim a user relies on and cannot verify | property-detail | consumer/property/live-ownership-verified-copy | ✅ |
| The Society section renders **only** when the listing is bound to a society (D19). `societyForListing` used to end in `pool[fnvHash(listing.id) % pool.length]`, so the page printed a real building's name, builder, towers, units, year, occupancy and verification badge over a home that was not in it — never hedged, and indistinguishable from a recorded binding. Both directions are asserted, and the **unbound** one is the regression guard: the old code could always render *a* society, so what it could never do is render nothing. The heading is asserted absent rather than empty, because an emptied section still asserts membership and its registration/conveyance tiles were fed by `ownershipVerified`, a claim about the seller's title | property-detail, societies | consumer/property/live-society-binding | ✅ |
| Contact reveal + badge-not-gate | contact-gate-leads | consumer/property/live-contact-generic, consumer/property/live-contact-badge-not-gate | ✅ |
| Owner number is never revealed to a buyer — the gate hides it regardless of the owner's `hide_number` pref; approval unlocks in-app chat, not digits (D5, global policy) | contact-gate-leads | contact-identity-masking ("hides the owner number for a buyer regardless of the owner pref") | ✅ |
| Contact grant is per listing, not per owner. **Asked by role `link`, which is the role the component uses** — `Owner.jsx` renders Call and WhatsApp as `tel:` / `wa.me` anchors, so the retired mock spec's `getByRole('button', { name: 'Call' }).toHaveCount(0)` was green against the very markup it forbade | contact-gate-leads | live-owner-profile ("a visitor is routed to a listing and is never offered the number") | ✅ |
| Free-contact quota + exhausted modal | contact-gate-leads | referral-rewards | ✅ |
| **The owner-contact quota is the server's, not the browser's (D31b).** It used to be a `localStorage` counter, a locally-minted referral bonus and a gate that refused *before* making any request — three browser opinions, all reset by clearing site data and none of them visible from a second device. `GET /me/entitlements` now reports it and `POST /contacts/request` refuses with a 422 `contact_quota_exhausted`. The load-bearing assertions are the derivations: `used` is a count of `contact_requests` rows, so a repeat press on the same listing costs nothing and a refused press costs nothing either; and `referralBonus` is recomputed from the referrals that justify it, so a clawback takes its contacts back with no grant to reverse. Server-side twin: `EntitlementsEndpointsTest` | plans-billing-refer | consumer/live-entitlements (`--config=playwright.live.config.js`), consumer/services/referral-rewards (mock build, same arithmetic behind the mock provider) | ✅ |
| `/owner/:id` public owner profile — contract **and** rendered screen; the header, the trust badges, the listing rail counted against the `owner=` facet rather than "more than zero", the not-found screen (a separate claim from the API's 404) and a console-error guard | consumer/account/owner-hub | live-owner-profile (`--config=playwright.live.config.js`) | ✅ |
| **The owner reviews block distinguishes "no reviews" from "we could not fetch the reviews".** It shipped with three invented testimonials out of an i18n file — praise for an owner nobody had reviewed, indistinguishable on screen from the real thing — and no coverage at all. With those deleted the section has three genuine outcomes and the two that must never be confused are the empty one and the failed one: one is a statement about the owner, the other is a statement about us. The success path asserts only that the section *resolved* (no skeleton, no unavailable) rather than pinning the empty branch, because a spec that seeds one review for this owner would otherwise turn it red for a reason that has nothing to do with this page. The failure branch is unreachable from any fixture — a seeded owner either has reviews or has none, and both are successes — so it is forced by routing `/api/reviews/owner/{id}` to a 500 at the network, which is the same path a real outage takes | consumer/account/owner-hub | live-owner-profile (`--config=playwright.live.config.js`) | ✅ |
| `/compare` | (search-listings) | consumer/search/live-compare | ✅ |
| `/signin` `/signup` auth + OTP | auth | platform/auth/live-flow (`--config=playwright.live.config.js`), platform/auth/live-improvements (`--config=playwright.live.config.js`), mobile/live-auth-keyboard | ✅ |
| **Sign In discloses nothing about whether a number is registered** — an unregistered mobile and a freshly registered one both stay on `/signin` and reveal the same OTP entry, with no "new here?" hint. The live API has deliberately no existence endpoint, because answering it publicly is a user-enumeration oracle; `Signin.jsx` gates the mock's convenience bounce to `/signup` behind `!authIsLive`, and that branch dies in P5c. Asserted as a pair on purpose — "the unknown number went to OTP" is only evidence of non-disclosure if a known number does the same | auth | platform/auth/live-flow (`--config=playwright.live.config.js`) | ✅ |
| `/services` hub | services-calculators | consumer/services/live-services-hub | ✅ |
| `/services/packers-movers` | services-calculators | consumer/services/live-packers | ✅ |
| `/services/property-legal` | services-calculators | consumer/services/live-legal | ✅ |
| `/home-loans` | services-calculators | consumer/services/home-loans, consumer/services/live-loans-team | ✅ |
| `/services/interior-renovation` | services-calculators | consumer/services/live-interior | ✅ |
| `/services/property-valuation` | services-calculators | consumer/services/live-valuation | ✅ |
| `/services/rent-agreement` | rent-agreement | rent-agreement | ✅ |
| Two of its tests used to re-log as an admin and carry on into `/ops/rent-agreement` — a consumer spec proving a consumer flow should not need a second role, and that desk is gone. Both were truncated to their consumer half and retitled: the owner's upload is asserted on the request itself (matching the real `owner-pan` file name, not merely a non-empty `docs[]` — `serviceFlow` gives *every* request a placeholder `defaultDocs()` array, so a length check would pass without any upload), and the lead ticket is asserted to open at `new`. The dropped half asserted "Mark all verified", which is not coming back: the checklist is derived on read (D120) so there is nothing to mark, and `docs_review` was one of three statuses the React prototype invented that `ServiceRequestStatus` refuses by name | rent-agreement / service-queues | rent-agreement | ✅ |
| Rent agreement is a **paid** desk: create prices it server-side, parks it at `awaiting-payment` (invisible to ops) and returns a `paymentSessionId`; only the signed webhook settles it | rent-agreement | *(backend `ServiceRequestFlowTest.PaidGate` — not reachable from e2e, see note)* | ⛔ by design |
| …which is also why `ops/live-drafting-desk.spec.js` files a **valuation** request rather than a rent agreement: `findForQueue` excludes `awaiting-payment` on purpose, so the rental desk is always empty from e2e. The disclosure guard it asserts is type-agnostic, so nothing is lost by moving desks | rent-agreement / ops-desks | ops/live-drafting-desk | ✅ |
| The rent-agreement draft autosave deliberately omits PAN and Aadhaar (D159): a mid-fill refresh restores every other answer with those two blank, they never reach `localStorage`, and a draft written before the rule is redacted in place on the next visit | rent-agreement | consumer/services/rent-agreement | ✅ |
| `/emi-calculator` | services-calculators | emi-calculator | ✅ |
| `/contact` | support-tickets | consumer/account/live-support-tickets (public enquiry form) |
| `/support` tickets + FAQ. The **customer's** half of the domain — `ops/live-support-queue` owns the desk's. The thread and the list are asserted to show the id the *server* minted, read back outside the browser, because both render `{t.id}` raw and the retired mock spec matched `/SUP-\d+/` — a format only `providers/mock/supportProvider.js` has ever produced, so it described the mock rather than the contract. Creation and the empty state both run on throwaway accounts: the seed gives the *tenant* a ticket, so "no tickets yet" is a claim about who is signed in, and a ticket minted on a named actor would outlive this file (the live DB resets once per run, not per file) | support-tickets | consumer/account/live-support-tickets |
| `/notifications` | saved-alerts | consumer/account/live-notifications, consumer/property/live-alerts | ✅ |
| The inbox's category chips use the UI vocabulary (`match`, `enquiry`, …), while the server emits namespaced types. A real `saved.search.match` must map to **New Matches**, be visible in the row, and stay there when that filter is selected — otherwise a correct server row silently becomes a grey system message and disappears from every meaningful filter | saved-alerts | consumer/account/live-notifications ("renders server-owned inbox rows and filters their mapped categories") | ✅ |
| Mark-all and dismiss cross the notification boundary: the page's optimistic affordance is checked against a second `GET /notifications` outside the browser. This catches the plausible no-write regression where a card vanishes locally but the server still owns it | saved-alerts | consumer/account/live-notifications ("mark all read reaches the server", "dismiss removes a server row") | ✅ |
| `/saved` | saved-alerts | saved (desktop), mobile/live-inbox-saved (mobile) | ✅ |
| Removing a saved card stages an undo for 5s rather than committing — swipe **and** the per-card remove button both open the window, undo restores real state, and the window commits when it lapses (D99). Live, "stages" is asserted at the network: zero `DELETE /me/saved/*` while the undo is on offer, exactly one after it lapses, and the shortlist read back from the API outside the browser. A build that unsaved immediately and merely *rendered* an undo affordance passed the retired mock twin, which could only read the page's own copy | saved-alerts | mobile/live-saved-swipe-undo, consumer/account/live-saved ("remove stages an undo without telling the server") | ✅ |
| The undo affordance is announced (`role="status"`), takes focus when the card that raised it unmounts, and names the card in its accessible name — a destructive gesture must not have a gesture-only escape hatch (D99) | saved-alerts | mobile/live-saved-swipe-undo ("announced, focused and names the card") | ✅ |
| Shortlist is one shared set — heart, navbar badge and `/saved` agree, and the set is the server's (`GET /me/saved`), not the device's | saved-alerts | consumer/account/live-saved, consumer/property/live-reels, consumer/account/dashboard |  ✅ |
| Saved searches + alert cadence picker (`off\|instant\|daily\|weekly`), one shared list — the dashboard writes `alertFrequency` directly, so a non-default cadence survives an off→on cycle (D84). Read back off the **server** after every change, because `SavedSearchContext` patches its own state optimistically and rolls back only on a rejection: the picker therefore holds the value you chose even when the write never lands, so the rendered `<select>` cannot testify about persistence. Deleting is asserted the same way — the list's load path swallows errors into an empty array, so "No alerts yet" is also what an unreachable API looks like | saved-alerts | consumer/property/live-alerts, consumer/flatmates/alerts, consumer/account/live-notifications | ✅ |
| Alert cards require sign-in to create a managed alert; anonymous submit still feeds the demand-gap then routes to `/signin?reason=alerts` (D85). The signal is asserted as a named `alerts` count on the locality's Supply Gap row — a fresh slug that carried no row before the click — not as "the report changed": the same page logs a passive `search` signal for that slug on load, so a row-exists assertion would pass with the alert half deleted. A signed-in submit is proven by reading `GET /me/saved-searches` back and naming the fields the form chose (`channel: 'sms'` against the provider's `whatsapp` default, cadence, deal, locality, type), since the confirmation panel renders from the request the browser sent | saved-alerts | consumer/property/live-alerts, consumer/flatmates/alerts | ✅ |
| **"N properties match your alert" is counted by the catalogue, not by one page of it** (D227) — both the notifications inbox and the dashboard retention strip derived that number in the browser, each by fetching listings and filtering the result. The fetch returned `PAGE_SIZE = 100`, so the count was accidentally right at 38 fixtures and would have become a silent ceiling at 101 — not an error, not a blank, just a smaller number rendered with full confidence, guarded only by a `console.warn` the runner discards. The count now rides on the saved-search record as `matchCount`, from `SavedSearchService.countMatchingNow` — a `SELECT count(*)` with no page to be truncated by. Proven live, because the mock version could only watch one in-browser function agree with itself about a catalogue the same process held in memory, and so could not fail for the reason the bug existed. Asserted as cross-surface equality against the number the server actually returned (the two screens used to derive it separately and were one edit away from disagreeing), a strictly-decreasing city→locality→BHK chain that no single fetched page can produce, and the cases that must report nothing: an unlisted locality, a switched-off alert (which the server still counts — the surfaces are hiding a number that exists), and a `flatmates` alert with criteria identical to a listings alert that counts three | saved-alerts | consumer/property/live-alert-match-count | ✅ |
| Visits — book, confirm, reschedule (moves the slot in place and resets to scheduled, `PATCH /visits/{id}/slot`, D87), cancel; caller-scoped | property-detail | consumer/property/live-scheduled-visits | ✅ |
| A buyer viewing their booked visit gets no WhatsApp handoff to the owner — the owner→visitor `wa.me` link is owner-only, and masked numbers never form a link (D5, global policy) | property-detail | consumer/property/live-scheduled-visits ("a buyer viewing their booked visit gets no WhatsApp handoff to the owner") | ✅ |
| **The owner earns the visitor's number by confirming, not by being posted to** — `visitorMobileVisibility` reveals it on `confirmed`/`completed`/`no-show` and masks it on `scheduled` and `cancelled`, so booking a visit is not a way to harvest a number and neither is cancelling one; rescheduling re-masks, because the agreement was to a slot rather than to a person. The reveal reaches the screen without a reload, because the dashboard re-reads the bundle after a status write instead of trusting its own optimistic patch. Mock mode could not fail either half: it stored the visitor's mobile in the clear under a key the test wrote itself | property-detail | consumer/property/live-scheduled-visits ("the owner earns the visitor number by confirming, and the handoff appears without a reload") | ✅ |
| Flatmate seeker post can be taken down ("Mark filled"/"Delete") to relieve the live-post cap, soft-archived via `DELETE /flatmates/posts/{id}` (D71) | flatmates | consumer/flatmates/guardrails | ✅ |
| **The `/flatmates` board splits by "is there an address yet?", against the live API** — places (rooms, and groups that carry a `propertyId` or `society`) on Move-in, people on Team up, legacy `?view=` deep links resolving to the right one, and an empty tab offering the other rather than a dead end. Converted from the mock file, and three of its tests changed subject in the process: the vacant-flat disclosure, the "Master bedroom" label and the split-price line all read fields **no client may set** — `roomKind`, `priceBasis`, `shareMax` are absent from `FlatmateRoomCreate` on purpose, since a client that could name its own `priceBasis` could price a shared bed as a private room. The only writer is `POST /properties/{id}/split`, so the spec performs the whole act: post a rent listing → Ops approve → split into one master bedroom → moderator publishes. The budget test is the one that gained most: the mock could only say "a card is still here", while this names the split room by id **and** names a seeded per-person room, above the same budget, that is correctly gone. Runs on both viewports (its `CROSS_VIEWPORT` entry moved to the live config's `mobile` project) | flatmates | consumer/flatmates/live-discovery (`--config=playwright.live.config.js`, chromium + mobile) | ✅ |
| **Caller-scoped flatmate state comes from the API, never browser storage.** `/me/flatmate-posts` powers the Dashboard inventory and the Flatmates own-request banner/exclusion, including a pending post that the public feed must hide; `/me/flatmate-interests` restores the sender's CTA state across devices while the host sees none of that outbox. The mock intentionally retains per-browser tap memory so its duplicate-409 contracts remain exercisable. | flatmates | consumer/flatmates/live-my-flatmate-state, consumer/flatmates/discovery (mock-only keeper) | ✅ |
| Flatmate posts, rooms and groups start at `mod_status = pending` and are invisible to everyone but their author until a moderator approves; the author's banner says "in review" instead of "live", and public feeds filter on a whitelist so a new state fails closed (D72) | flatmates | consumer/flatmates/moderate-before-public | ✅ |
| All three interest doors (seeker post, room, group) go through the seam rather than a local flag, so the ask reaches the provider and is visible to the host (D181) | flatmates | consumer/flatmates/interest-api ("a first room enquiry reaches the provider and the card flips to sent") | ✅ |
| A repeat ask is the API's benign `already_interested` 409 and reads as information, never a red "something went wrong" — asserted on the toast's own variant class, because the words are what a future edit changes (D175, D181) | flatmates | consumer/flatmates/interest-api ("the same seeker on a second device is told the owner already holds the enquiry, not that a thread is waiting") | ✅ |
| That benign 409 also leaves the *device* where the success path would: the bell entry and the Messages hand-off are written on both paths, so a second device's "Interest sent" card is no longer sitting over an empty inbox (D183). The inbox assertion goes through the **Requests** tab — a fresh ask is `state: 'pending'`, and `Messages.inTab` keeps Chats for threads the other side has answered | flatmates | consumer/flatmates/interest-api ("a second device that never made the enquiry still ends up holding the thread behind its sent card", "a second device that never made the join request still ends up holding the thread") | ✅ |
| …and it is idempotent on `propertyId`, checked against both the hand-off queue and the conversations it is drained into, so the device that *did* ask never grows a second thread (D183). Both places are load-bearing: `lib/chat.js` *removes* `pnPendingRequests` the first time Messages mounts, so a queue-only guard would duplicate for anyone who had already opened their inbox | flatmates | consumer/flatmates/interest-api ("a repeat enquiry does not give the device that made it a second thread") | ✅ |
| `group_full` is a *different* 409 on the same door and gets its own refusal message + error tone; the client no longer pre-checks seats, so a group that fills while the board is on screen is answered rather than mis-labelled (D181) | flatmates | consumer/flatmates/interest-api ("a group that fills while the board is on screen answers group_full, not already-asked") | ✅ |
| That refusal also *corrects* the stale board it came from, so the card returns as Full rather than re-offering a Join button that can only be refused again (D181) | flatmates | consumer/flatmates/interest-api ("a group that fills while the board is on screen answers group_full, not already-asked") | ✅ |
| When both conflicts are true at once, seats are answered before duplicates — "you already asked" would imply a seat is still waiting on the host (D181) | flatmates | consumer/flatmates/interest-api ("a seeker who already asked is told the group is full, not that they already asked") | ✅ |
| An ordinary (non-409) failure rolls the optimistic flip back, so the card never claims a done-state for an ask that did not happen (D181) | flatmates | consumer/flatmates/interest-api ("an ordinary failure rolls the card back instead of leaving it claiming success") | ✅ |
| The done-state still survives a reload — the page remembers its own asks — but that memory never short-circuits the call, which is what made the 409s unreachable before (D181) | flatmates | consumer/flatmates/prefreeze ("the group done-state survives a reload (dedupe is persisted)"), consumer/flatmates/interest-api | ✅ |
| That memory is per-signed-in-user, so the next person on a shared browser neither sees the previous one's asks nor is left with no button to press (D181) | flatmates | consumer/flatmates/interest-api ("the next person to sign in on a shared browser does not inherit the first one's asks") | ✅ |
| `/plans` | plans-billing-refer | consumer/services/live-plans-checkout | ✅ |
| `/checkout` | plans-billing-refer | consumer/services/live-plans-checkout | ✅ |
| `/refer` | plans-billing-refer | refer, referral-rewards | ✅ |
| The freemium listing ceiling and the count measured against it both come from the server (`GET /me/entitlements` and `GET /me/listings`), so a fresh browser that has posted nothing still paywalls an owner who is over quota. Both halves used to be the browser's own opinion, and a second device reported zero listings used — the paywall was, in effect, a paywall against clearing your cookies | plans-billing-refer | live-listing-quota ("the server, not the browser, decides the ceiling and the count", "an over-quota owner is paywalled on a browser that has never posted") | ✅ |
| An owner with nothing listed is not paywalled, and neither is one whose entitlements call fails — the quota is a mirror of a gate `POST /me/listings` enforces anyway, so it fails permissive rather than blocking someone entitled to post | plans-billing-refer | live-listing-quota ("an owner with nothing listed still gets their first post free") | ✅ |
| `POST /me/listings` refuses an over-quota post with `422 listing_quota_exhausted`, and the message names the arithmetic ("1 of 1") so the owner can tell whether taking a listing down would help. Until D234 this rule lived only in the browser, which meant it did not exist — the endpoint accepted any number of listings from any owner | plans-billing-refer | live-listing-entitlements ("the free tier is one live listing, and the refusal says so", "the seeded owner is over their ceiling and is told the numbers") | ✅ |
| `DELETE /me/listings/{id}` takes the owner's own listing down and frees its slot immediately, soft-archiving rather than destroying. Enforcing a ceiling without an exit would have made the free tier one listing *ever* — a `PATCH` cannot set `status`, by design, so before D234 there was no way back under the limit except to buy a plan | plans-billing-refer | live-listing-entitlements ("taking a listing down frees its slot") | ✅ |
| Take-down is owner-scoped and answers 404 rather than 403 for somebody else's listing, so the endpoint is not an existence oracle for anyone holding a uuid | plans-billing-refer | live-listing-entitlements ("one owner cannot take down another owner's listing") | ✅ |
| `GET /me/entitlements` reports `agreements.free`, the free rent agreements referrals have earned — derived per request from qualified referrals rather than stamped, so a clawback takes the perk back with it. It was a local division of a localStorage counter, which survived clawbacks and could be re-minted by clearing site data | plans-billing-refer | live-listing-entitlements ("a user with no qualified referrals has earned none") | ✅ |
| `/tenant-profile` | rent-tenancy | tenant-profile | ✅ |
| `/pay-rent` + tenancy | rent-tenancy | pay-rent, live-my-rental, live-tenant-finances | ✅ |
| The tenant hub and the Rent Wallet **against the live API** — the rented home, the owner's name, the rent and the payment history all come from `GET /me/tenancies` and `GET /me/rent-payments`, and the demo-rental seeder that used to write a tenancy into localStorage is gone | rent-tenancy | live-my-rental | ✅ |
| A **failed** rent payment is shown as failed and excluded from lifetime rent, from the on-time count and from the downloadable Rent Passport. The local store only ever held successes, so all three counted every row — the last of them inside a document the tenant hands to a landlord | rent-tenancy | live-tenant-finances | ✅ |
| The Verified-Tenant meter reads the **server's** score and shows a dash, not `0%`, before it arrives — "not known yet" and "you scored nothing" are different claims about a person | rent-tenancy | live-my-rental | ✅ |
| `/schedule-visit` | schedule-visit | live-scheduled-visits | ✅ |
| `/societies` `/society/:slug` | societies | consumer/society/live-community, consumer/society/live-community-replies, consumer/society/live-community-board, consumer/society/live-community-whatsapp, consumer/society/live-community-moderation, consumer/society/live-location, consumer/society/live-tabs, consumer/society/live-select, consumer/society/live-society-suggest, consumer/society/live-rera-catalogue, consumer/society/live-follows | ✅ |
| The society hub offers the **location correction to a verified resident and to nobody else** — `iAmResident` is read from `GET /societies/{slug}/membership`, so the button appearing at all means that account's flat survived the round trip. Proved against a signed-in account with no flat in the building, which clears every check weaker than residency; the two tests are the same page and the same shape of account, differing only in the one fact the gate is about | societies | consumer/society/live-location ("a verified resident is offered the correction, and the pending row is read back from the server", "a signed-in stranger is not offered the correction at all") | ✅ |
| A submitted correction **reaches the server and is read back from it**: the dialog POSTs `kind: 'location'` to `/societies/{slug}/proposals` for a 201 `pending`, and the "under review" chip is still there after a full reload — where the only place it can come from is `getSocietyProposals`. The retired mock twin wrote `pnSocietyLocationFixes` to localStorage, so its chip proved the browser remembered its own click | societies | consumer/society/live-location ("a verified resident is offered the correction, and the pending row is read back from the server") | ✅ |
| **The pin the page draws is not the pin the server holds.** `useSocietyHub` builds the society from `resolveSociety` — the bundled 348-row catalogue — and nothing merges the detail response over it, so `lat`, `lng` and `locSource` (exactly the three fields an approved correction writes) are read from a JS module. An approved pin cannot move the Get-directions link or raise the "Pin confirmed by a verified resident" caption until the catalogue migrates; the server-side claim is proved at `live-society-proposals` ("an approved pin moves the society and says a neighbour moved it") and is deliberately **not** re-asserted in a browser, where it would be asserting the bundle | societies | live-society-proposals (`--config=playwright.live.config.js`) | 🟡 catalogue unmigrated |
| Following a society is an **account** fact, not a browser one: a follow made on the directory survives a reload on that society's **hub**, which is a different read from the directory's — the card carries `followedByMe` on the row the listing returned, while the hub asks about one society on its own. A hub offering "Follow" over a society the directory calls "Following" is the exact disagreement the port was for (D227) | societies | consumer/society/live-follows ("a follow made on the directory is what the society hub shows") | ✅ |
| The dashboard **tile** that counts follows and the **panel** that lists them are two readers of one server fact and must agree: two follows means a tile reading `2` and a panel naming both societies. They used to read one localStorage array independently, which is how they came to disagree | societies | consumer/society/live-follows ("the dashboard follow tile counts what the panel lists") | ✅ |
| Adding a society nobody has listed yet **mints a real row** — `POST /societies` — and the follow that follows it is an ordinary `PUT /me/societies/{slug}/follow`, not a local placeholder. The retired mock twin asserted the opposite (a browser-held follow retried on every load); `pnLocalSocietyFollows` is drain-only now, and this spec asserts it stays empty | societies | consumer/society/live-follows ("adding a society nobody has listed yet mints a real row, and the follow is an ordinary server write") | ✅ |
| Signing out empties the follow set rather than leaving the previous account's — on a shared machine one person's followed societies must not render as the next person's. Proven adversarially: a real UI log-out, then a second fresh account on the same browser is offered `Follow` on the society the first one followed | societies | consumer/society/live-follows ("signing out empties the follow set rather than leaving it for the next account") | ✅ |
| Unfollowing on one surface clears it on the others, and the directory offers to follow again — a stale "Following" badge claims an alert the user will never get | societies | platform/live-society-follow ("a follow on the directory is written to the server and read back from it") | ✅ |
| Society follows **against the live API** — the follow reaches `PUT /api/me/societies/{slug}/follow` and survives a reload because `GET /api/me/societies/following` returns it, which is the read that did not exist while the writes sat unused. Asserted on observed request traffic, not on rendered state, because a mock-shaped store would render identically while writing nothing (D227) | societies | live-society-follow (`--config=playwright.live.config.js`) | ✅ |
| A `/society/:slug` in the RERA catalogue renders *that row's* real name, locality and specifications — the assertion that catches a silently-empty catalogue, because a fabricated page produces plausible values but never the right ones (D129) | societies | consumer/society/live-rera-catalogue ("a RERA society hub renders that row's real name, locality and specifications") | ✅ |
| A slug that is **not** in the catalogue renders an honest placeholder — no invented specs, no "Society Verified" badge, no community-estimate rating | societies | consumer/society/live-rera-catalogue ("an unknown slug renders an honest placeholder, not a confident society") | ✅ |
| Society reviews are keyed on the **slug** everywhere, so the directory card, the property-page society block and the hub agree on the same review bucket | societies | consumer/society/live-rera-catalogue ("the societies directory card reports reviews written against the society", "the property page society block reports reviews written against that society", "a review posted on the hub shows up on that society's directory card") | ✅ |
| A society with **no** reviews says so on its directory card and invents no number to say it with — no zero, no placeholder average, no parenthesised count. The guard against the D101 class of defect, where a fabricated value renders as a confident claim about a building nobody rated | societies | consumer/society/live-rera-catalogue ("a society with no reviews says so, and invents no number to say it with") | ✅ |
| The **property page's** society block says "Not rated yet" for an unreviewed society rather than drawing a hard-coded 4.2 star strip, and reads its aggregate from the review seam rather than a localStorage bucket a live session never writes (D195) | societies | consumer/society/live-rera-catalogue ("the property page society block invents no rating for a society nobody has reviewed") | ✅ |
| The societies directory rating **against the live API** — the card reports the `avgRating`/`reviewCount` the server aggregated in grouped SQL on `GET /societies`, not a reduction of the browser's own store (which a live session never writes, so the old path rendered every society unrated). The fixture is minted by the spec through the review composer, but only when the server's count is still 0, because `ReviewService` allows one review per author per target; the assertion is on the server's re-read, so a green tick cannot mean "the catalogue was empty and the card correctly said nothing" | societies | live-society-rating (`--config=playwright.live.config.js`) | ✅ |
| When the live rating read **fails**, the directory card says the rating is unavailable rather than claiming the society is unrated — a failed request and a building nobody reviewed are different facts and must not render as the same one | societies | live-society-rating (`--config=playwright.live.config.js`) | ✅ |
| The society review composer collects the **five aspects the hub draws bars for** — Safety, Maintenance, Management, Amenities, Connectivity — and none of the property vocabulary. The ids are stored keys shared by the composer, the bars and the server's `ReviewCategories.SOCIETY_KEYS`; a rename in any one of them orphans every rating written under the old id | societies | consumer/society/live-review-aspects ("the society composer offers the five aspects the hub draws bars for, and no others") | ✅ |
| Rating two aspects draws **exactly** those two bars, at the values rated, and no bar at all for the three skipped ones. Presence was what the defect already satisfied — every bar rendered a plausible number that was `baselineBars` alone, because the composer sent no categories and the server's vocabulary was the property one for every target — so this assertion could only become a presence one once D197 deleted the baseline. `toEqual` on the whole bar map is what catches an unrated aspect being folded in as a 0. Live, the rated values (5 and 1) are both unequal to the overall rating (4), so the vocabulary has to survive `POST /reviews/society/{slug}` → `ReviewCategories.SOCIETY_KEYS` → the summary read intact — a server that stored the overall against every category, or filtered the society keys back out on the read as the original defect did, produces a grid of 4s or no grid at all. That half was unassertable against the mock provider, which handed back the client's own vocabulary | societies | consumer/society/live-review-aspects ("rating two aspects draws exactly those two bars, at the values rated") | ✅ |
| A review with **no** aspects rated draws no bars at all: aspects are optional, the review still counts toward the headline (asserted via `5/5`, so "no bars" cannot pass for a page that never updated), and a grid it contributed nothing to must stay empty. "Average over everyone" and "average over everyone who answered *this*" look identical until somebody skips a row (D197) | societies | consumer/society/live-review-aspects ("a review with no aspects rated draws no bars at all") | ✅ |
| Per-aspect society ratings **against the live API** — `categoryAverages` comes back keyed on the society vocabulary only (a property key surviving into a society's aggregate is the defect this replaced), and, when the spec wrote the fixture itself, the two rated aspects carry their values while the three skipped ones are **absent rather than 0**. The same two facts are then read off the **rendered** `society-bar-*` cells, because an API-only assertion cannot catch a client mapper: `http/reviewMapper.js` filtered `categoryAverages` through the property vocabulary for every target, so a correct payload still arrived at the page as `{}` — invisible while `baselineBars` drew five plausible bars over the hole, and invisible to the suite because the mock provider is target-aware (D197) | societies | live-society-rating (`--config=playwright.live.config.js`) | ✅ |
| Resident verification **against the live API** — a flat is one physical unit, so the server normalises wing+flat into one key with separators stripped (`B-704`, `b 704` and `B704` are the same home) and a partial unique index refuses a second *verified* resident in it. A competing request is still recorded and flagged rather than refused, because the server cannot tell a handover from an impostor and the committee can; rejecting the outgoing resident releases the flat, so a sale is not a support ticket. This is the state that lived in `pnSocietyResidents` until D240 — a resident who verified their flat on a laptop was a stranger on their phone | societies | live-society-residency (`--config=playwright.live.config.js`) | ✅ |
| `GET /societies/{slug}/membership` answers **without a token** so the hub can render the "claim this society" invitation on first paint, and withholds the claimant's mobile and email from that public read while the ops queue publishes them — otherwise "who claimed my society" becomes a way to lift a committee member's number off a page anybody can load | societies | live-society-residency (`--config=playwright.live.config.js`) | ✅ |
| Approving a society claim moves **three things in one transaction**: the claim row, the society's own `claimStatus` badge (a second copy of the fact that the directory card reads), and the still-pending residency requests, which re-home from the ops queue to the committee's. Already-decided rows keep their queue — the record of who decided is not ours to rewrite. The approved claimant *is* the society admin; a neighbour, verified or not, still gets a 403 on the queue, which carries names and mobiles | societies | live-society-residency (`--config=playwright.live.config.js`) | ✅ |
| One live claim per society: a second committee gets a 409 from a partial unique index rather than a read-then-write race, while the sitting claimant re-posting is treated as a correction to their own pending claim and succeeds | societies | live-society-residency (`--config=playwright.live.config.js`) | ✅ |
| The seeded fixture carries **both** residency queues — Blue Ridge Towers is claimed so its pending requests sit with the committee, Kumar Palaash's claim is still with ops so its residents queue to ops too. Two societies rather than one because "assigned_to is always committee" passes every assertion a single claimed society can make | societies | live-society-residency (`--config=playwright.live.config.js`) | ✅ |
| Society Q&A **against the live API** — a question is readable with no token at all (the person with the most to ask about a building has not moved into it, so a Q&A only residents can read cannot help the person it exists for), the answer carries a resident badge, and the payload names its author without ever carrying a mobile — a question is not a transaction, so there is nobody here for a reader to ring. This is the state that lived in `pnSocietyQA` until D240: every answer a resident wrote to a prospective buyer was stored in the resident's own browser and read by nobody | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| The resident badge is **recomputed on every read, never stored**: rejecting a resident retracts the badge from everything they had already written. The browser-local version froze `resident` at posting time, so somebody the committee later found had never lived there kept wearing "verified resident" on every answer they had ever given — the one thing a trust badge must not do | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| The noticeboard is **resident-gated for writing and public for reading**, and `canRemove` is computed per viewer on the server: a stranger posting gets a 403, a verified resident can post and take their own item down, a *neighbour* — equally verified — cannot, because residency buys posting and not moderation, and an anonymous reader is handed `canRemove: false` rather than a delete button that 403s. An active noticeboard is the most honest signal a society page can give somebody deciding where to live, so the read must not need an account | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| Board ordering is **two orderings in one list** — dated events first by when they happen, then undated notices newest-first — asserted with the notice written *last*, which is exactly the case a naive newest-first board gets wrong by burying next week's AGM under a note about the lift. Filtering by `kind` narrows it and an unknown `kind` is a 400 rather than a silently empty board | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| An **event without a date is refused** (400, backed by a database check constraint) because it would sort into the calendar and render an empty date cell — a broken page rather than a rejected write — while a **notice that arrives with a date has the date dropped** rather than refused, since a 400 there would point at a field the composer does not draw. An unknown `kind` is refused outright | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| Reading is open and writing is not, asserted as the asymmetry it is: an unauthenticated question or notice is a 401 while both reads still answer 200 on the same society in the same test. An answer posted to another society's question **through this society's URL** is a 404 — without that check it would be written and then be invisible, because the hub reads answers through the question's own society | societies | live-society-community (`--config=playwright.live.config.js`) | ✅ |
| The community tab **against the live API** — a tip, a trusted pick and a photo are all readable with no token at all. Every one of them used to live in `pnSocietyContributions` in the author's own browser, so the tab showed each visitor a community of exactly one person: themselves | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| A recommended tradesman's **phone number is withheld from a reader with no account** while the rest of the recommendation stays public. The number belongs to a third party who never agreed to be on the open web; a sign-in wall costs a genuine neighbour one tap and costs bulk harvesting the whole exercise | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| "Helpful" is **`PUT`/`DELETE`, not a toggle**: the same `PUT` sent twice is still one vote, so a request retried after a dropped connection produces the state the tap intended instead of silently undoing it. Withdrawing a vote never cast is a no-op rather than a 404, and the response carries the new count so the button can update without re-reading the page | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| **Helpfulness beats recency** — a newer tip leads the list until somebody weighs in on an older one, asserted with the vote cast *after* both were posted, which is the case an ordering that only reads `created_at` gets wrong | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| Each kind carries its **own minimum** (a tip needs words, a pick a name, a photo a URL) and a field belonging to another kind is **dropped rather than refused** — a 400 would point at something the composer does not draw for that kind, and a contact detail on a row no screen renders offers nobody a way to have it removed | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| A reply belongs to **its own author, not to the tip it sits under**: the tip's author gets a 403 removing somebody else's reply. Owning a tip does not make you the moderator of the conversation about it | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| Removal is author-or-staff and **takes the thread with it** — a neighbour gets a 403, the author gets a 204, and the replies underneath go too, because a conversation under something invisible answers a question nobody can see. Staff moderate any society, including an unclaimed one with no sitting committee | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| Writing needs an account and reading does not, asserted as the asymmetry it is on one society in one test; and a contribution **reached through a different society's URL is a 404** — without that re-check a card could be voted on or deleted somewhere the result would never be visible, since the hub only ever reads a society's own list | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| The seeded fixture carries **all three kinds, a vote by somebody other than the author, and a thread**, so a fresh database renders a populated tab rather than an empty state — and the photo is an `https` URL, never a data URI, since base64 in localStorage is exactly why a shared photo used to be invisible on every device except the one that shared it | societies | live-society-contributions (`--config=playwright.live.config.js`) | ✅ |
| An **approved detail suggestion reaches the catalogue**, and the columns it left out are untouched — asserted with a deliberately partial suggestion. The suggestion used to be written to `pnSocietySuggestions` in the author's own browser, and the ops queue meant to review it read the *reviewer's* browser, so it was permanently empty however many residents filled the form in | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| Suggesting details is **open to anyone signed in**, with no verified flat anywhere — demanding a resident before accepting a builder name is what leaves a bulk-imported society blank forever, and the response says so by reporting `authorIsResident: false` | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| The **WhatsApp invite is withheld from anyone without a verified flat, approved or not**: a signed-out reader and a signed-in outsider are both told the group exists (`whatsappAvailable`) and handed no URL, while the resident of C-1201 gets it. The invite is a key to a private resident space, and an account is not a flat | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| Posting the group link needs a verified flat (403 for a stranger), and the URL must match `https://chat.whatsapp.com/…` **anchored at both ends** — `https://evil.example/?x=https://chat.whatsapp.com/AAAAAAAA` is refused, which is exactly the link an operator glancing at a queue would wave through | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| An **approved pin moves the society and stamps `locSource: 'community'` in the same write** — a coordinate a neighbour walked to and one lifted from a RERA filing are both coordinates, and only one of them has been to the building. Every society imported with a bad pin used to stay wrong for everybody however many residents corrected it on their own screen | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| A **valid coordinate in another city is refused** (Delhi, on a Pune society) — a stray map drag used to be able to relocate a society across the country | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| **Re-proposing corrects your own pending row** (same id back, still 201), while somebody else's pending submission answers 409 rather than being overwritten — overwriting would discard their work silently | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| A **decision is final**: the second one is a 409 and the approved value survives the attempted reversal. A rejection changes nothing about the society at all | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| The **ops queue publishes the invite it exists to screen** and no mobile numbers — an operator cannot screen a link the response redacts, and a phone number is not needed to decide whether a link is a scam | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| The queue is staff-only (403 for an ordinary account), proposing needs an account (401 anonymous), and an **empty suggestion is refused rather than queued as a no-op** that would cost somebody a decision for nothing | societies | live-society-proposals (`--config=playwright.live.config.js`) | ✅ |
| A **minted society reaches the shared catalogue**: a second, signed-out caller can open it by slug and find it by search. Every member-added society used to be written to the adding browser's `localStorage`, so it existed for exactly one human being — nobody else could find it, follow it or list a flat in it, which is the only reason anybody adds one | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| A minted row arrives as `source: 'community'` with **`verifiedAt` null** — a society somebody typed in must stay distinguishable from one we imported, or the hub cannot caption it honestly | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| **Adding a society that already exists answers 200 with the row that exists**, not 201 and not an error — asserted across two different accounts with different case, different spacing and **no locality at all**, which is the case a slug-only guard waves through because the slug folds the locality in. The duplicate it would create is unrecoverable without a manual merge: listings, follows, reviews and residency claims pile up against both slugs | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| A name that slugifies to nothing (`!!! ???`) is refused with 422 — a society at `/society/` is one nobody, including its author, could ever open — and minting anonymously is a 401, because the row records who added it and that is what makes one account minting fifty societies visible rather than merely suspected | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| The **ops candidates queue holds member-added societies and nothing else**: every row is `source: 'community'` with a null `verifiedAt`, the seeded candidate is in it and the seeded confirmed society is not. The queue used to read the *operator's own browser* and was permanently empty, so not one member-added society had ever been confirmed however many people added one | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| The queue is staff-only — an ordinary signed-in account gets 403 | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| Verifying stamps the society, removes it from the queue, and **leaves `registration` and `conveyance` false**. Those describe the building's legal paperwork rather than our confidence in the record; the browser code this replaces set both, so confirming that a society exists silently told every buyer its conveyance deed was done | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| A **second verification is a 409**, not a silent overwrite — the record of who verified it is the only thing that says who to ask about the society later, and it is the question that gets asked the day two operators disagree | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| A MahaRERA society cannot be verified (422 — it is not a candidate, and an operator asked to confirm 320 bulk imports is an operator who stops reading the queue), and an unknown slug is a 404 | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| An **unrecognised locality is dropped to null rather than stored** — `locality_slug` is a foreign key, so an invented area would be a 500 on a correctly-filled form, while an unplaced society is a state the schema already allows | societies | live-society-minting (`--config=playwright.live.config.js`) | ✅ |
| **Reporting society content reaches a shared queue at all.** Every recommendation, reply, question, answer and noticeboard item carries a Report control; pressing it wrote a row to `pnSocietyReports` **in the reporting member's own browser**, and the ops queue that was supposed to read those complaints read the *moderator's* browser. The queue was empty by construction. All five surfaces are now filed against the platform `reports` table | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| The reason is validated against the **society vocabulary**, not a global list — `pricing` on a recommendation is a 400, because it is a complaint about a listing and means nothing here | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **`personal` exists only for society content**, and is refused on a property. It names what this surface actually attracts: a real tradesman's real mobile number, published by somebody who is not him — the complaint the old free-text box could only express as prose, and which the queue would have skimmed past as `other` | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| The **duplicate guard is per reporter**: the same person twice is a 409, a second neighbour is a second real complaint. Fifty neighbours reporting one post used to be fifty rows in fifty different phones, countable by nobody | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **Upholding a complaint actually takes the post off the hub** — `actioned` + `hide_content` stamps `removed_at`/`removed_by` and the post is gone from the public read. `actioned` used to be a word: the report changed state and the content stayed up | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **Removing a question takes its answers with it and leaves the neighbouring question alone** — a thread whose question has gone is a page of replies to nothing. Removing an *answer* leaves the question standing and the other answers visible | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **A second moderator clearing the same target succeeds rather than 409ing** — two neighbours reporting one post is the ordinary case, and a queue that cannot close the second complaint accumulates them forever. The first moderator's name is not restamped: it is the only thing that answers an appeal | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| A decision **cannot enforce unless it is upholding the complaint** ("dismissed, and also taken down" is not a decision anybody means), and `suspend_account` is refused for society content rather than accepted and silently discarded. Neither refusal touches the post | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| The queue is **staff-only** (403 for the member who filed the report — an empty moderation queue reads as "nothing to do") and **filters by society kind**, with an invented kind refused rather than quietly returning everything | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **Living in the society does not make you a moderator** — the resident who filed the complaint cannot decide it, however close to home the post is | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| A complaint about content that never existed **fails at the decision, not at filing** — a report is a safety signal and losing one to a stale id is worse than filing one that cannot be upheld | societies | live-society-reports (`--config=playwright.live.config.js`) | ✅ |
| **The hub page itself reaches the server, not just the endpoints.** Six society specs drove the API directly and could not have noticed that the page never called it; a question asked through the composer now survives a reload and comes back attributed to the account that asked it | societies | live-society-hub (`--config=playwright.live.config.js`) | ✅ |
| **Helpful is a server count.** Pressing it twice settles at one, and the pressed state and the count both survive a reload — it used to be a number this browser kept about itself, so two neighbours each saw a tip nobody else had found useful | societies | live-society-hub (`--config=playwright.live.config.js`) | ✅ |
| The report dialog **names the thing that was clicked**. It knew three of the six kinds and fell back to "review", so reporting a neighbour's recommendation asked you to confirm a complaint about "this review" | societies | live-society-hub (`--config=playwright.live.config.js`) | ✅ |
| A report filed **through the page** lands in the ops queue as `society_contribution` carrying the reason code the reader picked. The consumer half and the moderator half are read in one test on purpose: the dialog closing only proves a 2xx, and the previous implementation returned 2xx while filing the complaint against a property id | societies | live-society-hub (`--config=playwright.live.config.js`) | ✅ |
| **Each Add button opens its own form**, and what that form files is on the feed after a reload. The dialog is named after the button that opened it, so "Add local pick opened the local-pick form" is a claim — a shared generic title passed while all three opened the tip form. The badge over a stranger's tip reads `Verified`, not `Resident`: every row now carries one or the other, so the mock's "three Verified badges" (read as identity-verified authors) no longer means what it said | societies | consumer/society/live-community ("each Add button opens its own form, and what that form files comes back on the feed") | ✅ |
| A signed-in member who has **verified nothing** is let straight through to the contribute form — ADR-019 badge-not-gate, on the society hub. An absence claim, so it is gated on the contribute dialog actually opening: a form that never opened would satisfy "no Aadhaar wall" trivially | societies | consumer/society/live-community ("a member who has verified nothing contributes directly — signing in is the only floor") | ✅ |
| The **filter chips narrow the feed and their counts add up** — `All (3)` / `Tips (2)` / `Local picks (1)` on a society nothing else in the run writes to, so the numbers are exact. A chip that says "Tips (2)" and then shows one is invisible to a test that only checks what is on screen; and each filter is asserted both ways, since filtering down to nothing satisfies the absence half on its own | societies | consumer/society/live-community ("the filter chips narrow the feed, and their counts add up") | ✅ |
| The **remove control is drawn only where the server says it may be**. The row arrives carrying `canRemove`; the mock computed it from mobile equality in the browser. A neighbour's tip — same society, same feed, rendered right there, excluded for no reason but authorship — is asserted present before the control is asserted absent, and the author's own removal is re-read after a reload. A page that draws the button anyway hands a neighbour something that 403s: the server holds, but the UI has lied | societies | consumer/society/live-community ("the remove control is drawn only where the server says it may be") | ✅ |
| A **reply lands under the tip it was written for**, and is still there after a reload. A second tip sits in the same feed as a decoy, so "the reply is on the page" cannot pass for "the reply is on the right thread" — the retired mock kept replies in an array this browser owned, where a thread could not be mis-attributed because there was only ever one reader. Ownership of the reply itself is `live-society-contributions` L190 | societies | consumer/society/live-community-replies ("a reply filed under a neighbour's tip belongs to that tip, and is still there after a reload") | ✅ |
| A signed-in member who has **verified nothing reaches both the reply box and the report dialog** — ADR-019 badge-not-gate, on the two community controls that most look like they should want KYC. Both halves are positive: the reply box is typed into and the report dialog is opened by name, so a page that rendered neither would fail rather than quietly satisfy "no Aadhaar wall" | societies | consumer/society/live-community-replies ("a member who has verified nothing reaches both the reply box and the report dialog") | ✅ |
| A verified resident's **event lands on the day it is dated**, and a notice lands on the board — both re-read after a reload. The calendar dot is checked on the day *and* counted on the next one: a page that dotted every day would satisfy "the event is on the 20th" on its own. The resident badge is matched inside the byline row rather than as an exact node, because it is rendered as a fragment of one concatenated line. Who may post is `live-society-community` L147; ordering is L194; the date rules are L232 | societies | consumer/society/live-community-board ("a verified resident posts an event onto the day it is dated, and a notice onto the board") | ✅ |
| A signed-in **non-resident gets no Add controls, and is told why** — not shown an empty board with no explanation. An absence claim, so it is gated on two positives first: the board heading, and the sentence naming verified residents and the committee. The server's refusal is `live-society-community` L147; this is the half that decides whether the page bothers to say so | societies | consumer/society/live-community-board ("a signed-in non-resident gets no Add controls, and is told why rather than shown an empty board") | ✅ |
| A resident who has **just posted the group link is told it is under review** — and gets no Join button in the meantime. The pending sentence is the positive anchor for the absent link, and both survive a reload, because the card reads three different fields off one proposals call and a pending row that leaked into the approved branch would hand out an unscreened invite. That the link is withheld from everyone until approval is `live-society-proposals` L138; the URL rules are L179 | societies | consumer/society/live-community-whatsapp ("a resident who has just posted the group link is told it is under review, and is given no join button yet") | ✅ |
| An **approved invite reaches a verified resident as a link that is safe to click** — `href` exactly the invite, `target="_blank"`, and a `rel` carrying `noopener`. Read by a *second* resident, not the author, so the card cannot be passing on the author's own echo of what they submitted. Ops approve over the API rather than through their console, which `live-society-proposals` L286 already covers | societies | consumer/society/live-community-whatsapp ("an approved invite reaches a verified resident as a link that is safe to click") | ✅ |
| A signed-in **non-resident is told the private group exists, and the invite is nowhere on the page** — the teaser and the "verify you live here" button are asserted first, then the Join link is absent and the invite string is absent from the whole document body. Checking the rendered link alone would miss an invite sitting in a prop, a data attribute or a script payload, which is exactly how a private link escapes | societies | consumer/society/live-community-whatsapp ("a signed-in non-resident is told the private group exists, and the invite is nowhere on the page") | ✅ |
| The **second complaint from the same person is answered, not silently swallowed**. The duplicate guard is the server's (`live-society-reports` L175); what only a browser can show is that the reader is told their report is already in hand instead of being thanked again for one that was thrown away. The first press's confirmation is the anchor — without it, a dialog that failed both times would look identical to a working guard | societies | consumer/society/live-community-moderation ("the second complaint from the same person is answered, not silently swallowed") | ✅ |
| **A post removed from the ops queue is gone from the society page a neighbour is reading** — the two consoles agree. Filed in the consumer hub, upheld in the moderation queue, then re-read on the hub by the same reader. A second tip on the same society is asserted still present afterwards, so "the reported post is gone" cannot be satisfied by a page that dropped the whole feed. The queue row is found by the reporter's own note because it deliberately carries no copy of the offending text. The server-side removal is `live-society-reports` L194 | societies | consumer/society/live-community-moderation ("a post removed from the ops queue is gone from the society page a neighbour is reading") | ✅ |
| **Details offered for a society we have never confirmed are held for review, not printed as fact** — and the hub still says so after a reload, which is what separates a proposal on the server from a value this tab happens to be holding. The pending badge replaces the "Help verify" invitation, so the same visitor is not asked twice; the submitted builder and unit count are asserted absent from the page both before and after the reload, because a hub that repeated them would be making the same unbacked claim the fabricated-`genericSociety` bug made, only with a stranger's numbers. The server rules are `live-society-proposals` L100 (an approved suggestion reaches the catalogue), L127 (anyone signed in may file one) and L322 (an empty one is refused). Replaces the localStorage-era assertion that read `pnSocietySuggestions` back out of the tab that had just written it | societies | consumer/society/live-society-suggest ("details offered for an unconfirmed society are held for review, not printed as fact") | ✅ |
| **The verified badge is a catalogue fact, and a society nobody has confirmed does not wear one.** `live-rera-catalogue` proves the badge is absent on a slug the catalogue has never heard of, which on its own passes against a page that stopped drawing the badge at all; this is the confirmed MahaRERA row it has to keep drawing it for, paired in one test with a *minted* society — a real server row with a real hub, reachable and followable, that we simply know nothing about. Same page, same components, and the only difference is what we hold. Deliberately the one place a browser-level society test uses a minted slug rather than a catalogue one: the hub resolves `soc` out of the bundle, and "on the server but not in the catalogue" is precisely the state the honest-placeholder branch exists for | societies | consumer/society/live-society-suggest ("the verified badge is a catalogue fact, and a society nobody has confirmed does not wear one") | ✅ |
| `/locality/:slug` | societies | consumer/search/live-locality-intel, consumer/search/live-locality-select | ✅ |
| `/reels` is **the catalogue behind two gates**, not a curated list. Homes only and three photos minimum, checked both ways: every rendered reel is a residential listing the API also calls residential, and `p5032` — an approved Plot carrying six photos, so past the photo bar outright — is proven present in the catalogue and absent from the feed. The retired mock twin asked `/src/data/db.json` from inside the page whether the fixture agreed with itself, and had no non-residential row that cleared the photo bar, so deleting the type gate entirely would have left it green | (property-detail) | consumer/property/live-reels (`--config=playwright.live.config.js`) | ✅ |
| **Saving from a reel reaches the server shortlist.** `toReel` dropped `uuid`, so `saved.toggle` fell back to the slug and `PUT /me/saved/{propId}` — which binds a `UUID` — rejected it: no reel could be saved unless the property was already shortlisted, the one case where the context could recover the key itself. The heart filled and rolled back. Found by asserting `GET /me/saved` from outside the browser rather than reading the page's own copy, which is what the localStorage-era assertion did | (property-detail) | consumer/property/live-reels (`--config=playwright.live.config.js`) | ✅ |
| City switcher: propagation, coming-soon waitlist, cancel = no-op, admin offline revert | — | platform/city-propagation | ✅ |
| The operator's **live geo + city policy against the API** — map bounds / blacklist written through `PUT /admin/settings`, city launch state written through `PATCH /admin/cities/{slug}`, and both read back only through the public browser paths (`GET /geo` and `GET /cities`). Nothing in the test touches what the page reads. That separation is the subject: before these routes existed the admin console wrote Postgres while `lib/geoConfig.js` read browser-local state, so an operator could take Mumbai live or redraw its bounds and no visitor's browser would ever learn it | settings | platform/live-geo-policy (`--config=playwright.live.config.js`) | ✅ |
| `GET /geo` publishes the geo block and **nothing else** from the settings document (not `fees`, `flags`, `movePack` or `site`), and withholds the operator's `note` on each blacklist entry — an internal moderation reason about a real place, written for colleagues, that the client has no use for. Asserted both at the field and across the serialised document, so a `note` reappearing at another depth is caught | settings | platform/live-geo-policy (`--config=playwright.live.config.js`) | ✅ |
| A city taken live on the server is a **destination rather than a waitlist prompt**, both after a reload and on the in-tab `punenest-settings-change` signal without one; and an install with no `geo` row at all (the shipping default — the block is deliberately unseeded, the defaults live in the client's `CITY_GEO`) still renders Pune with its locality rail, so "no geo overrides" cannot degrade into "no cities" while `GET /cities` remains the roster source of truth | settings | platform/live-geo-policy (`--config=playwright.live.config.js`) | ✅ |
| The **shortest blacklist term the server will publish is the shortest the client will match** — one character is dropped on the wire, two survives and is acted on by `isBlacklisted` in the browser. Two constants in two languages (`GeoPolicyController.MIN_BLACKLIST_TERM` and the `term.length >= 2` in `lib/geoConfig.js`) with only a comment holding them together, and either direction of drift is silent: the operator's blacklist simply stops suppressing a place, with nothing logged | settings | platform/live-geo-policy (`--config=playwright.live.config.js`) | ✅ |
| The **city roster is server-owned**: the Maps panel's launch toggle writes `PATCH /admin/cities/{slug}` and is read back over the API from outside the browser that wrote it, then confirmed against the public picker a logged-out shopper uses. The mock-mode twin (`admin/maps-geo`) structurally cannot prove this — it asserts against the same `localStorage` the write went to | settings | admin/live-city-roster (`--config=playwright.live.config.js`) | ✅ |
| A **roster that will not load shows nothing rather than a guess** — `/cities` aborted, the panel renders the "could not be loaded" notice and *no* launch switch, while the city-limit toggle and blacklist (a different read) keep working. The removed fallback invented `slug` by lowercasing display names, so the toggle would have looked functional while writing against a key the server need not have | settings | admin/live-city-roster (`--config=playwright.live.config.js`) | ✅ |
| A **failing roster does not silently un-hide blacklisted places** — with `/cities` aborted, a blacklisted term is still listed in the console *and* still matched by `isBlacklisted` in the page. `loadGeoPolicy` fetches geo and roster together; under `Promise.all` the roster's rejection discarded the healthy `/geo` response, and the blacklist's empty default fails **open**, so an unrelated endpoint going down restored every place the operator had hidden | settings | admin/live-city-roster (`--config=playwright.live.config.js`) | ✅ |
| The **retired `geo.cities.*.live` key is refused with 422**, and the message names `PATCH /admin/cities/{slug}` — so a stale console bundle cannot store a launch flag that nothing reads and be told 200. The refusal is total: the roster is unchanged afterwards | settings | admin/live-city-roster (`--config=playwright.live.config.js`) | ✅ |
| **The console consolidation sweep still holds against the live API** — the eleven claims about what was merged away and what replaced it: `/admin/support` redirecting onto the Service Requests desk with no Support entry left in the sidebar, Analytics no longer carrying a Revenue tab, Staff Activity having dropped Today's Progress and gained a cross-link to the audit log, Content offering neither a Localities nor a City Demand tab, Finance's Deal Pipeline card pointing at Enquiries, the Settings audit log cross-linking back to Staff Activity, and the Enquiries desk owning the four KPI tiles, the funnel that used to be Analytics' Conversion tab, and the visits table. Converted from the mock file because a consolidation claim is precisely the kind that a mock cannot settle: "this tab is gone" is true of a provider that never had it. Three of the mock's tests were dropped rather than carried — two duplicated `live-content-desk` and `live-analytics-page`, and one asserted a reviewer name (`Gauri Rao`) that exists only in `db.json`, which `live-admin-content` already covers properly by naming the author of a row it fetched first. The mock file is now **deleted**: its stated reason for not being live — that five named admin pages read `lib/mockApi.js` — had gone stale without anyone noticing (four of the five had moved to services), which is the failure mode a keeper rationale has | (all back-office) | admin/live-consolidation (`--config=playwright.live.config.js`) | ✅ |
| **The `/admin/localities` console against the live API.** `platform/live-locality-queue` already proves the queue *route* — what it returns, what it refuses, who may read it. Nothing proved the screen an operator actually uses, and the mock twin could not: it reached into `puneNestDB_v5`, deleted `localitySlug` off a seeded listing, and asserted the component renders a shape. Here a real listing is posted with an area the resolver cannot place, and the row is then shown to carry the owner's own words (the operator's only clue about which area was meant) with no way to contact them; filing it under an area is proved by the row leaving the queue **and** by re-reading `GET /admin/locality-queue` from outside the browser that filed it; the assign dropdown's options are compared against the live `GET /localities` rather than a literal, because the directory used to render the bundled `data/localities.js` and an area added through the console did not appear until someone shipped a release; the Directory tab's first page is compared name-and-slug against the server's own answer in the server's own order, so a client that re-sorted or fell back to the bundle disagrees while still looking plausible; and a signed-in buyer is refused. The mock's "Live and unfindable" test is deliberately **not** carried over, and the reason is a finding: that state is unreachable through the API — approving an unfiled listing is refused, and un-filing a filed one answers 422 — so the tile counts a population that can only predate the approval guard, and the only ways to reach it (writing a null into the column, or seeding one) assert that the component renders a shape rather than that the server produces it. Exactly what the mock was doing | localities | admin/live-localities-console (`--config=playwright.live.config.js`) | ✅ |
| **The `/admin/properties` console against the live API.** `admin/live-properties-moderation` already proves the moderation *verbs* at the route — flag, clear, archive, restore, and the two 400s. What nothing proved was the desk itself, and the mock twin could not: its counters agreed with the store the page had just read, which is a tautology no matter what the server thinks. The load-bearing test here is the one that reads a KPI tile and the row counter off the screen and checks them against `GET /admin/properties` fetched independently — a client that filters client-side, drops a page, or counts an optimistic row disagrees with the server while still painting a confident number. Around it: the nine-tab strip and its URL sync, so a deep link an operator pastes into chat opens the queue it names; search proved both ways, since a search that narrows but never empties is indistinguishable from one that quietly ignores the term; the status, deal and date filters each asserted to keep exactly the right row rather than merely to change the count; the verification modal proved to carry what an approval decision needs; the CSV export named for the active tab across four tabs, because an export that always writes the same filename silently overwrites the last one; and both guards, buyer and signed-out. Four tests mint their own `Zztest ` listing through `POST /me/listings` under a fresh owner and reject it in `afterEach`, so nothing depends on the shared seed. The **Pipeline** board, the four card-action modals and the WhatsApp templates are deliberately not carried — the first has no seeded concierge rows so every assertion would land on an empty board, the second duplicates the route-level moderation spec, and the third is gated on contact-mask state this spec does not control. `admin/properties.spec.js` **stays on the mock** and must: it is the only cover for the Duplicates tab, which has no server at all | properties | admin/live-properties-console (`--config=playwright.live.config.js`) | ✅ |
| **The Staff Posted tab finds the desk's work when `postedByStaff` is a uuid the browser never wrote.** `post-on-behalf.spec.js` claimed this tab and could not have failed: the console used to pack `postedByStaff: <display name>` into the create body, the mock provider stored the object it was handed verbatim, and the tab filtered on the field the client had just written — client, store and assertion were one sentence three times. The server ignores that field and derives the staff id from the caller's token, so live it is a uuid, and whether the tab still finds anything is a real question. Proved with an owner-submitted listing created seconds apart under the same shape — same status, same locality, same price band — as the adversarial control: identical on every axis the tab does *not* filter on, so removing the `postedByStaff` gate turns the test red rather than leaving it green, and the owner row is then found under All Listings through the same search box so a zero cannot be a typo or a console that had stopped rendering cards. A second test pins `postedByAdmin`, whose loss has a visible consequence and no error: `AdminPropertyCard` picks between two entirely different progress rows with it, so a concierge listing that lost the flag renders "In Review" to a moderator whose actual blocker is an owner who has not answered the phone — asserted from both sides, since "the desk card said Photos & Docs" is only interesting if the owner card does not. Not carried over: the old spec's "Posted By column with staff name", asserted as `getByText('Administrator').first()`. There is no such column — the staff tab renders the same card list as every other tab — and the match was the signed-in name in the admin topbar, which is on every page of the console | properties | admin/live-properties-console (`--config=playwright.live.config.js`) | ✅ |
| **The `/admin/settings` desk writes to the server and the server is what the site reads.** The mock twin asserted that a save produced a toast, which is a claim about a component's own optimism — it passed against a provider that never persisted anything. Here the general form is proved to be *filled from* `GET /admin/settings` rather than from a default, a site-detail save is re-read out of the stored document from outside the browser that wrote it, and the fee schedule is proved by the number moving on the **public** `GET /pricing` route a shopper hits — the one place a settings bug becomes a wrong price rather than a wrong form. The feature-flag test asserts a **negative**: cancelling the confirmation issues zero `PUT`s, because a confirm dialog that has already written by the time it asks is worse than no dialog. Both guards are proved twice over, at the router and at the API, since a screen that redirects while its endpoint stays open is not actually closed. Teardown snapshots the live `site` and `fees` blocks in `beforeAll` and writes back only the keys that moved, and the fee change is made relatively (`before + 7`) so it cannot drift into `PlatformSettings.MAX_PRICE`. The mock's audit-log test is not carried: `listAudit`/`clearAudit` are browser-local by design (D39), so there is no server claim in it | settings | admin/live-settings-console (`--config=playwright.live.config.js`) | ✅ |
| **Geo policy set in the Maps console is what every visitor is served.** A blacklisted term is proved hidden on the public `GET /geo` a logged-out shopper's search reads, not merely struck through in the operator's own tab — the distinction matters because the blacklist's failure mode is to fail *open*, so a write that never left the browser looks identical to a working one until someone searches for the place. Removing the term is proved the same way, in reverse. The city-limit switch is read back off the server rather than off the control. Snapshots come from `GET /admin/settings` rather than from `/geo`, because the public projection drops each entry's operator note and restoring from it would quietly erase why a term was blocked. The corresponding mock tests were **deleted** rather than kept alongside — `per-city live toggle persists to the shared city roster` because the roster moved to `PATCH /admin/cities/{slug}`, and `city-limit toggle and blacklist persist to settings.geo` because both were read back out of the same `localStorage` the write had gone to, so they passed whether or not anything left the tab. That mattered more here than elsewhere: the blacklist fails *open*, so a write that never reached the server is indistinguishable from a working one until someone searches for the hidden place. `admin/maps-geo` survives with one test, the boundary editor, which stays on the mock deliberately: there is no server claim in it | settings | admin/live-maps-geo-console (`--config=playwright.live.config.js`) | ✅ |
| `/messages` — a thread is `staged` or it is not (D52); a staged one shows the waiting line, not a composer, and an owner-side thread with an unread message opens ready to reply with **no** accept/decline panel (that negotiation belongs to the contact gate, one layer up) | contact-gate-leads | messages-inbox, mobile/live-inbox-saved | ✅ |
| `/flatmates` | flatmates | `consumer/flatmates/**` (30 specs), consumer/home/live-flatmates-rail | ✅ |
| **A host can see and withdraw their own rooms (`GET /me/flatmate-rooms`, `DELETE /flatmates/rooms/{id}`).** The dashboard panel read the browser store, so a host who posted a room from their phone could not see it on their laptop, and "Delete" removed a local row while the room stayed on the public board. The host read is the interesting half because it is deliberately *not* the public one: it includes `pending` and `removed` (a host must be able to see that their post is stuck in review — hiding it is how a moderation queue becomes a black hole), excludes archived, and populates `ownerMobile` with the caller's own number, which the public board masks. It is asserted as caller-scoped rather than filtered — a widening query param on it must narrow to the caller anyway, because an unknown param is silently ignored rather than rejected, so "I passed a filter and got rows back" proves nothing on its own. Withdrawal is pinned on its three refusals as much as its success: 404 on a second withdrawal (so a retry cannot report a phantom success), 403 for a stranger, and 409 for a `split_room` that another party is depending on. Every subject is minted, never seeded, because the seed's flatmate rows are shared. Server-side twin: `FlatmateHostRoomEndpointsTest` | flatmates | consumer/flatmates/live-host-rooms (`--config=playwright.live.config.js`) | ✅ |
| Legal pages (privacy/terms/refund/disclaimer) | — | platform/live-legal-pages (`--config=playwright.live.config.js`), platform/live-verification-disclaimer (`--config=playwright.live.config.js`) | ✅ |
| `/list-property` wizard | list-property-wizard | `consumer/list-property/**` (18 specs), mobile/live-wizard-sticky | ✅ |
| **The wizard writes through the property seam** (D219) — the payload handed to the provider carries a unit-bearing `address` line (the flat number ahead of the society, because a building-level AddressKey flags a whole tower and helps nobody), and the four fields whose names differ between the record and the contract survive the crossing: `electricityConsumerNo`, a numeric `floor` that is neither NaN nor a fabricated 0, `reraId` (the record calls it `rera`), and one `maintenance` folded out of the wizard's deal-split pair. A name mismatch on a write does not fail — it drops the value in silence, and the meter is the strongest duplicate signal the server has | list-property-wizard | consumer/list-property/live-seam-write | ✅ |
| **The wizard reaches `POST /me/listings` against the live API** (D219) — the server's duplicate probe runs inside that request, so before this slice the detector was reachable only from admin post-on-behalf while the abuse it exists to catch arrives through this form. Mock-mode specs cannot see the difference (the mock provider writes to the same localStorage the old code wrote to), so only the wire distinguishes "saved" from "posted": 201 with a server-minted id, and the row coming back from `GET /me/listings`. Runs as a freshly minted account, never the seeded owner whose exact listing count another test asserts | list-property-wizard | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **The owner's ownership document reaches the server, not the lister's own browser** (D228) — the same live wizard test also sets a document and asserts `POST /me/documents/{id}` returns 201 for the `Ownership Proof` category. Until this slice `persistListing` filed the wizard's documents with `addDocument(...)` into `localStorage`, and only `if (!isRent)`. That file is the one that earns the **Verified Owner** badge, so the evidence landed in the one place the moderator who has to check it can never look — every owner-posted listing was unverifiable by construction, and nothing said so: the picker showed the filename and the progress meter counted it. The listing under test is a **rent** listing, which makes it the exact case the `!isRent` guard discarded. No mock spec can catch either half, because the mock document provider calls the very `addDocument` the old code called against the same store and key — the full mock wizard suite stayed green (90/90) across the fix. Asserted against the **slug**, since `propertyMapper` maps `id: p.slug ?? p.id` and the server resolves either. Watched failing with the fix reverted before being trusted | list-property-wizard, documents | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| Editing a live listing tells the owner **which** of the two re-review outcomes they are buying: a BHK edit says "comes off search", a price edit says "stays live and searchable" and must not show the off-search copy. The pair is the assertion — the banner previously said "off search" for both, which is a broken promise when the listing quietly went dark and a deterrent against honest price cuts when it did not (Q14, D76). The classification itself is pinned separately, without a browser, by `frontend/scripts/check-listing-foundation.mjs` | list-property-wizard | consumer/list-property/live-edit-policy (`P1 — a Tier-A edit surfaces the re-check summary + status timeline`, `P1 — a price edit is re-checked but the banner promises the listing stays live`) | ✅ |
| **"Have I already listed this?" is the server's answer, not the browser's** (D226) — the block used to come from `evaluateListingDedup` scanning the mock store, which against a live API is the seeded demo catalogue: it could stop a real owner over a fixture and then offer a link to an id the server had never issued. Now `propertyService.checkOwnDuplicate` → `POST /me/listings/duplicate-check`, scoped to the caller's own listings. The live spec asserts both halves — the meter and the normalised address each find the owner's real listing, and the id it returns opens through `GET /me/listings/{id}` — plus the boundary that keeps it from becoming a lookup: **a different owner's identical listing is invisible**, even when both signals match. That is the line between this and the staff duplicate probe, which reads cross-owner collisions and is staff-only for exactly that reason | list-property-wizard | platform/live-own-duplicate (`--config=playwright.live.config.js`), consumer/property/live-dedup, consumer/property/live-dup-modal | ✅ |
| The listing editor reads the owner's listing from the server, **and in the shape the wizard uses** (D237) — `?edit=<id>` used to prefill from `getListing(id)` in localStorage, so an owner opening it on a second device, after clearing site data, or from the enquiry panel's "Add photos" link (built from a server id this browser never held) got the blank default form — and submitting it would have PATCHed those defaults over a listing that was not blank. It reads `propertyService.myListing` → `GET /me/listings/{id}` now, owner-scoped because a listing under moderation is not on the public endpoint at all. Repointing the read was only half of it: a mock record carries its own `form` snapshot while a server row carries the contract's names (`type` for `propertyType`, `desc` for `description`, `3` for `"3 BHK"`), so the first version filled almost nothing and every mock spec stayed green — hence `toEditForm`, whose field set is pinned to `ListingUpdate` rather than to the visible form, because `toListingUpdate` drops only `undefined` and a field the prefill cannot restore is a field the next save erases. The live test asserts values, not a heading: carpet area (free text, so no default can fake it) and BHK (which has a default, and was seeded away from it). Three consequences are asserted alongside: the duplicate guard's CTA opens `?edit=` on the id it holds instead of detouring to `/dashboard`; an edit whose listing failed to load refuses to save rather than saving the defaults; and an address the owner never retyped is not recomposed from empty parts | list-property-wizard | live-property-integration (`the edit form prefills from the server, not from this browser (D237)`, `--config=playwright.live.config.js`), consumer/property/live-dup-modal (`Re-listing the same unit shows the duplicate guard modal and blocks the post`), consumer/list-property/live-edit-policy | ✅ |
| **An owner can take their own listing down, and the slot comes back** (D238) — the dashboard's Delete ran `setListingStatus(id, 'deleted')`: the moderator's route, which an owner is not authorised for, carrying a status that route rejects anyway — and it was not awaited, so both failures were swallowed, the owner was toasted "deleted", and `refreshListings()` put the listing straight back. With the quota enforced server-side (D235) that was not untidiness, it was a ceiling with no exit: the free tier is one listing at a time and this was the only way to let go of one. Now `propertyService.takeListingDown` → `DELETE /me/listings/{id}`, which existed and had never been called, kept separate from `archiveListing` because that is the staff path and takes a reason an owner does not owe anyone. Relabelled "Take down" since the server soft-archives — "Delete" promised an erasure the product does not perform. The live test asserts the whole loop: a second post is refused 422 while the first stands, the DELETE answers 200, the row comes back from `/me/listings` marked `archived` (the API keeps the owner's whole file; the dashboard is what filters it), and the retried post is accepted 201 | list-property-wizard | live-property-integration (`an owner can take their own listing down, and it frees a quota slot (D238)`, `--config=playwright.live.config.js`) | ✅ |
| `/dashboard` hub | consumer/account/owner-hub | dashboard, consumer/account/action-center, consumer/account/doc-info, consumer/account/owner-finances, mobile/live-dashboard-hub | ✅ |
| `/dashboard#enquiries` document-request inbox — the **owner's screen**, not the endpoint. The sibling `live-buyer-document-access` grants by calling `PATCH /me/documents/requests/{reqId}` directly and so cannot fail for the bug this row exists for: the dashboard once read its inbox from `localStorage` while the Documents tab read it through `documentService`, so a grant issued here decided the request in the browser's own copy, rendered every screen below correctly, and told the server nothing (D125 item 2). The PATCH is asserted on the wire and the row is re-read through `GET /me/documents/requests` **outside the browser**, where `sharedDocumentCount` counts files rather than categories. The caption is built from the categories the API returned, never from the constant the spec uploaded, since `EnquiriesPanel.itemDoc` joins `docTypes` in an order no endpoint promises | consumer/account/owner-hub | consumer/account/live-doc-requests-grant | ✅ |
| `/messages` through a real approved contact gate: a unique buyer sends a quick reply and typed message, both return 201 and survive a reload; buyer and owner contact controls reflect the server decision; the dashboard delegates to the canonical inbox. The synthetic thread is scoped to a new buyer against Meera's seeded Baner listing, never a shared seed conversation | consumer/account | consumer/account/live-messages-inbox (`--config=playwright.live.config.js`) | ✅ |
| `/tenant-profile` reads and replaces the server-owned profile: save returns the persisted occupation/income, reload hydrates them, and empty-name validation issues no destructive PUT. The published score checklist remains interactive while the score itself stays server-owned | consumer/account | consumer/account/live-tenant-profile (`--config=playwright.live.config.js`) | ✅ |
| A pending owner contact request renders **Serious Buyer** only when the server's nested `requester.verified` bit says so, before approval and without trying to infer identity from the masked mobile | consumer/account/owner-hub | consumer/account/live-contact-request-verified-badge (`--config=playwright.live.config.js`) | ✅ |
| **Owner-private lead notes live on the server, not in the browser.** A note and a follow-up date are typed into the lead sheet, and the load-bearing assertions read them back from somewhere the writing browser does not own — the API directly, then a **second browser context** signed in as the same owner. Both are necessary: a reload-and-still-there check would have passed against the old `localStorage` store too, which is exactly the defect (annotations under `puneNestLeadNotes:<ownerDigits>` were per-browser, so an owner's phone and laptop kept disjoint CRMs). The follow-up assertion pins the calendar **year**, because the sheet emits epoch millis into an `Instant` field that reads bare numbers as epoch *seconds*; without the client-side conversion the PUT never reaches 200. A second test proves emptying the last field **deletes** the row (204) rather than storing a blank one | consumer/account/owner-hub | consumer/account/live-lead-notes (`--config=playwright.live.config.js`) | ✅ |
| A granted document request opens via the server notification deep link, contains the API-matched uploaded categories with no download control, and honestly shows the no-file state when approval precedes upload | documents | consumer/account/live-view-documents-flow (`--config=playwright.live.config.js`) | ✅ |
| Server-backed Action Center, dashboard, and deal/offer controls are exercised with isolated account fixtures; local photo requests, recent searches, controlled stale timestamps, and the masked-owner finalization retry remain deliberate mock-only boundaries | consumer/account | consumer/account/live-action-center, consumer/account/live-dashboard, consumer/account/live-deals-offers (`--config=playwright.live.config.js`) | ✅ |
| `/dashboard#enquiries` flatmate host inbox reads the `ownerId` bucket, not a mobile-derived one, so a request filed for the signed-in host is visible even when the old mobile-keyed store would have split it (D186) | consumer/account/owner-hub | consumer/flatmates/owner-id-inbox | ✅ |
| **Starting DigiLocker verification grants no badge** — the modal's two entry points open, `POST /me/verification/aadhaar` answers **202 with a hosted consent URL**, the browser is handed to it, and a fresh read of the profile is still unverified. A client that could talk itself into a trust badge is a security defect, and this is what would notice. Moving this spec live **changed its subject**: on mocks it asserted only the happy path (D21 — modal → DigiLocker mock → the green pill renders), because the mock grants the badge inline | trust-and-verification | platform/auth/live-verify-funnel (`--config=playwright.live.config.js`) | ✅ |
| The **rendered** "ID verified" pill on `/dashboard?tab=profile` once the badge is granted, and both funnel CTAs retiring together (D21). The grant is driven through `POST /me/verification/aadhaar/simulate` — the `@DevOnly` endpoint built for this in D122 — which runs the production `handleWebhook` path, so this stands on a real grant rather than a faked one. Paired with the row above on purpose: "starting grants nothing" would pass just as happily against a badge feature that never worked at all | trust-and-verification | platform/auth/live-verify-funnel (`--config=playwright.live.config.js`) | ✅ |
| `/owner-hub` + `/owner-hub/property/:id` passport | consumer/account/owner-hub | owner-hub, consumer/property/live-property-passport | ✅ |
| **The owner hub through the `managed` seam** — Rent-o-meter registration, the passport read, the document vault, rent tracking and publish all route through `managedService` / `documentService` rather than `lib/data/managedProperty.js` and `puneNestDocs:`, so the same specs stand against both providers. Every one of these paths is now asynchronous and every write can be refused, which is the point: publish re-runs listing validation server-side and answers 422 on a record captured loosely enough to be legal as a private draft and illegal as an advertisement (D32) | consumer/account/owner-hub | owner-hub, consumer/property/live-property-passport, consumer/account/dashboard | ✅ |
| `/view-documents/:requestId` secure viewer | consumer/account/owner-hub | view-documents-flow, doc-viewer-scheme | ✅ |
| **A granted buyer opens their own documents without the owner forwarding anything** — the buyer's half of the document gate against the live API. The submit was already covered (`platform/live-verification-disclaimer`); the **read** was not, and that is where the promise lived. `shareToken` is owner-facing by contract — it is minted so the owner can forward the link to a lawyer deliberately — so before `GET /me/document-requests/{reqId}/documents` a buyer whose owner never forwarded it saw the status "Granted" and a dead end. The load-bearing assertions are the negative ones: the requester projection carries `shareToken: null` **while the owner's inbox row carries a real token**, so the redaction is evidenced rather than assumed; a *pending* request reports `sharedDocumentCount: 0` and its document read is a 404, because an ask is not permission to inventory a stranger's vault; and a stranger's 404 is compared **body-for-body** (minus `traceId`) with the 404 for an id that never existed, since two distinguishable refusals restore the existence oracle the shared status code was chosen to remove. No mock can hold an opinion about any of this — there is no token in it to redact and no reason to hide anything from itself. Also pins **register 37**: the `document.granted` notification's `link` is asserted to equal `/view-documents/{reqId}` and the viewer is then opened **by that link** rather than by a URL the spec assembles, so a server that stopped emitting the deep link cannot pass; and the stranger's browser sees the neutral "Access not available" — the same words for pending, lapsed, unknown and foreign, because a screen that distinguishes them undoes the shared 404. Builds and removes its own `Society NOC` file (deliberately not the `Sale Deed` slot `live-property-integration` occupies) and uses throwaway buyers, never `ACTORS.buyer`, whose `p5021` request the disclaimer spec expects to find pending | consumer/account/owner-hub, documents | consumer/account/live-buyer-document-access (`--config=playwright.live.config.js`) | ✅ |
| **Deals / Offers / Negotiation / Finalization** — incl. a declined finalize request surfacing "the owner hasn't confirmed — ask again" now the status read returns terminal rows, not pending only (D111) | deals-offers-finalization | deals-offers | ✅ |
| **Buyer deal visibility on the property page** — a buyer on a *closed* listing sees the terminal "no longer available / rented out" banner and the offer + finalize cards are hidden; a *reserved* listing shows the "Under Offer" banner but keeps the offer UI (still queueable); an untouched listing shows no banner and the normal negotiate card. Reads the property's public `dealStatus` mirror, not the owner-scoped deal (D110) | deals-offers-finalization | consumer/property/live-deal-visibility | ✅ |
| Language switching + hi/mr locale integrity | — | platform/live-i18n (`--config=playwright.live.config.js`, chromium + mobile), platform/live-settings-preferences (`--config=playwright.live.config.js`) | ✅ |
| Dashboard ▸ Profile & Settings **behind a real session** — reduce motion, the delete-account confirmation gate, and the app language. Those three genuinely are device settings with no provider in front of them (`pnLang`, a root class), so what the conversion changed for them is that the *person* is real: a preference keyed by mobile only means anything if the mobile belongs to an account the server agrees exists. The owner card is the sharper case — `isOwner` is derived from real inventory, so the seeded version had to hand itself a fabricated listing to make the card appear at all, where Meera's four make it render for the same reason it renders in production | — | platform/live-settings-preferences (`--config=playwright.live.config.js`) | ✅ |
| **The owner privacy switches are account state, not device state (DM).** They sat in `pnOwnerPrefs:<mobile>` — a key that survived nothing and travelled nowhere, so a preference about who may reach you was answered by whichever browser happened to ask. Both now ride `PATCH /auth/me` (`hideNumber`, `verifiedContactOnly`) and come back on every `/auth/me`, `/auth/login` and `/auth/staff-login`. The two are asserted differently on purpose: `hideNumber` is checked at the UI as a *request* (`postDataJSON().hideNumber === true`) because the old spec asserted a localStorage key that live mode no longer writes — an assertion that would have gone on passing while reading a value nothing produced; `verifiedContactOnly` is checked at the API on a throwaway account, because it is the one with teeth (`ContactService.request` throws `VerificationRequiredException`) and flipping it on a fixture actor would change the answer every other spec gets when asking to reach her. The `hideNumber` press is undone in the same test for the same reason. Copy is load-bearing here too: `hideNumber` is inert today (zero `isHideNumber` reads in `src/main`), so the card describes it as a recorded preference rather than a guarantee | — | platform/live-settings-preferences (`--config=playwright.live.config.js`) | ✅ |
| **Download my data is answered by the server (DN/DL).** It used to be `exportUserData()` reading the browser's own localStorage — a subject-access export that could only ever return what this device happened to hold. `GET /me/data-export` returns 73 datasets over 9 domains with the redaction rule stated in the document itself, and is rate-limited through `WriteRateLimitFilter.LIMITED_READS`. The load-bearing assertion is the negative one: a counterparty's display name (`Meera Deshpande`) must not appear anywhere in `datasets`, because the redaction is stricter than briefed — counterparties surface only as an opaque salted `partyRef`, and an export that leaks the other side of a deal is a privacy incident dressed as a feature. Server-side twin: `DataExportCoverageTest` | — | platform/live-settings-preferences (`--config=playwright.live.config.js`) | ✅ |
| **Notification preferences are a server document, not a browser one** — the row above used to include them and said they "stay in localStorage after the mock retires". That was wrong about the platform even when written: `GET`/`PUT /me/notification-preferences` had existed since D94/D15 with tests and an OpenAPI entry, and nothing in `frontend/src` had ever called it, so the server enforced none of these settings — a quiet-hours window suppressed the alerts the *client* derived and nothing else, and a notification the server wrote at 03:00 arrived at 03:00. Two tests: a channel toggle is asserted as a `PUT` carrying **all six** required fields (a partial body 422s, and a swallowed 422 looks exactly like a success); and quiet hours + match alerts set in one browser are read back in a **second browser context** with its own storage and session, both flipped away from their defaults so a fallback-to-defaults regression cannot pass | — | platform/live-settings-preferences (`--config=playwright.live.config.js`) | ✅ |
| **Redirects** (`/map`, `/docs`, `/help-center`, `/share-flat`, `/owner-hub`, `/admin/support`) + the 404 catch-all | — | platform/live-route-redirects-404 (`--config=playwright.live.config.js`) | ✅ |

## Mobile chrome & ergonomics

Mobile-only behaviour has no desktop equivalent to piggy-back on, so it gets its own
table rather than a column in the route matrix above.

| Behaviour | Spec(s) | Status |
|---|---|---|
| Bottom tab bar: mount, destinations, raised Post slot | mobile/live-bottom-nav | ✅ |
| Bottom inset reservation (`.has-bottom-nav` / `--pn-bottom-inset`) | mobile/live-bottom-inset | ✅ |
| Safe-area insets, `theme-color`, manifest link | mobile/live-safe-area | ✅ |
| PWA manifest + icon set | mobile/live-pwa | ✅ |
| Home-screen install nudge: engagement gate, escalating silence, platform split | mobile/live-install-prompt | ✅ |
| Chrome policy per route (`lib/chrome.js`) — footer/bottom-nav/assistant | mobile/live-bottom-nav, mobile/live-inbox-saved, platform/live-feature-flags (`--config=playwright.live.config.js`) | ✅ |
| **Consumer feature flags are honoured end to end against the server** — 10 switches (`mapSearch`, `compareProperties`, `scheduleVisit`, `emiCalculator`, `reviewsEnabled`, `videoListings`, `inAppMessaging`, `savedListings`, `onlineRentPayment`) each asserted both ways, plus the four route guards that must turn a disabled feature's URL away rather than merely hiding its link. The flag is written through `PUT /admin/settings` — the only writer — and read through whatever the browser makes of the public `GET /flags`, so nothing in the test touches what the page reads. That separation is the subject: before `GET /flags` existed the admin console wrote to the API while the browser read an unrelated copy from localStorage, maintenance mode reported success and served the site, and the seeded version of this spec passed throughout because it was writing to the same place the client read from | platform/live-feature-flags (`--config=playwright.live.config.js`) | ✅ |
| **The product survives losing every feature** — the listings and property pages still render with all 26 consumer flags off and zero `pageerror`s. The seeded ancestors of these two tests read `puneNestDB_v1`, a store key three versions stale; `JSON.parse(null)` returned null, the guard returned silently, and both spent their lives asserting that a page with **all flags enabled** renders cleanly, under a name claiming the opposite (`maintenanceMode` is excluded on purpose — switching it on blanks the app, so an all-off smoke test would assert the maintenance screen rather than the product) | platform/live-feature-flags (`--config=playwright.live.config.js`) | ✅ |
| **No mobile chrome leaks onto desktop** | platform/live-desktop-noleak-guardrails (`--config=playwright.live.config.js`) | ✅ |
| **The application actually boots** — four routes (`/`, `/listings`, `/dashboard` as owner, `/admin` as admin) render a non-empty body with zero `pageerror`s. This exists because `npm run check`, `npm run lint` and `npm run check:size` were all green on 2026-08-11 while the app served an empty `<body>` on every route: a Vite build resolves an import cycle happily and only a browser executes it, so no static gate can see the failure (D208) | platform/live-boot-canary (`--config=playwright.live.config.js`) | ✅ |
| 44px tap-target sweep across consumer routes | mobile/live-tap-targets | ✅ |
| 44px sweep behind a session (`/dashboard`, `/messages`) and on staff surfaces (`/admin`, `/ops`) | mobile/live-tap-targets | ✅ |
| The sweep cannot pass vacuously: it waits on `appReady()` rather than `networkidle`, refuses to report a pass on fewer than `MIN_CANDIDATES` interactive elements, follows the client-side navigation with `waitForURL` before measuring the property page, skips `pointer-events: none` subtrees (closed off-canvas drawers) and measures `::before`/`::after` hit areas rather than trusting the `.tap-extend` class name | mobile/live-tap-targets | ✅ |
| 12px computed-font-size floor across 14 routes, signed-out, signed-in and admin | mobile/live-text-legibility | ✅ |
| Pull-to-refresh: coarse-pointer gate, past/short-of threshold, sideways-drag carve-out, `data-no-ptr` opt-out, scrolled-away-from-top | mobile/live-pull-to-refresh | ✅ |
| Service-landing hero is a responsive `<img>` with `srcset`/`sizes`/`fetchpriority` (3 landings) | consumer/services/live-hero-image | ✅ |
| Sliders, carousel dots, admin header icons (the controls the sweep missed) | mobile/live-touch-targets-p3 | ✅ |
| Property gallery dot rail ≥ 24px (shipped at 22 despite its own comment) | mobile/live-touch-targets-p3 | ✅ |
| Native share: cancelling the OS sheet is not an error | mobile/live-native-affordances | ✅ |
| Haptics: fires on save, suppressed by app **and** OS reduce-motion | mobile/live-native-affordances | ✅ |
| Toolbar controls stay on screen (`/societies`) | mobile/live-content-budget | ✅ |
| Trust-badge legibility floor (`/flatmates` VERIFIED) | mobile/live-content-budget | ✅ |
| Home tap targets + touch affordances | mobile/live-home-taps | ✅ |
| Home: featured before the fold, proof chips | mobile/live-home-featured-first | ✅ |
| Home Flatmates tile: trust features on one line, CTAs full-width, no h-overflow | mobile/live-home-flatmates-tile | ✅ |
| Sheets, swipe-to-dismiss, sticky actions | mobile/live-sheets-and-actions | ✅ |
| Sticky wizard footer on `/list-property` | mobile/live-wizard-sticky | ✅ |
| Sticky contact bar on `/property/:id` | mobile/live-property-contact | ✅ |
| **Content budget: primary content is reachable, not buried under chrome** | mobile/live-content-budget | ✅ |
| Assistant coach-mark: never covers the price, budgeted to 2 sightings | mobile/live-content-budget | ✅ |
| Top-bar scroll behaviour | mobile/live-topbar-scroll | ✅ |
| Context-aware navbar left slot / account pill | mobile/live-navbar-context | ✅ |
| Signed-in phone top bar holds 6 visible targets at 360px, not 7 — Compare moves to the account drawer below `lg` and `/compare` stays reachable there (D98b) | mobile/live-navbar-context ("sheds Compare to the account drawer") | ✅ |
| On-screen keyboard handling in auth | mobile/live-auth-keyboard | ✅ |
| Landscape orientation | mobile/live-landscape | ✅ |
| Space/density optimisation | mobile/live-space-optimization | ✅ |
| Ops console on a field phone | mobile/live-ops-field | ✅ |
| Dashboard hub navigation on a phone | mobile/live-dashboard-hub | ✅ |
| Phase-3 sweep | mobile/live-phase3 | ✅ |
| **Date/time pickers dock as bottom sheets below 640px** | mobile/live-date-time-fields | ✅ |

## Shared UI components

Components used across many routes get their own row rather than being implied by
whichever route happens to render them — that is exactly how the picker
stylesheet bug below survived a 178-file suite.

| Component | Spec(s) | Status |
|---|---|---|
| `DateField` / `TimeField` + their dialogs — styled, layered overlay on **every** route | platform/live-date-time-fields (desktop, `--config=playwright.live.config.js`), mobile/live-date-time-fields (sheet) | ✅ |
| `DualRange` manual entry — Cr/L/K parsing, junk input, Escape, bound ordering | consumer/search/live-dual-range-parsing, consumer/search/live-filter-slider-manual-entry | ✅ |
| `ConnectivityBanner` + `useConnectivity` + `LoadError` — **offline vs unreachable**: `setOffline(true)` raises the confident "You're offline" and coming back announces recovery then clears; a `page.route` abort while `navigator.onLine` stays **true** raises only the hedged "Can't reach the server"; a 500 answers so **no** banner is painted; a failed load offers a Retry instead of an empty state and the retry succeeds once the route is un-aborted; the live region is mounted while empty and announcing does not move focus (D165) | consumer/connectivity | ✅ |
| Paid placement (D59) — a boost ranks first in the default order, is labelled `Promoted`, and is **not** pinned over an explicit price sort | consumer/search/live-boost-ranking (`--config=playwright.live.config.js`) | ✅ |
| `ArticleFeedback` — positive/negative branches, persistence, per-article reset | platform/help/live-article-feedback (`--config=playwright.live.config.js`) | ✅ |


## Admin

| Route / Feature | Flow doc | Spec(s) | Status |
|---|---|---|---|
| `/admin` dashboard + RBAC nav | (all) | admin/live-consolidation (`--config=playwright.live.config.js`), admin/live-rbac (`--config=playwright.live.config.js`) | ✅ |
| **Admin topbar palette + notifications** — Ctrl+K still navigates real admin pages everywhere, but the five fixture-backed data categories are only offered while their domains still live on mock providers; on live builds the palette says what it is withholding and the bell says it is not counting rather than printing a demo-derived red dot or "All caught up." | (all) | admin/command-palette, admin/live-command-palette (`--config=playwright.live.config.js`) | ✅ |
| **The live suite runs against its own persistent database, `punenest_e2e`** — not `punenest` (a run would wipe dev work) and not `punenest_test` (the Java suite asserts that one is empty). Rows **commit and survive a backend restart**, which is the point: a user registered by a spec is still there after the JVM is killed, proven directly (`9700009911` → `a768c4d0-…`, identical id across a restart). Drift is controlled by resetting to baseline at **run start** rather than teardown — a teardown only runs if the run reaches it, and it destroys the evidence in exactly the crash case you wanted it. `global-setup.live.js` truncates every table it discovers from `pg_tables` except `flyway_schema_history` (discovered, not hand-listed: a stale list fails silently) and re-applies the three seeds in Flyway order. Idempotent — consecutive runs both settle at 88 users / 39 properties | — | e2e/global-setup.live.js, e2e/scripts/reset-e2e-db.sql (`--config=playwright.live.config.js`) | ✅ |
| **Live sign-in is a real OTP round trip, not a log scrape** — the `e2e` profile pins the code so specs stop reading it out of the backend log, but issuance, hashing and `otp_codes` are untouched, so the flow is genuine and still fails the way it should. Proven in four steps: replaying a used code → 401 *"No active OTP"*, a fresh request → 200 `otpSent`, a wrong code → 401 *"Incorrect OTP"*, the fixed code → 200 authenticated. Three independent guards keep it out of production — the property defaults empty, `application-prod.properties` pins it empty, and `@PostConstruct` refuses to start the context if a non-empty code survives into a production profile | — | e2e/helpers/liveAuth.js, backend `OtpService` (`--config=playwright.live.config.js`) | ✅ |
| **Restoring an archived staff account whose email address has since been re-used is refused, and the refusal is not cosmetic** — a guard that answers 409 and restores anyway is the original defect wearing an error message, and it would pass every other assertion. Asserted through `GET /users?archived=true`, deliberately: `UserResponse` carries **no `archived` field**, and `archive()` never touches the separate `status` column, so an archived user reports `status: "active"` over the wire (**D216**) — the earlier version of this spec asserted on `status` and was therefore asserting on that bug. Also covers: an address with no live claimant still restores, and staff login for the contested address answers **401, not 500** — D206 removed the password parameter so no credential can succeed, but 401-vs-500 still separates "resolved to one row and rejected" from "matched two rows again" | — | admin/live-user-restore-email-collision (`--config=playwright.live.config.js`) | ✅ |
| `/admin/properties` verification queue | property-verification | admin/properties, admin/duplicates | ✅ |
| `/admin/properties` **Pipeline board, on the two-axis funnel (D27)** — the board's six columns are not six stored values. `pipeline_stage` holds the four the desk works (contacted → info collected → listed → docs submitted) and `handback_milestone` holds the four the owner works (photos uploaded → Aadhaar verified → claim sent → claimed); V92 split them because one column meant the later write erased the earlier, so a listing that reached `claim_sent` and was moved back to `listed` lost the fact a claim link had ever been sent. The other two columns, **Under Review and Live, are `status` read sideways** — `pending` and `approved` — which is why the stage dropdown does not offer them and the server answers 400 for either: storing them as well made approving a listing and "moving it to Live" two ways to say one thing that could disagree, and the console's write went to `localStorage` where the disagreement was silent. Covers: the dropdown offering exactly the four settable stages; **no listing falling off the board**, which the previous version could do without saying so — it invented a stage for null and then dropped the row if the invented value was not a known key | property-verification | admin/properties | ✅ |
| `/admin/properties` **re-check queue, seeded through the product** (Q14) — the stays-live half: a real owner PATCH to the price of an approved listing is what queues the row, so the spec also pins `ListingEditRules` deciding the edit is `recheckOnly`; a spec that wrote the flag by hand passed unchanged after that rule stopped firing. Covers: the row naming the changed field and its waiting age (`recheckRequestedAt` is `NON_NULL` on the wire, so a dropped field renders *"waiting — no timestamp"*), the tab's count equalling the server's own `?recheck=true` total, and the listing genuinely still serving to an anonymous `GET /properties/{id}` at the new price while it waits. Both drains are read back over the API, not the screen: **Looks fine** leaves it `approved` and publicly fetchable, **Re-check failed** leaves it `rejected`, unfetchable, with the reason in the owner's own verification thread, and an empty reason is refused with the listing surviving. Found a real defect the mock twin could not: `PropertyVerificationService.decide` did not clear the re-check, so a rejected listing sat in the queue forever, over-reporting the backlog and offering a **Looks fine** button that would have put it back on the public site. The 76h overdue styling the mock spec asserted is deliberately gone — `requestRecheck` stamps `Instant.now()`, so that state is unreachable through the product's own API | property-verification | admin/live-property-recheck-queue (`--config=playwright.live.config.js`) | ✅ |
| `/admin/properties` **against the live API** — the moderation queue and its decisions | property-verification | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| `/notifications` **against the live API** — inbox read, seed suppression, mark-all-read, dismiss (`DELETE /notifications/{id}` round-trip, D93) | saved-alerts | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| `/messages` **against the live API** — inbox read, seed suppression, thread hydration on open, author attribution, reply round trip | contact-gate-leads | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| `/locality/:slug` reviews **against the live API** — slug-keyed read, no fabricated standing badge | trust-and-verification | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| `/property/:id` reviews **against the live API** — a different controller from the locality one, and the reason `listPropertyReviews` shipped pointing at `/reviews/property/{id}` (not a route; `entityType` is `society\|locality\|owner`) and 404'd on every live read while the page's catch rendered "no reviews yet". Both reads are asserted to arrive at `GET /properties/{uuid}/reviews` and `.../reviews/summary` with a 200, navigating by the **slug** so a page reusing its URL token would 404; the rendered average, review count, five star buckets and sparse per-aspect rows are compared against the *summary* payload rather than a tally of the cards, and the card text against the *list* payload. The fixture — a completed visit, so the server grants `context: 'visit'` — is minted by the spec, since the seeded database has no property reviews at all | trust-and-verification | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| `/support` **against the live API** — list, raise, reply, and the absence of the priority/attachment controls | cross-cutting | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Abuse reports against the live API** — file from a property page, duplicate refused, report reaches the staff-only ops queue, no Reopen on a decided report | trust-and-verification | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **The contact gate against the live API** — a signed-out visitor on a public listing queries it not at all | contact-gate-leads | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Saved shortlist against the live API** — `/me/saved` provenance, `PageEnvelope.page` on the wire, heart round trip | saved-alerts | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Saved searches against the live API** — `/me/saved-searches` is the source, and answers with a bare array not an envelope | saved-alerts | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **The saved-search match count is the database's answer** (D227) — `matchCount` on `/me/saved-searches` for a `deal`-only alert is asserted equal to `totalElements` from `GET /properties?deal=…&size=1`. Two independent code paths (a JPQL `count(*)` vs. a Criteria specification) reaching the same rows, so agreement is meaningful and drift in either breaks it; `size=1` proves the number is not a tally of what came back. Also asserted: the count is on the list read and the create response alike, survives a cadence `PATCH`, narrows with `bhk`, is zero for an unlisted locality, zero for a filter blob with no `deal`, zero for `flatmates`, and unreachable for another caller or an anonymous one. Locality parity is deliberately *not* asserted — the public search matches `localitySlug` exactly while the alert count matches `coalesce(localitySlug, locality)` so an alert saved before curation still counts, an intended difference | saved-alerts | platform/live-saved-search-match-count (`--config=playwright.live.config.js`) | ✅ |
| **Visits against the live API** — both sides of the relationship read separately, and reschedule moves the slot in place via `PATCH /visits/{id}/slot` (D87) | contact-gate-leads | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Subscription plans against the live API** — `/plans` serves the pricing page for a signed-out visitor, and buying a priced plan leaves it `pending` with the entitlement it gates still shut | plans-billing-refer | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Deals, offers and finalization against the live API** — the owner's deal book is one `/me/deals` read not one per card, a signed-out visitor asks the deal API nothing, and a buyer reads `/offers/mine` but is never offered the owner-only Accept | deals-offers-finalization | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Rent, tenancies and finances against the live API** — Pay Rent asks the rent API nothing when signed out and is served by `/me/tenancies` + `/me/payout-account` when signed in; the owner Finances tab reads summary, cashflow and dues from the server rather than reducing the page it holds; and the owner's rent-receipts panel is served by `GET /me/rent-ledger` and shows only money that actually landed (D236). The receipts half pins two defects that hid each other: the panel read a localStorage key nothing writes on a live build, so every owner who *had* been paid online saw "no online rent" — and because it rendered nothing, nobody saw that its badge was `entry.settlement \|\| 'Settled'`, which labelled a failed or still-pending charge as money received (the owner's side of the Rent Wallet defect, D231). The seeded tenancy's failed row is load-bearing: with three rows, two paid and one failed, a panel showing three has the old fallback and a panel showing zero has the old read | rent-tenancy | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **The flatmates board against the live API** — all three feeds are served publicly to a signed-out visitor and the board is not empty; a posted room is **absent** from the public feed until a moderator approves it and readable back off that feed with its price intact once they have (V41/D72); the filter bar narrows the board server-side across every facet (gender/food/roomType/furnishing/bhk/budget, policy, flatPref/roomPref), with `any`-valued preference facets falling back to a wildcard so a flexible post still surfaces | flatmates | live-property-integration (`--config=playwright.live.config.js`), consumer/flatmates/live-groups, consumer/flatmates/live-interactions, consumer/flatmates/live-alerts, consumer/flatmates/live-backfill | ✅ |
| **Flatmate groups creation and discovery** — `POST /flatmates/groups` creates a group with title, locality, rent, seats, and policy; `GET /flatmates/groups` paginates and filters (locality, budget, policy); `GET /me/flatmate-groups` is caller-scoped; ownership access control (403 for stranger, 409 for seat-count violations); a group with no open seats rejects new interest as **409** | flatmates | consumer/flatmates/live-groups (`--config=playwright.live.config.js`) | ✅ |
| **Expressing flatmate interest** (interest-api v2) — `POST /me/flatmate-interests` for type `room` or `group`; 201 on first ask, 409 on repeat with appropriate error message; 409 `group_full` for groups with `seatsOpen: 0`; interest persists at `GET /me/flatmate-interests?targetType=`; deletion via `DELETE /me/flatmate-interests/{id}` removes the record | flatmates | consumer/flatmates/live-interactions (`--config=playwright.live.config.js`) | ✅ |
| **Flatmates saved search alerts** — `POST /me/saved-searches` with `kind='flatmates'` creates alert scoped to caller; reads back at `GET /me/saved-searches?kind=flatmates`; toggle alerts on/off via `PATCH`; delete via `DELETE`; filter by locale, budget, gender, verification tier | flatmates | consumer/flatmates/live-alerts (`--config=playwright.live.config.js`) | ✅ |
| **Flatmate group seat backfill** — `PATCH /flatmates/groups/{id}` with `seatsOpen` updates available seats; prevents overflow (>total) and underflow (<0); verification tier persists across reopen/close; only host can modify; 403 for strangers | flatmates | consumer/flatmates/live-backfill (`--config=playwright.live.config.js`) | ✅ |
| **Service requests against the live API** — the tracker reads `/service-requests` (not the mock store), a request created through the service round-trips with a `submitted` status, structured `details` read back off the DTO (D119), an empty `docs`/`draft`, a `user`-authored reply read back off the thread, and no mock-only fields; `listPartyServiceRequests` stays `[]` live because it is the *mock's* second bucket and an accepted party is already in the main list, so returning anything would double every row (D119–D121) | cross-cutting | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **Rent-agreement co-fill end to end, against the live API** (V107) — an invitation is addressed to a **mobile number that has never registered**, which is the case the first design refused and the migration exists to allow: the party row comes back `pending` with the number **masked** (`98XXXXX210`), the invitee then registers, and their first authenticated read **claims** the row onto their user id. Claiming is deliberately not accepting — the request 404s for them until they say yes — after which they submit only their own section and the owner sees it arrive. Two browser contexts, not two sign-ins on one page: `signedInAs` writes a token snapshot without clearing the previous one, so re-signing the same page would fail several assertions later as an untraceable permission error. The 404 probes go over `request` rather than through the page, because Chromium logs a 404 against the app's own origin as a console error and `helpers/console.js` judges by provenance — a page-driven probe would fail its own `afterEach`. Second test: **withdrawing an unanswered invite frees the role**, proven by re-issuing it to a different number on the same request (201), not merely by the row disappearing — the reason to withdraw is almost always a typo, so a withdraw that left the seat occupied would be useless; the withdrawn party is gone rather than hidden, so deciding on it 404s | services/rent-agreement | live-property-integration (`--config=playwright.live.config.js`), backend `ServiceRequestFlowTest` | ✅ |
| **The Aadhaar badge against the live API** — the badge is read from `GET /me/verification/aadhaar` (not a mock store) and the seeded contact-gate flag does not grant it; a `start` returns a **pending** DigiLocker consent handle (`ref`, `verificationUrl`), never a granted badge, the next read reports `pending`, and no growth perk is fabricated; the **dev-only** `POST /me/verification/aadhaar/simulate` (non-prod `@Profile("!prod")`) then finishes the badge where no real webhook lands, so the earned-badge state is demonstrable in http/dev (D122) | trust-and-verification | live-property-integration (`--config=playwright.live.config.js`), backend `VerificationEndpointsTest` | ✅ |
| **The document vault against the live API** — the owner's `#documents` tab uploads through `POST /me/documents/{propId}` and deletes through `DELETE /me/documents/{propId}/{docId}` on a seeded, owned listing, proving the vault reads and writes the real endpoints rather than localStorage (D124) | consumer/account/owner-hub | live-property-integration (`--config=playwright.live.config.js`) | ✅ |
| **The owner hub against the live API** — the Rent-o-meter registers through `POST /me/managed-properties` and the record is re-read from outside the tab that made it; the passport's vault round-trips `POST`/`DELETE /me/documents/managed/{id}`, a *third* vault beside the per-listing one and the personal bucket; and rent tracking writes through `PATCH` without blanking the untouched valuation. **This spec exists because the mock ones cannot do its job**: `consumer/account/owner-hub.spec.js` still passes unchanged after the port, since the mock provider writes the same browser keys it asserts on — a silent regression to `lib/data/managedProperty.js` would keep every one of them green while the owner's data went back to living in one browser. Publish is deliberately not here: it mints a real listing in the moderation queue and the live database reseeds once per suite, so its 422-on-an-unpublishable-record contract is pinned in `ManagedPropertyFlowTest`, where the transaction rolls back (D32) | consumer/account/owner-hub | live-managed-properties (`--config=playwright.live.config.js`) | ✅ |
| **The published fee schedule against the live API** — the rent-agreement sidebar shows the seeded `platform_fees('rent')` service fee and GST, so the figure on screen and the figure the server bills are the same number by construction rather than by coincidence (D9, D150). The second half is the one that matters: `stamp_duty` and `registration` are **NULL by design** for that row (V52 — Art. 36A duty is a percentage of a per-agreement consideration, which one column cannot hold), so the total is labelled an *estimate*, and the spec fails if someone backfills them with a flat figure and turns a statutory estimate into a quoted price | services/rent-agreement | live-fees-and-photos (`--config=playwright.live.config.js`) | ✅ |
| **Listing photo upload against the live API** — the create-listing wizard posts multipart to `POST /me/photos`, and the returned URL is asserted **not** to be a `data:` URL, which is the only thing separating a real upload from mock mode's `FileReader` preview (that one always renders, so no mock spec can see a broken uploader). The dev response is the relative `/api/dev/storage/public/...` route, then the gallery must visibly render that server URL rather than a local copy kept beside it. This is D246: the previous `mock.storage.local` host never resolved, so dev photos never rendered and browser-side perceptual hashing could never fire. Runs with `STORAGE_ENABLED=false`, so `MockFileStorage`/`DevObjectStore` serve it without a vendor account; production R2 is cross-origin and must independently supply `Access-Control-Allow-Origin` for canvas hashing, as documented on `FileStorage.storePublic` | list-property | live-fees-and-photos (`--config=playwright.live.config.js`) | ✅ |
| **Platform pricing against the live API** — every plan card, paywall, checkout line and referral target read `fee()` out of `lib/store/billing.js`, which consulted a browser-local admin document and fell back to a `FEE_DEFAULTS` constant compiled into the bundle. No signed-out visitor has that document, so **live, every price PuneNest quoted came from the constant** — and an operator who changed a price in the back office was told it saved, and it did save, and nothing read it. The same seven numbers lived in the bundle and in the seed row, and while the browser never asked the server the two could not contradict each other, so they had drifted apart on five of the seven with nothing failing. Both tests are built around the fact that this failure is **designed to be invisible**: `PricingProvider` seeds its state with `PRICING_DEFAULTS` so first paint is never blank, which means a browser that never issues the request renders exactly what a successful one does. So the page test *waits on the response* — a silent failure times out rather than passing — and asserts the rendered figures against **what that response carried**, never against a literal, so the spec cannot rot into a second copy of the price list. FAQ 5 is the witness rather than a plan card, because a card's price is overridden by the `plans` catalogue when that table has the row and can therefore be right while `usePricing()` is broken | plans | live-fees-and-photos (`--config=playwright.live.config.js`) | ✅ |
| **A resident's society photo against the live API** — the photo modal wired only `EvidenceUpload`'s preview object into `photoUrl`, which on the wire is a `@Size(max = 500) String`; Jackson could not bind it, so the request died in deserialisation and every resident who tried to share a photo got "That could not be shared. Please try again." forever. **Mock mode cannot fail this**: the object round-trips through `localStorage` and `CommunityTab` renders its `dataUrl`, so all 54 mock society specs passed on a feature that had never once worked against the real server — which is precisely why the fix did not ship without this file. Asserts the multipart `POST /me/photos` happens *before* the contribution is filed, re-reads the row over HTTP outside the browser that made it, and requires the stored `photoUrl` to be a hosted URL rather than a `data:` one. The second test breaks the upload at the network edge and proves nothing is filed, the resident is told it was the *photo* that failed, and the caption survives for a one-tap retry. Both tests were confirmed to fail against the pre-fix code. | consumer-society-community | consumer/society/live-contribution-photo (`--config=playwright.live.config.js`) | ✅ |
| **`/staff-login` against the live API** — the screen used to read the team registry out of `lib/mockApi.js`, fabricate a user from a **browser-chosen** role and team, and sign it in without the OTP ever being checked against anything. Live it exchanges the code at `POST /auth/login` and takes role and team off the response, so the console a staffer lands on is a fact about their account rather than a radio button; an account that is neither `admin` nor `staff` is signed back out rather than merely not redirected. The picker and the demo shortcuts are gated to mock mode rather than disabled — a control that visibly does nothing is a worse lie than no control. (`POST /auth/staff-login` is email+password and cannot serve this screen: D206 removed the password from staff creation, so staff are passwordless until an emailed invite is redeemed) | (all back-office) | ops/live-drafting-desk (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/analytics`, the page itself, against the live API** — the eight-tab strip, deep links, URL sync, CSV export, the no-remount rule and every tab's headline copy, now under `playwright.live.config.js`. This row said "against the **mock** provider … six of the eight tabs are computed in the browser and have no server to talk to" until that was checked and found false twice over: **Geography** has read `listLocalities()` (`GET /localities`, measured `listingCount`/`demand`/`ratePerSqft`) since register 36, and **Seasonal** is illustrative *by decision* rather than by backlog — seasonality needs years of history the platform has not lived through — so the "when they follow" the old row waited on was never coming. Only Seasonal is browser-generated now, and it says so on screen. The conversion is what makes the per-tab renders mean anything: under the mock every tab drew from `db.json`, so "Engagement renders its charts" said nothing about whether the component survives the shape the API returns. The two assertions that carry the port: an unmeasured asking rate renders as an **em-dash in its own column while the market rate on the same row still shows a rupee figure** (asserting only the column headers would not catch the fallback returning, since both columns survive it), and an unmeasured turnaround renders **"not recorded" rather than `0h`** — scoped to the `Avg time to review` tile, because `0h` *is* legitimately on that tab from the generated Service Fulfillment and Concierge panels, which a page-wide assertion trips over and the mock config could never have revealed. Also asserts that Seasonal carries the **Illustrative data** banner **and that Traffic and Anonymous surfers do not**, which is the guard's real job — it catches a tab that has since become real still wearing the label, and that transition only happens on the live build — plus the five `Sample` chips across Pricing and SLA, checked for the chip specifically rather than for being on screen, because visibility would keep passing if someone deleted the label that is the only thing separating those panels from the measured ones | analytics | admin/live-analytics-page (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/analytics` Pricing and SLA against the live API** — the two tabs that now read the server, and the two lies the endpoints exist to retire. `pricingInsight()` filled a locality's missing asking rate with its own curated market rate, so an empty locality scored a deviation of exactly zero and was reported as *fairly priced* — the report flattered precisely the areas worth sourcing in; `slaMetrics()` drew every approval turnaround from a seeded PRNG, so the average did not move when the team got faster. **Pricing:** market and asking rates as separate fields present on every row even when null (an omitted key leaves the client one `?? marketRatePerSqft` away from restoring the fallback), `buy + rent == total` exactly (`≤` would pass with both filters broken and returning 0), and — the retired bug stated precisely — every locality that **has** a curated market rate but no priced buy listing reporting `null` rather than borrowing it. That last population is the only one that proves anything: a locality with no market rate either would report `null` under the fallback too. Inequality on the *measured* rows is deliberately **not** asserted, because the seed derives listing prices from the locality market rate and the two are legitimately identical wherever both exist. **SLA:** `targetHours` pinned at 24, the backlog anchored to more than one row before its longest-wait-first ordering is asserted (otherwise `worstPending: []` satisfies the whole test), and `?days=0` refused **400** rather than silently widened. **Both** routes refused **403** for a plain signed-in consumer — they carry separate copies of the same guard expression, so each is checked. The two UI tests assert a value only the database holds (a measured locality's exact asking rate and buy count; the title of the oldest listing in the real review queue) rather than static column headers, and this is not theoretical: on their first run they **failed**, because `analytics` had been left out of `VITE_API_DOMAINS` and the tab was silently serving the mock provider — the third time that hand-maintained list has done this (see tasks/lessons.md). Counts are otherwise asserted as relationships and null contracts rather than magnitudes, since other live specs create and approve listings during a full run (as `admin/live-reports` does for its property counts) | analytics | admin/live-analytics (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/analytics` Traffic, Engagement and Anonymous surfers against the live API** — the three tabs that read the page-view stack. They are asserted on their **contract shape rather than their magnitudes**, deliberately: the rollup job ticks 60s after startup and hourly after that, so no spec can make it run, and a suite that finishes quickly legitimately sees empty `page_view_daily*` tables (which is also why all four page-view tables are *waived* in `check-seed-coverage.mjs` — seeding the three rollup outputs would fake the job rather than a fixture). Every assertion here is therefore true of an empty window and a busy one alike. Covers: the day series **zero-filling** the requested window (length `== days`, adjacent dates exactly 86,400,000 ms apart, `series[0].date == from`) rather than skipping quiet days, which is the difference between a flat line at zero and a chart that lies by omission; `pageviews >= sessions`; the traffic sources list matching the **whole closed channel vocabulary** by set equality, since the service pre-seeds all five channels at 0 and a client that treats `length` as a signal will draw a doughnut of five nothing-slices; share percentages summing to ~100 **only when the window has sessions**, and being all-zero otherwise. Surfers: `anon + signedIn == total` exactly (a partition, not a filter), and `anonSharePct`/`conversionRatePct` being `null` **iff** the denominator is empty — a rate of `0%` over no sessions reads as a measured failure. Engagement: weeks are real ISO weeks whose wire value is the **Monday `LocalDate`**, and an unmeasured week reports `null` duration and `null` bounce rather than a perfect score; `topPages` sorted descending; `anonViews <= views` on both. Plus `?days=0` and `?days=401` refused **400** on all three routes, and all three refused **403** to a plain consumer (each carries its own copy of the guard). The one UI test asserts the null contract renders as an **em dash** end-to-end; it is not a mock/live discriminator today and says so in the file, because the mock page-view provider returns an empty window by design and an empty e2e window renders identically — it becomes one the moment the e2e database holds a rolled-up session | analytics | admin/live-analytics (`--config=playwright.live.config.js`) | ✅ |
| `/admin/users` + KYC | users-kyc | admin/live-users (masked directory, server-side status filter, search, flag needs a reason, flag survives a reload, Aadhaar badge not withdrawable, real timeline, suspend ends sessions, staff refused) | ✅ |
| **Restoring an archived user cannot collide with a live account's email** — archive is a soft delete, so an address freed by archiving can be taken by a new account; restoring the first then produced two live rows and a 500 on sign-in for *both* people. Asserts the 409, that it names the address, that a case variant still collides, that a non-colliding restore still works, and that two **archived** rows may share an address (the index is deliberately partial). API-level rather than UI-level because `AdminUsers.jsx` still restores through `mockApi.restoreRecord`, a localStorage write — `user` is not in `VITE_API_DOMAINS`, so the improved message is not yet reachable from an operator's screen (D209) | users-kyc | admin/live-user-restore-email-collision (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/finance`, behaviour only** — the console's eight KPI tiles, the four-band revenue chart, the MRR panels and the settlement ledger, exercised against the **mock** finance provider. That provider is deliberately sparse (the mock store has no subscription and no rent-payment collection), so this file asserts filtering, the two CSV exports, the detail modal and the *absence* of the three things the port deleted — the invented 65/35 payout split, the two modelled plan rows, and the `closed`/`refunded` status options that would now answer 400. Magnitudes are `live-admin-finance`'s to assert; pinning them here would make the mock a competing specification (D233, D235) | finance | admin/finance | ✅ |
| **Structural zeros on `/admin/finance` disclose themselves** — `payoutsCompleted` and `refunds` can never be non-zero (no payout or refund path exists) and service orders are excluded from revenue, so the screen names all three rather than presenting four figures the reader would trust equally. Asserts the default disclosure, that the marked rows keep their figures, that turning every flag on removes the disclosure without a code change, that flipping one leaves the other two standing, and that an absent flag is read as *not measured* (D63, D65) | finance | admin/finance-disclosure | ✅ |
| **The finance console against the live API** — every figure on this screen used to be invented in the browser: a seeded PRNG revenue curve, a ledger whose statuses came from rotating a hardcoded array, payouts as 65% of that invented figure, and a "Rent-pay fees" tile summing a `db.rentFeeLedger` collection that never existed, so the one tile that looked measured had always read ₹0. `GET /admin/finance` had existed, admin-gated, with no caller. Covers the three reads (`/admin/finance`, `/admin/finance/series`, `/admin/finance/transactions`), all admin-only; the series returning **every** month including empty ones and ending with the current partial month; the always-zero `services` band as a measurement rather than a missing field; the ledger carrying the **platform's fee and not the gross rent** (asserted with an upper bound, because the two differ by two orders of magnitude and both look like plausible money); no row carrying a mobile number; `closed` and `refunded` being *refused* rather than answered with an empty page. And the assertions the slice exists for, in the shape D233 requires — fetching the API and the rendered tile and requiring them to be **the same number**, plus the series and the overview agreeing about this month, and the plan book summing to `mrr`. Fixtures are three rent payments, one active subscription (the duplicate-guard fixture's Owner Plus, seeded for the property section and reused here as a non-zero plan book) and no boosts, so agreements and invariants are asserted rather than magnitudes: "MRR is ₹0" would pass whether or not the query works (D235) | finance | live-admin-finance (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/reports` against the live API** — the queue reads `GET /reports`, so the reason a moderator sees is the code the reporter's own modal sent, resolved through the vocabulary that belongs to *that target type* (a `spam` complaint about a person is not "duplicate listing"). Covers: the seeded queue and per-target rows, the five KPI tiles agreeing with each other and with the rows on screen, the tab-scoped reason filter (which previously offered two codes the server recognises for nothing and omitted nine it does, `broker` among them), an inapplicable reason being derived away on a tab change, the `3x` escalation badge firing at three and **not** at two, the reporter shown as **Withheld** and never "Anonymous" (`ReportResponse` omits `reporterId` on purpose), `Decided` on a terminal row where the mock spec expected a Reopen the page cannot render, checkboxes on `open` only while triage buttons also admit `reviewing`, the `?open=<uuid>` deep link, and **a real triage** — a report filed over the API, dismissed from the drawer, and re-read from the server. Also covers the **flatmate posts tab**, which this file's tile arithmetic is what forced into existence: `targetType: 'post'` reports were being filed and stored correctly and rendered in no tab at all, because the queue split rows two ways over a wire that has always carried three. Replaces the two retired mock specs for this screen (55 tests, 17 of them guarded or ending in `expect(true).toBeTruthy()`) | trust-safety-reports | admin/live-reports (`--config=playwright.live.config.js`) | ✅ |
| `/admin/team` | settings-team-staff | admin/live-team-access (directory, maker-checker queue, no hard delete, Manager not creatable, permission grid from the catalogue, narrow + restore), admin/live-rbac (server-side enforcement, the admin-only ceiling, unknown atoms) | ✅ |
| **Internal notes on a case, through the screens** — five moderation actions used to write their note into `db.internalNotes` in localStorage while the decision printed beside it was a real API call landing in the audit log, so the note was a private diary in one browser and every mock assertion about it passed for that reason. Notes are now a table of their own (`internal_notes`, four entity families, `notes:read`/`notes:write`), mutable, with the author taken off the token. This file covers the console side: a note filed beside a flag surviving the decision and a reload, a decision taken **without** a note not being reported as a partial failure (the decision has already landed server-side by the time the note is attempted, so a failed note must not make the toast say the listing was not approved), the byline, a note reaching the review modal's **Communication log** — which has had an indigo `note` style since it was written with nothing producing one — and the notes panel on a *person*, where the empty state and the disabled Add button are asserted because a textarea that silently discards whitespace is how an operator loses a note they thought they filed | property-verification / users-kyc | admin/notes | ✅ |
| **Internal notes against the live API** — the assertions a single-browser suite structurally cannot make, and the reason the item existed: a staffer files a note through their own token and an **administrator** reads it back through theirs, finding the staffer's real name on it rather than "You" or the id of whoever is looking. Deliberately inter-transparent — there are no per-team walls, because a note nobody else can read is one the next person on the case rewrites from scratch. Also: the byline is the server's, since nothing in the request names an author; an **edit** changes the words and not the author, because notes are retained customer information that goes stale rather than a signature; a note is keyed on the *pair* (`entityType`, `entityId`), so a listing's note does not surface in the account drawer of a user whose id collides; a consumer's token gets 403 in both directions; and the console's word `listing` is refused **400** on the wire, where the word is `property` — one mapper bridges the two and a permissive server would let a second spelling into the table unnoticed. The listing half is asserted at the route rather than through the console, because a listing this file creates is somewhere on page four of the catalogue and a spec that pages to find it is asserting about pagination | property-verification / users-kyc | admin/live-notes (`--config=playwright.live.config.js`) | ✅ |
| **The concierge wizard's six steps, split along the line where a server starts to matter** — the mock specs are now the part of this screen that has no server in it: the deal toggle relabelling pricing, the type cascade clearing a stale BHK before Review can print it, a land type hiding floor/facing/furnishing, the browser-local draft offering Resume after a refresh, and label-to-input association. Three tests left them for the live file, because each ended in a read or a write of `puneNestDB_v5` — and the mock provider stores the object the wizard handed it, verbatim, so "the amenities were saved" and "the deposit was not" asserted that the wizard agreed with itself about a request body the live API is never sent. That body is assembled by `toListingCreate`, which renames keys, translates two enums the contract 422s without, and drops one on purpose: a field the wizard collects correctly and the mapper never picks is collected into nowhere, and nothing on screen changes when it happens. So the live half drives all six steps and then asks the **owner's own session** what the server stored — amenities by name, and a deposit typed while the deal said rent reading **0** once the deal is flipped to sale. The two carry each other's controls: the amenities listing is a rent and asserts its deposit *did* arrive (48,000), so the zero in the sale case is the flip working rather than a field that never travels, and the sale listing ticks nothing and asserts the server supplies no amenities of its own. The duplicate-owner warning went too, and was the worst of the three — it seeded a pending listing into a store the live provider does not read, so the warning it proved could not fire against the real server | property-verification | admin/post-on-behalf(-fixes), admin/live-post-on-behalf (`--config=playwright.live.config.js`) | ✅ |
| **Owner outreach against the live API** — the first test of an endpoint that was fully built and never called: the console composed the WhatsApp text in the browser, opened `wa.me` itself, and bumped a counter in localStorage, while `message_template`, the `outbound_message` ledger and `POST/GET /properties/{id}/outreach` sat unused. Asserted at the route rather than through the console; the console half is the row below. Covers: the ten seeded templates served as copy the server owns (ids matching the console's retired `DEFAULT_WA_TEMPLATES`, so every call site keeps its id), a chaser coming back **`prepared`** with the owner's name interpolated and the handoff link carrying the exact body the server wrote (the console building that string itself is how its preview and its message could disagree), the read atom being deliberately *wider* than the write one, an owner refused a 403 on their own listing, and an unknown template answered **400** rather than sent as an empty message (400 and not 404 because the addressed resource — the listing's outreach collection — does exist, and the same branch also refuses a *retired* template, which is not a missing resource under any reading, so one status has to cover both). Two assertions used to pin known-wrong behaviour so that fixing it would be a visible change; one of them has now been fixed and its test rewritten. **`wa-pricing` quotes the locality's published rate** (`localities.rate_per_sqft` — 11200 for Kothrud, the same figure `GET /localities/{slug}` shows buyers; the mock's invented `9,500`-for-all-of-Pune is what was wrong, not the act of quoting a number, and the 140 localities with no published rate still render `{market_rate}` literally so the staff member can see there is none). Still pinned: **a chaser on an owner-posted listing is written but never counted** (the write needs only an owner with a mobile; the count is narrowed to staff-posted listings before it is asked for, so the ledger grows and `adminPipeline.reminderCount` stays 0) | property-verification | admin/live-outreach (`--config=playwright.live.config.js`) | ✅ |
| **The outreach console against the live API** — the other half of the row above: the screen that now calls it. The WhatsApp copy used to be interpolated in **three** places (the mock, the server, and a private `interpolateWaTemplate` inside `PropertyReviewModal`) and the modal's copy disagreed with the other two on every value that mattered — `staff_name` was the literal string `'You'`, so the preview read "— You, PuneNest" while the owner received the sender's real name; `claim_link` pointed at `/claim/{id}`, a route this application has never had; `market_rate` was a hard-coded `9,500` for every locality in Pune; and `listing_id` was the slug rather than the UUID. A staff member read one message and a member of the public received another, and nothing anywhere compared them. So the assertion with teeth is an **equality**: the text rendered in the preview must be, byte for byte, the `body` the server puts in the ledger — "contains the owner's name" would have passed on the broken version too. Covers: the template library arriving over HTTP rather than from the retired bundled array (the panel would have rendered identically with the API switched off), the preview/message equality, `status` still reading `prepared`, the `wa.me` handoff carrying that same string so the three things that could disagree are one, and the toast saying **"written"** rather than "sent" — click-to-chat means the staff member still has to press send, and may edit the text or close the tab first. Uses `wa-gentle` specifically: it is the only template whose every placeholder the browser can resolve identically to the server, since the others reach for `{claim_link}`, which the server builds from its own configured base URL | property-verification | admin/live-outreach-console (`--config=playwright.live.config.js`) | ✅ |
| **The communication timeline is the ledger, and nothing else** — the review modal's timeline used to be assembled by the mock's `getOwnerCommsLog`, where **five of its seven event categories were reconstructions**: "Claim link sent" was the boolean `claimLinkSent` printed at `createdAt + 1 hour`, "Photos uploaded" at `+ 1.5 days`, "Listing approved" at `+ 3 days`. No such times were ever observed — they were arithmetic on the creation date, rendered as history, on the screen an operator uses to decide whether an owner has been left alone long enough to chase again. It now reads the server's outbound-message ledger and shows only that. Asserts the delta (append-only, shared with `live-outreach`), that the newest entry's text **is** the ledger's `body` — the same equality the preview test makes, one surface further on — that it says **"Chaser written"**, and, the assertion with teeth, that **none of the five invented labels appear**; a test that only checked the chaser shows up would pass just as happily with the fabrications sitting above it. Also that no uuid is printed as an actor: `preparedBy` is a user id, and the audit log that resolves actors is admin-only by design while this is a staff screen | property-verification | admin/live-outreach-console (`--config=playwright.live.config.js`) | ✅ |
| **The concierge funnel against the live API** — listings staff created on an owner's behalf, and the hand-back that follows. Four of the 38 seeded listings now carry `posted_by_admin = true`; none did before, and the absence was load-bearing, because three separate features (the moderation card's chase button, the dashboard's "awaiting owner" queue, and `adminPipeline.reminderCount`) are all gated on that flag, so none of them could be reached from a screen or asserted in a test. Covers: the three funnel booleans being **derived** from `handback_milestone` by `PipelineStage.reached` rather than stored beside it — one fixture per point on the funnel, because four points is the smallest set that produces all four boolean combinations, and the expected table is written out rather than computed so the test cannot reimplement the rule it is checking and agree with itself while both are wrong; **both axes read back separately** (D27/V92), which is the assertion the split earns — the first two fixtures differ on `pipelineStage` and agree on every boolean, because the booleans are about the *owner's* progress and neither has had anything back from its owner, and the two fixtures carrying a milestone also sit at `docs_submitted`, which is `properties_handback_needs_listing` rather than a coincidence of the seed; the funnel naming the staff member by **id, not display name**, so a colleague editing their profile does not rewrite who posted a listing two years ago; `adminPipeline` being absent (not empty — `{}` leaks the same fact) from `GET /me/listings/{id}`, tested against the owner rather than the public because the public route serves only published listings and a 404 there would prove the status filter works rather than the projection; and a chaser on a concierge listing **being counted**, which is the other half of `admin/live-outreach`'s "written but never counted" — without a staff-posted fixture there was no way to tell a count that filters correctly from one that is simply broken. Deliberately asserts no stage transition: the console offers contacted / info_collected / listed / docs_submitted / under_review / live and the server's constraint allows listed / docs_submitted / photos_uploaded / aadhaar_verified / claim_sent / claimed — two different funnels wearing one column name, which is a product decision recorded in `tasks/todo.md`, not something a test should settle | property-verification | admin/live-concierge-funnel (`--config=playwright.live.config.js`) | ✅ |
| **The concierge desk against the freemium ceiling** — `POST /admin/properties` ran through the same creation the owner's own wizard calls, and so inherited the owner's plan cap. Because every owner this route provisions is brand new, and brand new means the free tier's single listing, an operator on the phone with somebody who owns three flats could record one and was refused the other two — with the *owner's* wizard copy ("You already have 1 of 1 listings live. Take one down, upgrade your plan, or refer an owner…"), shown to a member of staff, about an account that is not theirs. It also made the desk's own duplicate warning unreachable: a second listing for the same number was the very thing the quota rejected, so the warning that exists to catch "the owner rang again and got a different operator" could never fire. The exemption lives on the route, not on the account, so the assertions come in a pair: three consecutive concierge posts for one owner all answer 201, and that same owner's *own* `POST /me/listings` still answers `422 listing_quota_exhausted` — without the second half, "ring the office" would be the documented way around the paywall. What replaced the refusal is `GET /admin/properties/owner-standing`, which reports counts and only counts: an unknown number answers **200 with `known: false`** rather than 404, because a first call is the ordinary case at this desk and a console that renders its error banner for it renders one on most calls; an owner sitting *exactly* on their allowance is **not** flagged, since a note that appears for everybody is one an operator stops reading; and no plan name, price or renewal date is on the wire, because a desk that can read anybody's subscription off a phone number is a larger disclosure than a nudge needs. Guarded by `postOnBehalf:write` — a consumer session is refused | property-verification | admin/live-post-on-behalf (`--config=playwright.live.config.js`) | ✅ |
| `/admin/localities` + geo — the **locality curation queue** and the directory it files into. The queue used to be the browser's own `pnCommunityLocalities`: free text an owner typed that matched no locality minted a "community-tier" area in `localStorage`, and a human somewhere reviewed it. The listing that could not be placed was never in that queue, so it was approved with `locality_slug` null and went live *invisible* — reachable by direct link, absent from search facets, `/locality/{slug}`, saved-search alerts and the society join — while the console reported the area handled. Both actions on that queue (Verify, Dismiss) operated on the wrong object and are gone with the tier; the queue is now the **listings** with no locality, read from the seam, and the only action is to file one under an area that already exists (register item 24). Covers: an unplaced listing appearing with the words its owner typed, the separate "live and unfindable" count for the approved ones, the assign clearing the row, and the deliberate **absence** of any action that marks a row reviewed while leaving it unfiled — that state is the entire defect, so an escape hatch would reduce the server's refusal back to the warning it replaced. The Directory tab reads `listLocalities()` rather than the bundled `data/localities.js`, so an area added in the console is filable without a release | content-localities-societies | admin/localities, admin/maps-geo | ✅ |
| **The curation queue against the live API** — the ordering that is the bug. A listing is created through the real owner route with an area no resolver can match, so the null slug is the server's own answer rather than a fixture's; `GET /admin/locality-queue` then has to show it *with the free text*, because a curator shown only a title and a pin is guessing. Approving it answers **409** naming the queue as the remedy, filing it under `baner` succeeds, and the same approval then succeeds — the two halves only work together, since the refusal without a queue strands every unplaced listing and the queue without the refusal is optional, and an optional queue is the `localStorage` one again. Also: rejecting an unfiled listing is still allowed (junk free text is a *reason* to reject, so forcing a fake area first would manufacture the bad data the guard prevents), an unknown slug is 404 and a malformed one 400 (free text must not become a locality one screen further in), re-filing an already-filed listing is 409, the rows carry no owner name or number (clearing the queue is a geography question, and PII would make this a second readable seller list gated on `properties:read`), and a signed-in buyer is refused both routes | content-localities-societies | platform/live-locality-queue (`--config=playwright.live.config.js`) | ✅ |
| `/admin/services` moderation (absorbs old `/admin/support`) | services-moderation | admin/services-moderation | ✅ |
| `/admin/enquiries` funnel | enquiries-funnel | admin/enquiries | ✅ |
| **The demand board's masked list and its audited reveal** — the desk moved off `lib/mockApi` onto `enquiryBoardService` (register item 25), and the backend it moved onto had shipped in `eb825b6` with no route test at all, so the masking guarantee was a comment. Mock side adds two tests: at least one `98XXXXX210` on screen and **zero** ten-digit numbers anywhere, then a reveal that unmasks exactly one row and leaves the rest masked. Both run against the *mock* provider, which masks on the way out rather than serving the store's raw numbers — a mock that handed over real numbers would make masking a property of the server, and the mock suite would be exercising a different screen from the one that ships. The mock also keeps the "Responded" toast; the live console writes an internal note on the listing instead, because the board is read-only on the server and a status flip on a conversation the platform is not party to is a claim nobody can check. Two pre-existing defects went with the port: the detail modal's `Object.entries` dump (which printed every field the row happened to carry, internal ids included, under headings derived from property names) and the funnel's locality split, which read the area out of the listing *title* by splitting on the word " in " and therefore filed every real listing under "Unknown" (D25) | enquiries-funnel | admin/enquiries | ✅ |
| **The demand board against the live API** — the negative guarantees, which are the ones that die by accident. Asserts the board carries the seed's eight contact requests (a number, not "more than zero", so an empty page fails rather than passes quietly) and that **no readable mobile appears on any of the three tabs** — three tables, three columns, three different people, and on the deals tab a number that may have been typed by an owner closing off-platform and belong to nobody with an account here, which is exactly the branch a masking fix written against `users.mobile` would miss. Then the reveal: one row unmasks and **only** one, so a design that had added `?reveal=true` to the list would fail here while still showing a plausible screen; and the `enquiry.contact.reveal` row is then found on the staff-activity desk, which reads the same `audit_log` table — the two halves of the bargain asserted in one flow, because a reveal that logs nothing still returns the number. Deliberately no staff-vs-admin test: the console is admin-only at the router, so a staffer never reaches it, and the split is asserted where it is actually true, in `EnquiryBoardEndpointsTest` (D25) | enquiries-funnel | admin/live-enquiries (`--config=playwright.live.config.js`) | ✅ |
| **`/admin/content` CMS desk against the live API** — the four-tab shell, the banners counter, the FAQs tab, the create form and the route guards. This row used to cite the **mock content desk** until that was converted; the desk had no live coverage of its own, which was a gap rather than a decision — both of its data paths have been live domains for a while (`adminContentService` → `/admin/content/{type}`, and Reviews → `GET /admin/reviews`). Every magnitude is **fetched from the API and compared** rather than pinned: the mock file asserted `Showing 1–10 of 12 reviews` and the literal text of a seeded FAQ, which are facts about `db.json`, so porting them would have swapped one store's arithmetic for another's while looking like a conversion. The banners counter reading the database's own active/archived split is the discriminator — `services/config.js` falls back to the mock with a `console.warn`, not an error, so a static heading assertion would stay green while the desk read localStorage. The conversion also **found a create path that could not work in production**: the mock spec's happy-path "adding a banner saves it" filled only the headline and passed, while the server answers `422 A banners item needs 'image'`; the desk surfaces the server's field-naming message and keeps the dialog open, and both halves of that are now asserted. Splits with `tests/live-admin-content` (the seam, each type's lifecycle through a real commit, and the two Reviews-tab console decisions), which is cross-referenced from both headers | content-localities-societies | admin/live-content-desk (`--config=playwright.live.config.js`) | ✅ |
| `/admin/societies` | content-localities-societies | admin/live-societies-directory (`--config=playwright.live.config.js`) | ✅ |
| **The Directory tab paging off the server** — the last tab on this console that still read the bundled `data/societies.js`, and the tell was on the KPI beside it: "Societies" counted the array the tab had in hand, so the moment that array became one server page the tile would have read **20** and called it the platform's society count. The catalogue is 348 rows shipped as a 182 KB lazy chunk, which is why the old tab also had a completeness gate (`useSocietyCatalogue`) — a client-side pager over a list the client does not fully have yet paints 28 of 348 and, without a re-render, never corrects itself. Now `listSocietyDirectory` is a seam function on both providers: the http side returns `GET /societies`'s page envelope, the mock side awaits `ensureSocietyCatalogue()` and slices **for real**, because a mock that returned all 348 in one go would let a broken "Next" pass — page two would already be on screen. This test asserts the count line (`Showing 1–20 of N`), then clicks Next and asserts **both** that the line reads `21–40` **and** that the first cell changed, since a pager that advances its label while re-rendering the same rows is the exact bug the label hides. Then it searches and asserts the matching row appears — the search box is not a nicety here but the compensation for paging: twenty at a time over 350 rows is eighteen clicks to reach a known building, so server-side paging without server-side filtering trades one usability defect for another (D129). **Now live, and the conversion is the row's whole point.** A mock spec cannot separate the two implementations it exists to distinguish: `Table` carries an internal pager that slices rows already in the browser, and against a fixture holding the whole catalogue a client-side slice and a server round trip render the same range, the same rows and the same changed first cell. So the live file asserts the **outbound request** (`page=1` leaving the browser) rather than the rendering, and searches for a society read from the catalogue's *last* page — a target the loaded twenty provably do not contain, so a client-side filter would render the perfectly calm "No societies match that search" instead of failing. The KPI is pinned to the server's own `totalElements` and separately asserted **not** to read `20`, which is the documented silent regression: `dir.items.length` in place of `dir.total` raises no error anywhere and reports every catalogue of any size as one page long. Replaces the mock `societies.spec.js`, whose other five tests either had live homes already (`live-society-admin` for the overlay, `live-societies` for the claims queue) or were false against a seeded database — its "empty claims queue" and "empty moderation queues" both asserted an emptiness the seed does not have, the latter with no positive anchor at all | content-localities-societies | admin/live-societies-directory (`--config=playwright.live.config.js`) | ✅ |
| **The society desk's server-backed queues, and what the screen does when one fails** — the desk moved off `localStorage` onto the API seam, which put three states on it that no mock spec can reach: a queue that *fails*, a decision *in flight*, and a decision *already made*. The first is why the row exists — a failed fetch used to render as **"No claim requests yet."**, the most reassuring possible face for a request that never landed, on the screen whose only job is to say whether work is waiting. Asserted by putting `society` (and, for Moderation, `report`) onto the http seam for one page — the `services/config.js` rewrite from `consumer/connectivity.spec.js`, with an `assertPatched()` so a rewrite that quietly stopped matching fails loudly instead of asserting about a screen still on mocks — and fault-injecting underneath. The banner is asserted **beside the KPI still reading 0**, because that zero is the lie the banner exists to label, and Retry is asserted as a real re-fetch (fix the server, the work appears) rather than a dismissal. Then: both buttons on a row disable while that row's own PATCH is in flight and a *dispatched* second click — dispatched rather than clicked, so the assertion is about `decidingRef` closing before the paint does and not about Playwright declining to click a disabled button — sends no second decision; a decided row shows when it was decided while its pending neighbour keeps its buttons; a 409 surfaces the **server's** sentence ("This claim has already been decided.") rather than the generic fallback, and leaves the row pending and actionable. On Moderation a `society_contribution` report round-trips through `reportMapper` — rendered "Community post", with `spam` resolved against the *society* vocabulary and not the listing one — where Dismiss sends `{status:'dismissed'}` and Remove sends `{status:'actioned', enforcement:'hide_content'}`, two facts and not one, since the status alone closes the complaint and leaves the post up. Deliberately no `consoleErrors` assertion: these tests fulfil real 500s on the app's own origin, so an empty console would mean the injected failure never happened | content-localities-societies | admin/societies-queues | ✅ |
| **The society ops console against the live API** — the first live coverage this console has ever had, and the reason is the point: until `87f2d07` all five of its queues read `localStorage`, so a "live" spec would have signed in against a real backend, opened a real screen, and asserted against the test runner's own browser storage. There was no honest live spec to write. Every queue is now a route, so this file seeds no storage at all — every row it asserts either comes from the seed or is created by the test over the API. Covers: the four queues answering (asserted through the *absence* of the disclosure banner, because an empty table on an ops screen reads as "nothing to do" and a silent read failure is the expensive kind); a claim carrying the **registration number as its own field** rather than concatenated into the note, which is what V109 restored after the number was briefly smuggled into the claimant's free text and made unsearchable; the candidates queue holding the seeded unverified community society and **not** the seeded verified one — both are `source = 'community'` and only one carries a verification stamp, so a queue filtering on `source` alone would have an operator re-confirming a society ops signed off eleven days ago; a candidate with no recorded `mint_origin` (V108) **claiming none**, where the chip used to be a two-branch ternary that rendered a confident "From a listing" on exactly the rows that predate the column; verification taking a row off the queue while leaving `registration` and `conveyance` **false**, because those describe the building's legal paperwork and not our confidence in the record — the browser-side promotion this replaced set both, so confirming a society existed silently told every buyer its conveyance deed was done; a second operator getting **409** rather than overwriting the first, since who confirmed a society is the only thing that says who to ask about it later; a merge through the dialog taking the duplicate off the queue, onto the *Merged duplicates* record beneath it, and back again on Undo — with the duplicate's own row asserted still resolvable, because the merge is a pointer and that is the entire reason an undo can exist; the server refusing a **chain in both directions** and naming the merge to undo first (collapsing it is the one outcome that cannot be reversed, and a self-merge is 422 rather than 409 because nothing is in the way — the request is simply void); and the residents queue showing **both** seeded societies at once with the right desk on each, which is the half of the routing rule a single claimed society cannot show, and the reason `GET /admin/society-residents` exists rather than looping the catalogue. The merge picker is driven with a **catalogue** name, which is no longer a workaround: it now searches `GET /societies?q=` like every other picker, so a survivor minted over the API a second ago is findable — the catalogue name is used because it is a stable fixture, not because the search cannot see anything else | content-localities-societies | admin/live-societies (`--config=playwright.live.config.js`) | ✅ |
| **The society overlay editor against the live API** — until this slice the Directory tab's Edit dialog wrote `pnSocietyOverlay` in `localStorage`: registration verified, conveyance done, claim status, maintenance per sqft and an internal note saved to the browser that typed them and nowhere else. Four of those five are exactly what a buyer reads to judge whether a building's paperwork is in order, so an operator who ticked "conveyance done" corrected the record for themselves and for no one, while being told it had saved. That bug is invisible to a mock spec **by construction** — a seeded spec asserts against the same `localStorage` the dialog wrote, so it passes *because* the write never left the browser, and the mock `societies.spec.js` was green throughout — that file has since been deleted for the same reason. Every assertion here therefore reads the value back over the API with a token of its own. The first test also waits on the **GET** before the click, because `openEdit` used to seed the form from `getSocietyOverlay(slug)` and a round trip that never involved the server looked identical to one that did. The second pins the refusal path: the old form wrote whatever it was given and could not fail, which is why the success toast fired unconditionally — now an out-of-range figure is refused 422, the dialog stays open, and no toast claims otherwise. The third proves the note is **not** on the anonymous payload, asserted twice (absent key *and* prose absent from the serialised body, since either alone has an obvious false pass) with the four public facts as the control, so a PATCH that silently discarded everything cannot pass it. Rows are driven by the Directory tab's **first row** rather than a named society: the tab is now a real server page (`GET /societies`, twenty a page, `name` ascending), so which building sits in row one is the seed's business and not this test's — the slug is read back off the PATCH the dialog itself issues, so the test never needs to know which building it picked | content-localities-societies | admin/live-society-admin (`--config=playwright.live.config.js`) | ✅ |
| `/admin/settings` | settings-team-staff | admin/live-settings-console (`--config=playwright.live.config.js`), platform/live-settings-debug (`--config=playwright.live.config.js`) | ✅ |
| `/admin/flatmates` — **retired**; the route redirects to `/ops/flatmate-review`. The admin desk moderated seekers, groups and group applications out of `db.json`, could not see rooms at all, and had no view of the D72 publication axis, so the three queues it offered were a subset of the live desk's three read against a store nothing else wrote. What is left is the redirect and its two guards: an unauthenticated visitor is still sent to `/staff-login` rather than through to the ops desk, and a buyer is still refused. | flatmates | admin/flatmates, admin/flatmate-moderation-reach | ✅ |
| `/admin/staff-activity`, including **the module switched off**. `adminFlags.staffActivity.enabled` is a block of the shared settings *document* every administrator in the company reads; `post-on-behalf.spec.js` reached it by read-modify-writing `puneNestDB_v5` in the page, which tested that the component branches on a boolean it was handed and nothing about whether the switch an operator flips is connected to the branch. Here the flag is turned off through `PUT /admin/settings` and turned back on in a `finally`, with the restore proved through the screen rather than through the response — the read path is a different one (`AdminFlagsContext` merges the document over its defaults), and a restore that satisfied the API while leaving the console dark is exactly the leak the block exists to avoid. The fallback is asserted with its way out (an "Enable in Settings" link, because a dead end means an administrator being told by somebody else which of forty switches to look for) and against a control: the feed's table and its `kpi-total` tile must be **gone**, since every positive assertion above is also satisfied by a page that simply failed to load — the same pixels a broken fetch produces | settings-team-staff | admin/live-staff-activity (`--config=playwright.live.config.js`) | ✅ |

## Ops

| Route / Feature | Flow doc | Spec(s) | Status |
|---|---|---|---|
| `/ops/flatmate-review` — the flatmate desk, live-only. The mock modelled **one third** of it: `lib/data/flatmates.js` knew host verification and nothing at all about the D72 publication axis or about group applications, so two of the three boards had never been exercised by a test. Converting it also meant building something, not just wiring: the group-applications board read a table nothing in the product could write to, so `POST /flatmates/groups/{id}/apply` plus an owner inbox (`GET`/`PATCH /me/group-applications`) and `GET /me/flatmate-groups` were added — four routes the OpenAPI contract does not name, an intentional extension recorded in `docs/flows/ops/flatmate-moderation.md` section 10. Two rulings are visible on screen: the host's mobile is **masked in the mapper** even though `FlatmateReviewDto` sends it in full (a desk that can ring a host can be talked into ringing one on somebody else's behalf), and the moderation axis offers no `rejected` because on that axis it means what `removed` means. Live coverage (5 tests): a tenant declaration reaches Verification and approving moves the **badge and not the post** (the two axes must not reach each other); a rejection without a reason is refused; a brand-new seeker post is born Pending and is absent from the public feed until Publish, which is the whole claim D72 makes; Remove takes a note and the free text is rendered untruncated (it is where a phone number goes when the contact field will not take one); and the group-applications board shows a **real** row for the first time. All five fixtures are minted through the consumer path, because the seed's five flatmate rows are all already `approved` | flatmate-moderation | ops/live-flatmate-moderation (`--config=playwright.live.config.js`), ops/flatmate-review (the consumer cues + the shut panel) | ✅ |
| A group applies for a whole flat — the loop the mock never had. `lib/groupApplications.js` seeded two rows into `localStorage` and offered an owner two buttons; nothing could create an application, so the board it fed could never have a row. The card on the listing page appears only for a signed-in visitor who hosts a live group with an open seat, on somebody else's **rental** listing (a sale price is whole consideration, so per-head would be wrong by orders of magnitude). The owner answers from the dashboard Action Center, writing `status` — an axis the ops desk deliberately cannot reach, since it writes `modStatus`. Server rules are pinned in `FlatmateApplicationEndpointsTest` (13 tests), including that a stranger's `PATCH` gets **404 not 403**, to avoid an existence oracle | flatmate-moderation | consumer/flatmates/live-group-apply (`--config=playwright.live.config.js`) | ✅ |
| `/ops` dashboard — its tiles and doughnut now count real tickets from `GET /tickets`, and it no longer restates the server's team scoping (it passed `role === 'admin' ? undefined : team`, a second copy of a decision `TicketService.list` already makes — D44). In mock mode it shows the reason it is shut rather than a wall of zeros, because "0 open" and "nobody looked" are different sentences | service-queues | ops/requests (the shut panel), mobile/live-ops-field | ✅ |
| `/ops/requests` — the ops ticket board, live-only. Converting it off `lib/mockApi.js` was not wiring: the mock knew **three** statuses where `TicketStatuses` knows five, assigned by typing a display **name** where `TicketUpdate` takes a user **id** (and 404s an id that is no ops user), and read-modify-wrote the whole `notes` array where the server has a dedicated append — so two colleagues saving a note in the same minute erased each other. D184's refusal of a translation table applies unchanged, hence no mock provider. Live coverage (7 tests): a ticket a customer raised reaches the desk that owns it; the tiles carry the server's words and **not** the mock's `New`/`Done`; a claim writes the caller's own id and the name comes back resolved by `TicketMapper`; **a claim does not also advance the ticket** (putting your name on something and declaring it underway are two decisions); a note appends and a second does not erase the first; a note survives a reload; and a rental staffer cannot see a legal ticket that provably exists. The suite mints its own tickets via the deliberately unguarded `POST /tickets` (S43) because the seed ships none | service-queues | ops/live-ops-board (`--config=playwright.live.config.js`), ops/requests (guard + the shut panel) | ✅ |
| The verification case file (D218), live-only. Three things the mock cannot hold, because in mock mode none of them exist. **(1)** A second listing on one electricity meter is published, not refused — sometimes it is a broker relisting somebody's flat and sometimes a genuine re-let, and refusing outright punishes the honest case to catch the dishonest one — but it files a staff-only note naming the incumbent listing. The note deliberately does **not** quote the meter number, and that is asserted: `review_messages.body` is free text sitting outside the `PrivateFieldVisibility` projection and outside the erasure classification, so a note carrying the value would route a guarded field around its own guard where no mapper test would look (D219 review). The owner asking for their own case file gets **404, not an empty thread**: an empty thread still tells them a file was opened on them. **(2)** The stays-live re-check note is now composed **server-side**; until D218 `PropertyReviewModal` typed it in the browser, so an edit through any other client produced silence and the note claimed "approved" whether or not the write had landed. **(3)** A **pending** listing is not told it stays live and gets no case file manufactured for it — `requestRecheck` refuses on anything not publicly visible, so the note had been narrating work nobody was doing. Fixtures are minted on the wire through `POST /me/listings` rather than through the wizard, which keeps the fixture independent of the UI even now that the wizard reaches the same endpoint (D219). Server rules are pinned in `ListingNoticesTest` (19 tests) | property | ops/live-verification-thread (`--config=playwright.live.config.js`) | ✅ |
| Who may **read** and who may **decide** a verification case (D219), live-only and deliberately a separate file from the thread spec above: a broken workflow is a bug report, a broken refusal is a disclosure, and a red line should say which just happened. Four tests. A signed-in stranger gets **404** on every thread route — read, post, mark-read and open — because a 403 would confirm that a listing with that id exists and is under review, which is the fact a competitor walking ids would probe for; the owner and the desk are asserted to get 200 on the same case first, so the refusals cannot be explained away as "the case was never created". The **bodies** of the two 404s are compared as well as the codes, since "no such listing" and "not yours" as distinguishable replies would restore the oracle the status code was chosen to remove. The staff-only routes answer **403** instead, and that is not an inconsistency: their guard is a role rather than a relationship, `@PreAuthorize` refuses before the id is ever looked up, so nothing about the row can leak through a method that was never entered. Last, the maker-checker rule, which is the only refusal here that is about neither: a staffer listing their own flat is a participant **and** holds `properties:write`, so every other guard passes and the listing would publish with nobody having read it — a second staffer decides the same case without complaint, which is what separates "the rule works" from "the route is broken" | property | ops/live-verification-access (`--config=playwright.live.config.js`) | ✅ |
| `/ops/rent-agreement`, `/ops/legal`, `/ops/interior`, `/ops/packers`, `/ops/valuation` — **retired**. Five routes, one `OpsServiceQueue` over `localStorage`, while the work had moved to Postgres: in live mode every one of them was blind. They now redirect to `/ops/drafting-desk?type=<team>` (kept rather than 404'd — they are in bookmarks, in `TEAM_HOME` and in the flow docs), and `ops/requests` asserts the redirect. `TeamRoute` went with them; dropping it widened nothing, because `ServiceDeskAuthority.deskFilterFor` scopes a staff caller server-side and ignores a `team` they do not own (D44) | service-queues | ops/requests (redirect + guard-is-gone), ops/live-drafting-desk (the desk picker that replaced the guard's UX role) | ✅ |
| `/ops/referrals` — the fraud queue, live-only. The mock disagreed with the server about what a referral **is**, three times over: a `flagged` status `ReferralStatuses` does not have, both mobiles in full where the server masks them, and an Approve that granted a device-local perk where the server pays rupees. Converting it also moved a **rule**: the Aadhaar check was a greyed-out button in the browser, under a banner calling it mandatory, while `POST /referrals/{id}/approve` released the money to anyone who called it directly. `ReferralService.approve` now refuses with a 409 naming the reason — reading the referee's **current** badge, not the referral's `updatable = false` redeem-time snapshot, because redeem-then-verify is the ordinary order. The mock's perk grant could not be ported at all (it looked the referrer up by a number that is no longer on the wire, for a reward the contract declines to model) and is recorded as intentionally dropped. Live coverage (5 tests): a redeemed referral reaches the desk with **both** numbers masked and the signals in their place; approving credits rupees the referrer can see on `GET /me/referrals`, which is the only proof the decision reached them; the Aadhaar refusal arrives from the endpoint when the test goes round the greyed-out button; a clawback leaves `clawed-back` and **not** `rejected` (the one distinction a fraud desk needs — whether money ever left the building); and **High risk** is a `risk` filter with no Flagged tab beside it. The suite mints its own referrals through the consumer path — `GET /me/referrals` for a code, `POST /referrals/redeem` — because the seed ships none | referrals-fraud | ops/live-referrals (`--config=playwright.live.config.js`), ops/referrals (guard + the shut panel) | ✅ |
| `/ops/support` — the admin support queue, live-only. The mock could state the queue but not the thing the queue is *for*: one store and one flag meant "the desk read it" and "the customer read it" were the same bit, so the two-sided read model (D50/V53) — the whole reason `staff_unread` exists as a second column — was unfalsifiable against it. Live, opening a ticket clears the desk's side and is asserted to leave the customer's alone, read back through the raiser's own `GET /support/tickets/{id}`. The conversion also found a 500: `SupportTicketMapper` dereferenced a **nullable** `author_id` three lines below code that filters nulls out of the name lookup, so one message whose author had gone took the entire thread down for the person who raised it | service-queues | ops/live-support-queue (`--config=playwright.live.config.js`), ops/support-queue (routing only) | ✅ |
| `/ops/drafting-desk` **in mock mode** — the route guard, and that with no live API the desk shows the offline panel rather than an empty table. D184 retired the mock provider's three desk operations (the desk filters on the server's nine-value status vocabulary, which the mock store cannot speak; a translation table was rejected), so the screen now gates on `isHttpDomain('serviceRequest')`. An empty queue and a queue it cannot filter render identically, and only one of them is true — hence the panel | service-queues | ops/drafting-desk | ✅ |
| `/ops/drafting-desk` **against the live API** — 8 tests, the desk that replaced the five retired ones. Covers: the queue with its server-side filters; **a staffer is offered their own desk and no other, whichever desk they ask for** (the picker that replaced `TeamRoute`'s user-visible role — an empty queue and a forbidden queue must not look alike, D44); **a matter names the paperwork it is waiting for** (the read-only D120 checklist — "*n* of *m* received" plus every item including the missing ones, which are the point; no viewer and no "Mark all verified", because the fold is derived on read and the only thing that moves an item is an upload); and the identity-disclosure design (D151/D173) — the queue carries no identity number or mobile, an unassigned request is refused the reveal in the server's own words, taking it unlocks the reveal and Hide puts it away, a disclosure does not survive closing the matter and never reaches the URL, and the summary shows named fields only, never the raw `details` object | service-queues | ops/live-drafting-desk (`--config=playwright.live.config.js`) | ✅ |

---

## Notes on recent migrations

**A lazy-loading defect in a specification-backed read is unreachable from the Java suite.**
`GET /admin/properties` shipped a `LazyInitializationException` on `owner` — the moderation queue
returned 500 for every caller — while `PropertyModerationQueueTest` stayed green. It is not that the
Java test was thin: it already covers status widening, `archived`, `recheck`, `q`, the 403 for a
seeker and the 401 for an anonymous caller. It is that `AbstractApiTest` is `@Transactional`, so the
persistence context stays open for the whole test method and a lazy proxy can always resolve. An
assertion on `content[0].owner.mobile` would pass with *and* without the fix, which is worse than no
test at all. Reproducing it needs a non-transactional test that commits users and properties, and
`punenest_test` is required to stay empty of domain data. So the guard is the live spec — the
moderation-queue test in `live-property-integration` — and the thing it protects is the join fetch in
`PropertySpecs.adminSearch`, which is written the awkward way (`root.fetch` guarded on
`!Long.class.equals(query.getResultType())`) because Spring Data issues a separate COUNT query where
a join fetch is invalid SQL. Any future `JpaSpecificationExecutor` read that renders an association
is in the same blind spot and belongs here rather than in the Java suite.

**The paid rent-agreement path is deliberately outside this suite.** E2E runs mock-mode, and the
mock service-request provider returns no `paymentSessionId`, so the checkout branch is unreachable
here by construction — a spec for it could only assert against a stub of our own making. The gate
lives in `ServiceRequestFlowTest.PaidGate` on the backend, which drives the real state machine: a
priced request is created at `awaiting-payment`, the ops queue does not show it, a signature-verified
webhook moves it to `new` (or cancels it on failure), redelivery is inert, an unrecognised `type` is
a 400 rather than a free desk, a second unpaid request for the same desk is a 409, and identity
numbers in `details` are refused. What e2e still owns is everything either side of the payment: the
wizard's validation, the co-fill invite flow, the re-submission lock, and the tracker. End-to-end
confirmation that money moves needs the Cashfree sandbox, not Playwright.

**Share-a-flat → Flatmates rename.** Every `share-flat-*.spec.js` was replaced by its
`flatmates-*` equivalent, `shareflat-map-popup` → `consumer/flatmates/live-map-popup`, and
`home-share-flat` → `consumer/home/live-flatmates-rail`. `/share-flat` still redirects, and
`consumer/flatmates/live-discovery` asserts the legacy `?view=` deep links resolve. Three flatmates
specs still run cross-viewport because the discovery/posting surfaces differ on a phone —
`owner-split` and `posting` in the mock config, and `live-discovery` in the live one, its entry
having moved with the conversion rather than been dropped.

**KYC badge-not-gate migration (ADR-019).** The old Aadhaar *gate* specs were replaced
by badge-not-gate specs — `consumer/list-property/live-no-gate`, `consumer/property/live-contact-badge-not-gate`,
`consumer/flatmates/no-gate`, `consumer/flatmates/seeker-verify` (opt-in seeker badge) — plus
`platform/auth/live-kyc-growth-levers` covering the DigiLocker Verified-badge funnel on the dashboard,
and `platform/auth/live-verify-payoff` covering what the badge actually *buys*
(`--config=playwright.live.config.js`).

**The payoff moved half into the backend.** `verify-payoff` used to assert the badge flowing onto
the owner's listings by reading the mock catalogue back out of `localStorage`. Live, a browser cannot
earn a badge at all — the grant arrives on a signed webhook — so the write is proven by
`VerifiedOwnerListingsTest` (backend) and only the buyer-visible payoff stayed in Playwright. The
7-day Featured perk the mock granted on first verification was **not** ported: the backend has no
`featured_until` and no perk ledger, so it is an open product decision, recorded in
`docs/migration/README.md` rather than invented here.

**Suite consolidation.** The duplicate `frontend/e2e/` suite is gone; this directory is
the single source. Shared fixtures live in `helpers/app.js` (`seed()`, `OWNER`,
`SEEKER`, `ADMIN`, listing factories).

**Flat `tests/` → audience → feature tree.** 191 specs moved out of a single folder
into the tree above, and the viewport routing moved with them: `playwright.config.js`
now selects on `tests/mobile/**` rather than a `mobile-*` filename prefix, so the
convention is visible in the file layout instead of being a rule people had to know.
Filenames dropped the prefix their folder now carries (`flatmates-discovery.spec.js`
→ `consumer/flatmates/discovery.spec.js`).

The move surfaced three **empty** spec files that had been counted as coverage:
`mobile-zzdiag`, `desktop-mobile-guardrails` and `listings-mobile-only-controls` — the
last of which this matrix cited against `/listings`. All three are deleted; the
mobile-only deal-toggle assertions it claimed actually live in
`consumer/search/live-listings-responsive-controls`, so no coverage was lost. That is the
failure mode `scripts/check-coverage-citations.mjs` now guards against.

---

## Gap backlog

Debt wave 10 shipped four user-visible frontend changes and **no e2e**, which is the
only open backlog. Each is covered by a backend test or a build-time gate — the gap is
specifically that nothing drives them through a browser, which is where each one would
actually be wrong.

| Open gap | Why it is a gap, not a nicety |
|---|---|
| **Progressive disclosure on `/property/:id`** (D141) — four sections now start collapsed on mobile | `mobile/live-property-contact` scrolls to a fixed y-offset on this page and passes because the sticky bar is fixed, not because the content below it is right. A collapse that fails open costs nothing; one that fails **shut** silently hides amenities, and no assertion would notice. Needs a spec that expands each of the four and asserts the content arrives. |
| **The Turnstile widget on `/signin` and `/signup`** (D130) | The widget renders only when a site key is configured, so in mock mode it renders nothing and every existing auth spec passes either way. The failure worth catching is the opposite one: a misconfigured key rendering a *blocking* widget on a form whose button is deliberately not gated. |
| **Report triage enforcement** (D68) — `actioned` + `hide_content` must take the listing down | Covered by `ReportService` tests server-side. What no test covers is the admin screen's half: that the enforcement control is *present*, that it is refused on a non-`actioned` decision, and that the dashboard's `openReports` tile agrees with the queue it links to. |
| **`buy.stamp_duty` renders as absent, not as ₹0** (D118 / V55) | `check:finance` proves the client's window matches the server's; it says nothing about what a null renders as. The whole point of the change was that a wrong number looked like a right one. |

### Closed, with what closing them found

The Phase-2 gaps and the seven items that followed them are closed. The table below
records what closing the last batch found, because each one is a trap the next spec
author can fall into again.

| Closed item | What the new spec found |
|---|---|
| `/services` hub → `consumer/services/live-services-hub` | Nothing broken, but the hub's `.reveal` blocks sit at `opacity: 0` until scrolled into view. `scrollIntoViewIfNeeded()` deadlocks on them (scrolling requires visibility); force `.visible` instead, as `consumer/property/live-detail-improvements` does. |
| `/locality/:slug` → `consumer/search/live-locality-intel` | The intel cards are behind `Tabs`; only Overview renders on load. Also `NativeSelect` is a themed `.pn-dropdown`, so `selectOption()` never resolves — open the trigger and click a `[role="option"]` from the portaled menu. |
| `/pay-rent` → `pay-rent` | `puneNestDB_v5` cannot be seeded in an init script: mockApi migrates and merges it at module load, so a partial object leaves the app with no settings and a blank page. Load once, mutate the real DB, then navigate. |
| `/admin/localities` → `admin/localities` | `Table` renders a desktop `<table>` **and** a `.pn-card` list for phones, hiding one with CSS. Unscoped text assertions either trip strict mode or resolve to the hidden copy. The queue is **empty** in the seeded store — every listing in `db.json` has a locality — so a test needs one unfiled, and `puneNestDB_v5` cannot be written in an init script (mockApi migrates and merges it at module load). Navigate first, then mutate. |
| Help feedback → `platform/help/live-article-feedback` | Fine as shipped: a thumbs-down writes nothing until the comment form is submitted, and the widget resets per article slug. |
| `DualRange` parsing → `consumer/search/live-dual-range-parsing` | Fine as shipped: `Cr`/`L`/`Lakh`/`Lac`/`K`, `₹`, commas and spaces all parse; unparseable input is ignored rather than coerced to 0. |
| Responsive pickers → `platform/live-date-time-fields`, `mobile/live-date-time-fields` | **Two real bugs.** (1) `.pn-datefield` / `.pn-cal` / `.pn-timepicker` lived in `styles/routes/list-property.css`, which only `ListProperty.jsx` imports — so on the other ~19 surfaces the field rendered as a plain block and the calendar as a static, unlayered element that reflowed the page. Moved to `styles/components/date-time-fields.css`, imported by `DateField.jsx` and `TimeField.jsx`. (2) `.pn-timepicker { width: 250px }` sat after the bottom-sheet block at equal specificity, so on a phone the time picker docked as a 250px stub against the left edge while the calendar went full width. Now guarded by `@media (min-width: 640px)`. |

**Measuring a fixed overlay.** `boundingBox()` is document-relative, so on a
scrolled page it reports a docked sheet hundreds of pixels below the fold and
fails against correct CSS. Read `getBoundingClientRect()` in `evaluate()`, and
poll rather than sample once — the sheet animates up from `translateY(100%)`.

## Known-failing (pre-existing, product/spec drift)

None outstanding in this area. Three assertions in the old mock `types` spec under
`consumer/list-property` had drifted from the wizard and were carried here as
known-failing. All three were resolved when the folder moved to the live suite: the
land case now asserts what the product actually does (the 7/12 Extract is the
ownership proof on offer, and the document block raises no error — the old spec
demanded a mandatory-document error the wizard stopped emitting), while the MPCB and
Agreement Duration cases turned out to be reachable and pass unchanged in
`consumer/list-property/live-types`. The whole folder runs green against the live API.

Each ✅ spec asserts primary buttons/functions (happy path), the role/flag/team **guard**
(redirect when unauthorized), an **empty state**, and any **maker-checker** transition.

