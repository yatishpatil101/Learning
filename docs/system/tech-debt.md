# PuneNest Backend — Tech-Debt Register

**Status:** living document, and the **single source of truth for outstanding backend work**.
**Nothing here is scheduled until the backend feature slices are complete.** This is the parking
lot: the place a deliberate shortcut goes so it stops being a thing someone has to remember.

**Companion doc:** items that need a *human decision* rather than engineering time live in
[`open-questions.md`](./open-questions.md). The split is deliberate — mixing "we know what to do,
we haven't done it" with "we don't know what we want" makes a register unactionable, because you
cannot pick up the top item and start.

**Why this file exists.** Deferred items were accumulating inside `tasks/todo.md`, interleaved with
per-slice plans and RESULTS blocks across 2,600+ lines. That file is a *worklog* — chronological,
append-only, correct for "what happened in slice 6". It is the wrong shape for "what do we still
owe", because answering that meant grepping nine `### Deferred` headings and reconciling them by
hand. This register is the answer to that question, and it is the SSOT for it.

## Rules

1. **An entry needs a reason, an owner and a trigger.** "Deferred" with none of those is not debt
   management, it is forgetting with extra steps.
2. **Debt is deliberate.** A bug is not debt — fix it. Debt is a shortcut we would take again given
   the same information, and would not take with more time.
3. **A finished item is deleted, not archived.** Once the work is done and the reasoning lives
   somewhere it will actually be read — a Javadoc, a comment beside the code, a test that fails if
   the rule is broken — the register entry has no remaining job and is removed outright. *This
   reverses the original rule*, which kept a "Closed" table so nothing "quietly stopped being
   mentioned". That was the right instinct aimed at the wrong risk: a register nobody finishes
   reading is its own failure mode, and by 2026-08-07 the closed section was longer than the open
   one. Git history is the archive. **Two things are still never deleted:** a *ruling* (§2 — a
   settled "no", which exists precisely so it is not re-raised), and any closed item whose lesson
   has no home in code.
4. **Numbers are never reused.** Deleting D19 does not free D19. New debt continues from the
   highest number ever issued, so a reference in an old commit or comment can never resolve to a
   different item than it meant.
5. **This file does not schedule work.** When a slice picks an item up, it goes into `tasks/todo.md`
   as a checkable plan item; the entry here is deleted when that work lands.
6. **Per-slice RESULTS blocks stay in `tasks/todo.md`.** Only the *carried-forward* residue lands
   here, and it lands here **once** — no duplicate bookkeeping in two files.
7. **If it needs a decision, it is not debt yet** — it goes to `open-questions.md` and returns here
   once answered. An item blocked on "what do we actually want?" will never be picked up from a
   backlog, because the person reading the backlog is not the person who can answer it.

---

## 1. Open register

**56 open — none High.** 28 Med, 28 Low. Largest clusters: `design` 28, `frontend` 7,
`security` 4, `contract` 4. Highest number ever issued: **D199**; numbers are never reused
(rule 4), so the 133 gaps in this list are deleted items, not mistakes.

> **Counting this table by hand or by a naive script gets it wrong**, and has for months. Two traps:
> rows contain escaped pipes (`hasListings() \|\| …`), so splitting on `|` shifts every later column
> and silently mis-reads the priority; and the High rows are *also* listed in the summary above,
> with fewer columns, so counting row lines double-counts them. Unescape first, and prefer the
> 7-column row for any id that appears twice. A third, smaller trap: D84's escaped pipe lands
> mid-priority so it parses as `daily\`; its true value is Low, and the figure below counts it as
> one. A fourth: **nine** rows are *closed but kept* (D7, D36, D64, D69, D76, D180, D185, D186,
> D187) and carry `—` in the priority column, so counting rows counts them as open. The figures
> above satisfy 28+28 = 56 open, and 56 + 10 rows kept as closed + 133 gaps = D199, which is the
> check that they hang together.

> **The count above was wrong for months and is now derived, not asserted.** The header claimed
> "60 open" while the table held 78 rows — every pass edited the prose and not the arithmetic, so
> the one number a reader takes at face value was the least trustworthy thing here. Recount before
> editing it:
>
> ```powershell
> $l = Get-Content docs/system/tech-debt.md
> $s = ($l | Select-String '^\| # \| Item \| Area \| P \|').LineNumber
> $e = ($l | Select-String '^### Detail on the ones').LineNumber
> $rows = $l[$s..($e-1)] | Where-Object { $_ -match '^\| D\d+ \|' }
> $rows.Count
> $rows | ForEach-Object { ($_ -split '\|')[-3].Trim() -replace '\*','' } | Group-Object | Sort-Object Count -Descending
> ```

**There is no High item any more** — D67, the last one, was wired on 2026-08-11 and dropped to Med
with three named residuals. That is worth stating plainly rather than leaving an empty table: the
list is now entirely things that are safe to leave, which has not been true before.

> **API-polish pass (pre-integration).** The backend was reviewed end to end before frontend
> integration started. Five defects were found and fixed rather than recorded — they were bugs, not
> debt (rule 2): the 400 handler echoed Jackson's message verbatim, 405/415 were being swallowed into
> 500s by the catch-all, `server.servlet.context-path=/api` was documented everywhere and set
> nowhere, `punenest.web.cors.allowed-origins` was unset in prod, and the foundation-field rule
> covered five of the seven search facets, so an approved listing could be relabelled "furnished" or
> "ready to move" without re-moderation. Four endpoints were added (`listPropertyRooms`,
> `updateSavedSearch`, `listListingBoosts`, `listReviewsForModeration`) and four reads paged. Suite:
> 705 → 733 tests, green. The pass also *created* five new entries — D76–D80 below — because several
> fixes revealed adjacent problems that were out of its scope.

**Actionable today without any decision** — the honest short list is now short.
**D46**, **D49** and **D60** are one-liners, but each of the three is explicitly waiting on a
consumer that does not exist — an unassign gesture, a message attachment, a share channel — and
building the field before the caller is how the register filled up with `settings.permissions`
(D67). The remaining no-decision items are **D95** (one verify-funnel spec),
**D97(d)** (ops cannot see flatmate rooms — the one with a user-visible
consequence) and **D33** (trim the `@param` ceremony).

> **Debt pass, 2026-08-07.** Nine items closed: D90, D82, D19, D22, D83, D86, D97(d), D95, and the
> durable half of D33. Every fix was mutation-tested — the fix reverted, or damage deliberately
> planted, and the test watched to go red — because in each case a green assertion could have meant
> nothing.
>
> **Four of the nine were filed with the wrong size or the wrong cause**, which is the most useful
> pattern here: *the register's own numbers were the least reliable thing in it.* D19 said "7 files,
> cosmetic" and was 22 files including mangled rupee signs in live prices. D96 said "50 specs filter
> nothing" and was 9. D97(d) said "rooms are invisible" when ops could see nothing at all. D33 said
> 562 `@param` and it is 673. **Measure before scheduling, and re-measure before deleting.**
>
> Findings worth more than the items themselves:
>
> 1. **A `@Transactional` test harness cannot see a bug that happens at commit time.** D90 survived
>    750 tests because every HTTP test extends a rolling-back base class, and the defect was a
>    failing commit. The guard had to be written *outside* the harness that hid it.
> 2. **Detect by round-trip, not by a list of known-bad patterns.** The first D19 repair script used
>    a hand-built map and missed two whole families nobody had grepped for. A table can only ever fix
>    the damage somebody already noticed.
> 3. **A mock more permissive than its server passes tests the real thing would fail** (D97d).
> 4. **A mutation test caught a bad assertion, not just a bad fix.** The D95 one-shot-perk test
>    passed with the guard deliberately disabled: with no guard the perk moves to the *next* listing
>    rather than re-extending the first, so an assertion on the first listing's window could never go
>    red. Counting grants across the set can. An assertion that cannot fail is worse than no test —
>    it reads as coverage.
> 5. **Consolidating filters must merge them first.** The local console-noise lists were not subsets
>    of the shared one, so deleting them and pointing everything at `IGNORE` would have silently
>    *tightened* several specs. The shared list absorbed `gstatic`, `maptiler` and the React DevTools
>    banner first — but deliberately not bare `ERR_` or bare `maps`, which are broad enough to
>    swallow a real error.

> **D97(d) closed — and it was twice the item it was filed as.** The entry said "rooms are invisible
> to admin moderation". True, but the cause was broader: `AdminFlatmates` read `rawDb()` while
> **every** consumer flatmate flow writes localStorage, so ops could not see a single seeker, group
> **or** room a real user had posted — not just rooms. Worse, the second half was silent: nothing on
> the consumer side filtered `modStatus`, so *even the rows ops could see* were unmoderatable —
> "Remove" wrote a value no reader consulted and the post stayed on the board, while the page told
> the moderator that removed posts disappear.
>
> **The server was right the whole time**, which is what makes this a mock-fidelity bug rather than
> a product gap: `FlatmateRoomRepository`, `FlatmateGroupRepository` and `FlatmateSeekerPostRepository`
> filter `mod_status not in ('flagged','removed','rejected')` across nine queries, one commented
> *"the mod_status clause is not decoration: a flagged post must disappear"*. The mock now mirrors
> that set exactly (`MOD_HIDDEN` in `lib/data/flatmates.js`), the admin queue reads both stores and
> has a Rooms tab, and verdicts are written back to whichever store holds the row.
>
> **The filter went on the public board, not on the store getters.** `getRooms`/`getFlatmatePosts`/
> `getFlatmateGroups` have 31 call sites between them and the owner's own dashboard is one of them —
> an owner must still see a post that was taken down, with its status, rather than watch it silently
> vanish. Both halves are mutation-tested: drop the filter and the two "hidden" tests go red while
> the control stays green; revert the admin read and the visibility test goes red.
>
> **The lesson, which is bigger than flatmates:** a mock that is *more permissive* than the server it
> stands in for does not fail loudly — it silently passes tests the real thing would fail. Worth
> checking the other mock providers against their repositories for the same asymmetry.

**D36** (Spotless) is blocked on repo access, not on a decision. **D54** turned out to need more
machinery than its Low priority justifies — see its trigger. Everything else above the one-liners
now needs a product or infrastructure decision rather than an afternoon, which is the useful summary
of this list: **the backlog that could be worked around is worked; what is left is what was
deferred on purpose.**

`P` = rough priority. `T` = what unblocks it.

| # | Item | Area | P | Owner / Trigger |
|---|---|---|---|---|
| D4 | `CashfreePaymentGateway.createOrder` live HTTP | payments | Low | whoever obtains the merchant account |
| D7 | **Closed 2026-08-10.** Saved-search sweep now recomputes and persists `saved_searches.new_count` on a fixed cadence (`SavedSearchNewCountSweep` + `SavedSearchService.recomputeNewCounts`), guarded by `punenest.engagement.saved-search-sweep.enabled`; deterministic integration coverage in `SavedSearchNewCountSweepTest` | engagement | — | done in this wave; revisit only if counts move from in-process filtering to query-side aggregation |
| D11 | `role` / `status` as `String`, not enums | style | Low | — (deliberate, see §2) |
| D13 | **Scoped-staff `roleId` / `moduleAccess` do not exist server-side at all** — measured 2026-08-11, and the measurement is the finding: there is no `users.role_id` and no `module_access` column, no field on the entity, no property on any DTO, no JWT claim, and **no team-member management endpoint of any kind** (`grep -r 'team-members\|TeamMember' backend/src/main/java` → nothing). They are session fields the admin client invents at mock login. So there is nothing dead to delete here — the only backend-visible half of this item was `settings.customRoles`, now removed under D67. Scoping today is `users.team` + `settings.permissions`, which narrows and is enforced. Reopening this means building the slice, not finishing it, and it needs three answers first: should scoped back-office accounts exist server-side at all, or is per-team scoping the intended model? If custom roles return, must they be constrained to *narrow* the role baseline rather than widen it as the console currently does? And which server-side vocabulary does a "module" grant map onto — the existing `Capabilities`, or a route-level vocabulary that has to be designed? | admin | Med | product decision first (see `open-questions.md`), then a team-member slice — not a backend-only task |
| D15 | Notification preferences (R9) | engagement | Low | no table, no contract |
| D32 | ProfileTab identity chips are hardcoded English | i18n | Low | ProfileTab i18n pass |
| D33 | **~195 `@param` lines still restate their parameter in a synonym** — the 2026-08-07 pass amended `api-standards.md` §10 first (the durable half: without it they grow straight back) and deleted the **42** that are provably empty — pure name restatements and `(required)`/`nullable` suffixes that duplicate the annotation on the next line. **The remaining ~195 were deliberately not deleted.** The register's criterion was "≤4 words", and applied literally that removes genuinely useful lines: `@param orderId the provider's {@code order_id}` is four words and says which external field it maps to. A synonym (`@param title headline`) is a judgement call a regex should not make unsupervised. *Re-measured 2026-08-09: of the 670 remaining `@param` lines, **zero** are provably empty (name-restatement or bare `(required)`/`nullable`) — the durable pass took all of those. Every residual line carries units, an enum vocabulary, a default, a `{@link}` or a nullability semantic, so there is no risk-free bulk pass left; deletion stays per-line judgement per §10.* | quality | Low | by hand, per file, when that file is next edited — §10 now forbids adding more |
| D36 | **Closed 2026-08-10 — Checkstyle deleted, Spotless declared, whole-tree reformat deferred.** The reassessment resolved as "one tool, not two". *Measured, not assumed:* re-running the stored config on today's tree gave **1,307 violations / 437 main-source files** from **60 rule modules (54 distinct rules)**, and **93% came from two rules** — `ImportOrder` (748) and `SingleLineJavadoc` (468, which contradicts `api-standards.md` §10 / D33 and had to be deleted, not satisfied). The decisive measurement was running Checkstyle *again* over a Spotless-formatted copy of the same tree: violations went **up, 1,307 → 1,440**. `RightCurlyAlone` 6 → 141 and `Indentation` 8 → 51 because the formatter collapses `{\n}` to `{}` and uses its own continuation indent, while `ImportOrder` barely moved (748 → 733). The two tools do not complement each other, they **contradict** each other. After Spotless the residual genuinely-semantic findings are **25 across 4 rules** (`OverloadMethodsDeclarationOrder` 8, `OneTopLevelClass` 7, `VariableDeclarationUsageDistance` 5, `LocalVariableName` 2, `AvoidEscapedUnicodeCharacters` 3) — organisation and naming preferences, not one bug. Every actual bug-catcher in the config (`FallThrough`, `MissingSwitchDefault`, `EmptyCatchBlock`, `NoFinalizer`, `AvoidStarImport`, `NeedBraces`) already reported **zero**, and the only offline engine is **10.13.0**, which predates Java 22–25 syntax on a Java 25 project. `backend/config/checkstyle/` is therefore deleted (recoverable from git history); boundaries stay with `ArchitectureBoundaryTest`. **Still open, deliberately:** Spotless is declared in `backend/pom.xml` but bound to **no lifecycle phase** and the tree is **not yet reformatted** — see D68 | build | — | closed; the reformat itself is D68 |
| D68 | **The Spotless whole-tree reformat has not been applied** — `spotless:apply` over `backend/src` is **612 of 755 files, 12,411 changed lines** (6,609 insertions / 5,802 deletions), broad but shallow: median 8 changed lines per file, 227 files at ≤5 lines, only 66 above 50. It is not enormous, but it is enough to bury every code review for a week, and it must not be smuggled into a feature branch alongside logic. Land it as a **standalone commit that touches nothing else**, and in that same commit add the `<executions>` block binding `spotless:check` to `validate` — the gate and the tree it requires have to arrive together or the build breaks for everyone. *Why Palantir:* measured against the two google-java-format styles on scratch copies of the same tree — `palantirJavaFormat` (4-space/120) 612 files / 12,411 lines, `googleJavaFormat` AOSP (4-space/100) 716 files / 37,859 lines, `googleJavaFormat` GOOGLE (2-space/100) 753 files / 126,407 lines. The tree is already 4-space (leading-whitespace histogram peaks at 4/8/12/16/20/24, zero tabs) and already wraps near 110 (68 lines over 110 columns, 20 over 120), so Palantir is the style this code is *already written in*. Note the reformat also deletes unused imports, so it is not purely cosmetic and needs a compile | build | Med | a quiet moment with no other backend lane in flight |
| D37 | **Service-split trigger: past ~450 lines, split by use-case, never by layer** — services top out at 405 lines and are trending up, so agree the rule now while it is free and nobody is defending a specific file. A `RentService` that grows becomes `RentBillingService` + `RentPaymentService`, never `RentServiceHelper`: a helper class named after its parent is a file split, not a design | architecture | Low | agree now, apply on the next service that crosses it |
| D41 | Deleting a document leaves its object in the store — needs a bucket lifecycle rule | storage | Low | with the real object store |
| D44 | **Service requests are not team-scoped** — `service_requests` has no `team` column and `type` is free text, so every ops user sees every request while tickets are desk-scoped. Inferring a desk from `type` would silently hide work the day a new type appears | design | Med | when the service catalogue is a closed vocabulary (Billing & Growth slice) |
| D45 | **A ticket and a service request do not mirror each other** — the ops board and the customer's workflow are two tables with no link, so ops working a request has to find the ticket by hand | design | Med | with the Services & Support slice (`/support-tickets`) |
| D46 | ~~**`TicketUpdate` cannot unassign** — a record cannot distinguish an absent field from an explicit `null`, so `assigneeId: null` is read as "leave it". Needs a sentinel or a dedicated endpoint~~ **Closed 2026-08-10.** Sentinel rather than a second endpoint: `TicketUpdate.UNASSIGN = "none"` on the existing field, matching the vocabulary already in `VerificationStatuses.NONE` / `ContactStatuses.NONE` / `RecurringIntervals.NONE` rather than inventing a `JsonNullable` the codebase has nowhere else. Absent still means "leave it", a uuid still assigns, and anything else still 404s through the existing `NotFoundException`, so no caller had to change. `TicketUnassignTest` (4) + `TicketQueueTest` (19) green | design | Low | done |
| D49 | **`MessageCreate.attachments` is accepted and dropped** on both message surfaces, matching the `PropertyVerification` precedent. The wire field exists, the behaviour does not | contract | Low | when the frontend actually attaches files to a message |
| D184 | **The drafting desk's status filter speaks server vocabulary the mock store does not** — `/ops/drafting-desk` sends `?status=` using the API's values (`new`, `assigned`, `in-progress`, `draft-shared`, `approved`, `completed`, `cancelled`), which is correct against the server. In mock mode the rows come from `lib/serviceFlow.js`, whose statuses are the stepper's (`docs_review`, …), so most status filters return nothing. Deliberately not papered over with a translation table: a mapping that exists only to make a demo look right is a second vocabulary to keep in sync | frontend | Low | dies when the ops surface stops dual-running mock and live |
| D185 | ~~**Open Session In View was left at Boot's default (on)**, so any lazy association touched during response serialisation issued its query from the view layer — an N+1 that never appears in a service, never fails a test, and only shows up as latency~~ **Closed 2026-08-10.** `spring.jpa.open-in-view=false` in `application.properties`. Safe because no controller returns an entity: every handler maps to a DTO inside the service transaction, so nothing is left lazy by the time Jackson sees it. Chosen partly because being wrong is *loud* — a missed site throws `LazyInitializationException` rather than silently degrading. **The setting is repeated in `src/test/resources/application.properties`, and that repeat is the whole guarantee:** the test file *shadows* main rather than merging with it, so without the repeat the suite would have booted with OSIV on and proven nothing about what production runs. Full suite green with it off | performance | — | done in this wave |
| D186 | ~~**The catalogue's paged reads had no total order**, so a page boundary could repeat or skip a listing~~ **Closed 2026-08-10.** `PropertySort.DEFAULT` sorted on `createdAt` alone and `PropertySpecs.boostedFirst` on `(boost rank, createdAt)` — none of the four sortable columns is unique, so ties were resolved by whatever the planner found convenient, and it is free to decide differently between two executions of the same query. Unpaged that is untidy; **paged it is a correctness bug**, because page 1 and page 2 are separate queries: a row that sorts differently between them is returned twice or never, and the reader cannot tell. Both paths now append `id DESC` as a final tiebreaker (PK, so it can only decide ties, never reorder rows that genuinely differ). `id` is deliberately **not** added to the sort whitelist — a client may rely on it for stable paging but may not sort by it. Found because `BoostRankingTest.elapsedWindowNeitherRanksNorDiscloses` failed in a full-suite run: with the boost elapsed both rows ranked equally, leaving `created_at` as the only discriminator, and `@CreationTimestamp` resolves to the platform clock — coarse enough on Windows that two back-to-back inserts share an instant. **The test was asserting on a tie and calling it insertion order.** It now backdates `created_at` through `jdbc` so the ages are a fact rather than a hope; a setter was rejected (it would make a production creation time writable to suit a test) as was sleeping between inserts (same guess about granularity, paid for in wall-clock time) | design | — | done in this wave |
| D187 | ~~**The e2e suite had no app-readiness signal and was using `networkidle` as one**~~ **Closed 2026-08-10.** Vite fetches the module graph and only then evaluates it, so the last response lands well before any app code runs. Measured on a dev page load: last module fetched `+251ms`, `networkidle` resolved `+839ms` **against an empty document**, `main.jsx` executed `+1181ms`, first render `+1702ms`. Specs survived this only by coincidence — the mock store was written *synchronously* during module evaluation, so a `page.evaluate` queued at idle could not execute until the main thread was free, which was after the write. D129 moved the seed behind a dynamic `import()`, putting the write one hop past that boundary, and the coincidence stopped holding: specs reading the store straight after `goto` began seeing `null`. **The dangerous half was the specs spelling it `JSON.parse(getItem(KEY) \|\| '{}')` and writing the result back** — a read-modify-write whose "defensive" fallback overwrites the entire seeded database with `{}` plus one field, silently, surfacing many assertions later as a plausible-looking product bug. 16 such sites existed. Fixed with an explicit contract rather than per-spec waits: `main.jsx` sets `data-pn-boot="ready"` on `<html>` once the seed **and** the one-shot migrations are done, and `e2e/helpers/app.js` exports `appReady(page)`. Probing `localStorage['puneNestDB_v5']` was rejected as the signal — the dev-only disk hydration writes that key *before* the migrations run, so it can be present but stale. 13 exposed call sites converted, every destructive fallback turned into a throw, pure reads left alone. Also fixed the two independent boot fetches being serialised, which is what opened the idle window in the first place | test | — | done in this wave |
| D190 | **The "Ownership Verified" badge is unearnable — it is set by nothing outside dev seed data.** `Property.ownershipVerified` is declared (V-era column `ownership_verified`), serialised on `PropertyResponse`, rendered as a badge, and **assigned in exactly one place in the repository: `R__zz_dev_demo_data.sql`.** There is no setter, no service write, no admin action — grep `setOwnershipVerified\|ownershipVerified =` returns only the field declaration. So in production the badge is permanently `false`, while `home.json` promises in **all three locales** that "ownership [is verified] through the Index II document (Ownership Verified)". Q15 (closed 2026-08-11) sets the required evidence: ownership proof (tax receipt / electricity bill / Index II), owner identity (Aadhaar or PAN), physical existence (timestamped photos or video walkthrough). RERA cross-check considered and **not** selected. **Much of the machinery already exists and is not wired to the flag:** `PropertyVerificationService` already carries per-deal checklists (`RENT_CHECKLIST` = Index II / electricity bill / Aadhaar; `BUY_CHECKLIST` = 6 items incl. "Ownership proof (Sale deed / Index II)"), `ReviewChecklistItem` persists per-item state, and the listing wizard already collects per-type ownership documents (7/12 Extract for land vs Index II elsewhere, `types.spec.js:236`). So this is mostly *connecting* a completed checklist to the badge — plus the genuinely missing half: **expiry**, since a 2019 tax receipt proves nothing today, which makes verification a checklist with state rather than a boolean. **This is the load-bearing half of the trust model** — ADR-019's badge-not-gate thesis rests entirely on the badge meaning something, and D101 already showed what happens when it does not (fabricated `registration: true` produced "Society Verified" for buildings nobody had confirmed exist) | design | Med | before any surface advertises verification as a reason to trust a listing — the copy already does |
| D191 | **Referral credits are minted off raw counters with no distinctness check** — `pnReferralStats` counts `invited` / `joined` / `listed` and issues credits from those numbers, so one person with several SIMs can mint them. Q17 (closed 2026-08-11) sets the rule: **credit the referrer when the referee's first listing passes verification**, i.e. clears D190's document gate. Deliberately *not* verified-mobile (a SIM costs less than the credit it mints), *not* referee KYC (too much friction on the exact surface being used to buy liquidity), *not* manual review (does not scale, and delays the reward that makes referrals work). **This is why D190 comes first:** the document gate does the anti-fraud work twice, so no separate fraud machinery is needed. Sharpened by Q16 — referral credits offset the owner paywall during the liquidity build, so a fake referral costs listing quota rather than cash, but buys supply-side spam capacity, which is the one thing the trust model exists to keep out | security | Med | with D190, or sooner if referral volume rises before the gate lands |
| D192 | **There is one flat admin role** — every moderator sees every listing, every user and every payment. Q18 (closed 2026-08-11) chose team scoping **with admin-defined per-user permission sets**: the admin creates an ops user and picks that user's permissions directly, rather than selecting a fixed bundle. This is what `settings.customRoles` was reaching for, and D67 records why the client-side version was removed rather than wired — it composed by **union** where `PermissionMap` may only narrow, so honouring it was privilege escalation. Rebuilding it needs, in order: (1) a **permission catalogue** derived from `PermissionMap` so a grant can never exceed what the server already enforces; (2) per-user grant persistence — `roleId` and `moduleAccess` exist nowhere server-side, no column, no claim, no endpoint (D13's slice); (3) the grants reaching the request as a **verified claim**, not as anything the browser asserts; (4) admin UI following the existing console patterns; (5) **enforcement stays server-side** — the UI hides what a user cannot do, the server *refuses* it | security | Med | when ops headcount exceeds the point where everyone is trusted with everything |
| D193 | **Every unmapped path under `/api` answers 500, not 404 — so a typo'd route is reported to the client and to monitoring as a server fault.** Found 2026-08-11 while probing the live API for Lane B: `GET /api/me` with a valid bearer token returns `{"error":"internal","status":500}`. `GlobalExceptionHandler` (~L250) has an `@ExceptionHandler(Exception.class)` catch-all and **no** handler for `NoResourceFoundException`/`NoHandlerFoundException`, so Spring's "no such route" signal falls into the generic bucket. It hides in ordinary use because unauthenticated requests hit the security filter first and get a 401 — you only see it once you are holding a token, which is exactly when a client integrator is exploring the surface. Two costs: it inflates error-rate alerting with non-errors, and it tells a caller "we broke" when the truth is "that route does not exist", which sends them debugging the wrong system. **This is the same category of lie as the bug Lane B was written to catch** — the property-review 404 that rendered as a friendly empty state. Fix is a handler that maps the no-route exceptions to 404 in the standard error envelope, plus a test asserting a nonsense authenticated path is 404 and not 500 | contract | Med | before any third party integrates against the API, and before error-rate alerting is trusted |
| D194 | **A real tenant is never offered the review control against the live API** — `ReviewsSection.jsx` decides `hasTenancy` by calling `getTenanciesFor(myMobile())`, which reads the **mock store**. Against the live API that store is empty, so the check is unconditionally false and the write path is dead in http mode: the section renders, the reads work, and the button to leave a review simply never appears for the one person most entitled to. It passes every mock-mode test because in mock mode the store is the source of truth for both halves. Found 2026-08-11 during Lane B, which had to mint its fixture review **through the API** partly because of this. The visit-based path is not affected — the server derives `context: "visit"` correctly — so this is specifically the tenancy door. Needs a real answer to *how tenancy is proven live* (an authenticated read, presumably off the same seam `/me/tenancies` serves) rather than a client-side lookup; that is a product decision as much as a wiring one, which is why it is recorded rather than patched | frontend | Med | when tenant reviews are expected to work outside mock mode |
| D195 | **A hard-coded `4.2` is rendered as a real star rating for societies that have no reviews** — `frontend/src/pages/consumer/property/SocietySection.jsx:60` supplies a literal fallback where the aggregate should be, so the society block on a listing page shows a confident, specific, invented score for a building nobody has rated. **This is the D101 pattern exactly** (fabricated `registration: true` produced "Society Verified" for buildings nobody had confirmed exist), and it is the same lie in a more quantitative costume: a reader has no way to tell 4.2-because-people-said-so from 4.2-because-a-developer-typed-it. Found 2026-08-11 while moving the society **directory** onto the review seam; it was used verbatim as that work's mutation-test payload, so the directory now *rejects* this value while this page still ships it. The server already refuses to fabricate — `SocietyResponse` returns `avgRating: null` for an unrated society and its docblock says "no rating is not a rating of zero", and `SocietyRatingService` leaves unrated societies absent from the grouped-SQL map rather than defaulting them. So the fix is to stop the client undoing that: read the one society's aggregate from `reviewService.getEntityReviewSummary('society', slug)` (one request, already live, already how the society hub does it) and render "not rated yet" when it is null. The reason it was not fixed in the same pass is that deleting the `4.2` is a visible behaviour change on a page that work was not scoped to touch | design | Med | before any surface uses a society rating as a reason to trust or choose a building |
| D196 | **The home page's society tiebreak is computed from a store that is empty in http mode, so it silently does nothing** — `frontend/src/pages/consumer/home/SocietiesSection.jsx:56` computes a rating that **nothing renders**; it exists solely as a third-tier sort tiebreak, and it is derived from `entityRating`, which reduces a `localStorage` bucket a live session never writes. Live, it is permanently zero for every society, so the tiebreak is inert and the ordering it was meant to produce never happens. Harmless today precisely because it is invisible — which is also why it will stay broken indefinitely without a note. Two honest options: read the aggregate (rejected during the directory work as a bad trade — it would pay a 4-page / 348-row walk on the **home page** to break ties among eight cards), or delete the tiebreak. **Whether that ordering should exist at all is a product call**, which is why this is recorded rather than patched. Related: `entityRating` (`frontend/src/lib/store/reviews.js`, re-exported `store.js:15`) cannot be retired until this and D195 are resolved — they are its last two call sites | frontend | Low | when the home page's society ordering is next questioned, or when `entityRating` is retired |
| D197 | **The society rating shown to a reader is a blend of real resident averages and an invented baseline, and the blend is never disclosed.** `useSocietyHub.js:229-237` computes both the five aspect bars and the headline `overall` as `(realAverage + baselineBars(soc)) / 2` whenever `showEstimate` is true — and `showEstimate` is `!soc._thin && !soc._community`, i.e. **true for every curated society**, which is essentially the whole directory (348 seeded rows, `source: "curated"`). `baselineBars` (`helpers.jsx:59`) is a pure function of `occupancy + year`, clamped to 3.4–4.9, so it always produces a plausible-looking score and never a suspicious one. Two distinct problems, and the second is the serious one: **(1)** an *unrated* society renders five confident aspect scores nobody gave — the D101/D195 pattern again; **(2)** a *rated* society has its residents' actual average **diluted 50/50 by the fabricated number, while being labelled `(N)` as though N residents had said it.** One resident rating 2.0 on a society whose baseline computes to 4.1 displays **3.1 "(1)"**. The reader reasonably believes one person said 3.1. That is not an estimate presented as an estimate; it is real data quietly averaged with fiction. Disclosure is **partial and inconsistent**: the hero (`Society.jsx:90`) does render `society.communityEstimate` = "(community estimate)" — but **only when `rating.count === 0`**, never on the blended path; and the Reviews tab's aggregate (`ReviewsTab.jsx:26,33` — the `{overall}/5` figure and all five bars) carries **no estimate label in either branch**. Note this survived the 2026-08-11 vocabulary fix: that work correctly made a bar with a real number behind it a real resident average, but the blend sits *downstream* of it. **Whether a cold-start estimate should exist at all is a product decision** (it is plainly deliberate — a directory of "not rated yet" is a bad first impression while liquidity is being built, which is exactly Q16's phase), so this is recorded rather than patched. The engineering half is not in doubt either way: if the estimate stays, it must be labelled **everywhere** it appears and must never be blended into a count-labelled figure; if it goes, `baselineBars` and `showEstimate` go with it | design | Med | before a society rating is cited anywhere as evidence, and before the directory is marketed on resident ratings |
| D199 | ~~**A 44px tap-target assertion failed on a measurement artifact, not a regression** — `e2e/tests/mobile/ops-field.spec.js` compared `boundingBox().height` against exactly `44` and, under full-suite load only, Chromium returned `43.99993896484375` (= 44 − 2⁻¹⁴) for a control whose CSS floor is `min-height: 44px`~~ **Closed 2026-08-11.** It was the sole hard failure in an otherwise clean 1668-pass run and it named a *different* button on the retry (`Add a note to Owner Aadhaar`, then `View Owner Aadhaar`), which is the tell: a real layout regression is deterministic about which control it breaks. Confirmed by re-running the file with `--repeat-each=3` on the `mobile` project — 18/18 green. Fixed by adopting the tolerance this repo had already chosen for exactly this problem in `phase3.spec.js:48` (`MIN_TAP - 0.5`), lifted into a named `TAP_EPSILON` with the reasoning written next to it rather than left as a bare magic number. Half a pixel cannot mask the regression the file guards, which is 26px icon buttons — proven, not assumed: stripping `tap-target` off the note button in `OpsServiceQueue.jsx:351` still fails the assertion, and the mutation was reverted and the file re-run green across both `mobile` and `mobile-small` | e2e | — | done |
| D198 | **The property review composer's star buttons have no accessible name at all — thirty identical unlabelled buttons in one dialog.** `frontend/src/pages/consumer/property/StarInput.jsx:7` renders `<button type="button">` wrapping only an `<Icon name="star">`; there is no `aria-label`, no visually-hidden text, and no `title`, so the accessible name computes to empty. `ReviewModal.jsx` mounts it six times (once for the overall rating at L50, once per `RV_CATS` aspect at L58), so a screen-reader user opening "Rate this property" hears "button" thirty times with nothing to distinguish 1-star-for-Locality from 5-star-for-Accuracy. The visible `<span>` naming each aspect is a **sibling**, not a label — it is never associated, so even the row context is unavailable. This is a **WCAG 2.1 4.1.2 (Name, Role, Value)** failure on the only control that writes user-generated content on the listing page, and it is pre-existing, not introduced. It is recorded now because 2026-08-11's per-target review vocabulary made it **asymmetric**: the society composer built in that pass labels every star `aria-label="{n} star for {aspect}"` and its e2e spec asserts an exhaustive `ASPECTS.length * 5` count, while the property composer it was modelled on still labels nothing — so the repo now demonstrates the right pattern one directory away from the wrong one. The fix is small and has a worked precedent to copy (including the trap that precedent hit: `getByRole({ name })` matches by **substring** unless `exact: true`, so adding these labels will collide with any existing `name: 'N star'` query — grep `\d star` across `e2e/**` before and after). Not fixed in that pass because the modal was out of its scope and the change is not assertion-free | design | Med | with the next accessibility pass, or the moment the property composer is touched for any other reason |
| D52 | **The frontend models a conversation `state`** (`active`/`incoming`/`pending`) in `frontend/src/lib/chat.js` that the contract has no field for. The contract wins; the UI will need either a derived value or a spec change | contract | Low | when the React client is wired to the real API |
| D53 | **A conversation is not moderatable** — participants only, staff and admin included, so a reported chat cannot be read by anyone. Deliberate (a role check hidden inside the participant guard is how private surfaces quietly stop being private), but moderation will need its own audited endpoint | design | Low | when abuse reporting covers messages |
| D54 | ~~**Find-or-create on a conversation races to a 409** — two simultaneous first messages produce one row and one unique-index violation. The loser still does not get handed the existing thread, it just gets a truthful status~~ **Closed 2026-08-10.** The register's own warning is what shaped the fix: the retry happens *outside* the doomed transaction, not inside it. `ConversationService.start` now wraps the whole attempt in a `TransactionTemplate` (the pattern `BoostService`/`SubscriptionService` already use), and on a violation that `ConstraintViolations.isOn` attributes to `uq_conversations_pair_property`/`uq_conversations_pair_general` it re-runs the same call in a second transaction, where the probe finds the winner's row and returns `created=false`. Anything else rethrows untouched. Guarded on `isActualTransactionActive()`: under an ambient transaction the template would *join* rather than open, and the retry would re-enter the doomed context and surface as a 500 — worse than the 409 it replaces — so that case degrades to the old truthful 409 instead. `ConversationStartRaceTest` covers both doors (one caller double-tapping, both parties at once) and asserts against a `racesRetried()` counter, because every other assertion is equally satisfied by an accidental serialisation and would have let the test quietly stop testing this. 21 tests green with `ConversationEndpointsTest` | reliability | Low | done |
| D55 | **`sameDevice` and `sameIp` on a referral are always false** — no device fingerprint is captured anywhere and the request IP is not recorded, so the two strongest self-referral signals are absent from the fraud desk. Reported as `false` rather than omitted because the contract requires the fields; an absent signal is safer than a fabricated one, but the desk is working blind on exactly the fraud it exists to catch | design | Med | when request-IP capture lands (needs a proxy-header policy first) |
| D56 | **The `qualified` referral status is never produced** — a referral goes `pending` → `rewarded`/`rejected`. `qualified` was meant to mean "the invitee did something real", but nothing tracks invitee activation, so staff approve on judgement alone | design | Med | when there is an activation event worth gating on (first listing, first lead, first payment) |
| D60 | **`Referral.channel` is derived from the referrer's role, not from how the link was shared** — there is no share-channel parameter on redeem, so the field describes the wrong dimension. Harmless until someone builds a report on it | contract | Low | when redemption carries a share context |
| D61 | **Referral farming is caught by humans, not by code** — one person with several mobile numbers can redeem repeatedly; only `uq_referrals_referred_mobile` and the staff fraud desk stand in the way. Deliberate: automated velocity blocks would reject genuine roommates and flatmates, which is the platform's most common referral. Revisit only with D55's signals in hand | design | Med | when the reward budget makes manual review too slow |
| D63 | **`payoutsCompleted` and `refunds` on `/admin/finance` are structurally zero** — no payout and no refund path exists anywhere on the platform (`payout_accounts` stores a destination and nothing writes a remittance). Reported rather than omitted so the figures move the day payouts ship instead of appearing from nowhere, but a finance screen showing four numbers of which two can never be non-zero invites the reader to trust all four equally | design | Med | with the payout execution slice |
| D64 | **Closed 2026-08-10.** `boosts.paid_at` (V60) is now stamped only by `Boost.activate()` (the payment-webhook path); free packs and any future comp/grant activations use `Boost.activateComp()`, which leaves it null. Revenue queries in `AdminMetricsRepository` switched from `starts_at is not null` to `paid_at is not null`, so a manual-grant path cannot inflate reported revenue | reliability | — | done; backfilled from `starts_at` for existing rows |
| D65 | **Service orders are excluded from platform revenue** — `service_orders.amount` is a quote, not a receipt, and the marketplace takes no money through the gateway, so counting it would report revenue the platform has not received. The consequence is that `/admin/finance` under-reports the moment the marketplace does start collecting | design | Med | when a service order has a payment path (see D58) |
| D67 | **`settings.permissions` is enforced now; `settings.customRoles` is still read by nothing** — the map ceased to be inert on 2026-08-11: `PermissionMap` (`@Component("permissions")`) resolves the stored document against the signature-verified principal only, and capability checks are **`and`-ed onto** the existing `@PreAuthorize` role guard, never substituted for one, so the map can only *narrow*. It fails safe in three states — no row, a row that is not a JSON object, and staff with `team == null` (legal per V2) — all fall back to the role baseline; once the document is well-formed and the caller has a key, an absent key denies, which is what makes an admin's edit actually remove access. `R__seed_permission_map.sql` seeds a complete, behaviour-identical map so the deny-on-omission cliff is never hit in practice. What remains: (a) ~~`customRoles` is keyed by `roleId` against a per-user column the JWT does not carry — that is D13's slice~~ **Closed 2026-08-11 by removal, not by wiring.** Three things had to exist before the stored document could enforce anything, and two of them are product decisions: there is no key to resolve a bundle against (no `users.role_id`, no claim, no team-member endpoint — so a bundle could only be selected by something the client sends, which is an allow-list the client opts out of); there is no mapping from its module vocabulary (`enquiries`, `properties:verify`, from the client's `lib/adminModules.js`) onto the server's `Capabilities`, and inventing one is writing policy nobody agreed; and it composes by **union** (`BASE ∪ role-bundle ∪ moduleAccess`) where the permission map may only **narrow** — honouring a widening document is the precise privilege escalation the narrow-only rule exists to prevent. So: `V61` deletes the row (a loaded allow-list left for a future commit to discover is worse than one that is enforced or one that is gone), `AdminSettingsService.UNSUPPORTED_KEYS` answers **422** rather than dropping it silently (a silent drop still returns 200 and still lets an operator believe a decorative control granted something), and it is off the `AdminSettings` schema. `AdminSettingsDeadKeyTest` (7) proves the refusal is whole-request, unaudited, and does not extend to ordinary writes. **The admin console still composes the key client-side**, so the frontend lane must stop sending it before the settings screen is wired to HTTP — today nothing reaches the server, because there is no `services/` seam entry for `/admin/settings` at all; (b) `export_csv` is stored and guards nothing, because CSV/PDF render client-side and there is no export endpoint to guard; (c) team-less staff keep the role baseline, so per-account scoping is still open. **Client half closed 2026-08-10:** the removal was server-side only, which left the mock composing `customRoles` *inside* its settings document — so the mock and the contract disagreed, and any full-document save would have earned a 422. It now lives in its own top-level `customRoles` collection (`mockApi/team.js`, with a one-time migration for browsers holding a `v5` DB that still nests it), out of `db.settings`, out of the derived `data/settings.json`, and `AdminFlagsContext` reads it through `getCustomRoles()` rather than off the settings blob. The editor itself was **labelled, not deleted**: it does real work — it scopes what this console renders, via `effectiveModuleKeys` and the `ModuleRoute` guards — and Q13 has not decided whether scoped back-office accounts should exist server-side, so deleting the UI would answer an open question by attrition. The Custom roles tab now carries a notice saying access is still decided by role and team, and that these bundles tidy navigation rather than restrict what anyone can reach | security | Med | a permissions editor over `view_dashboard` / `view_service_requests` / `update_ticket` is now honest; one that also renders `export_csv` would still be lying to the admin, and one that renders custom roles now fails loudly (422) instead of quietly, and says so on the tab |
| D69 | **Closed 2026-08-10.** `AdminMetricsService.series(...)` now has a 30s in-process TTL cache keyed by metric/range/window, cutting repeated dashboard reads from immediate re-scan to cache hits; behavior locked by `AdminMetricsServiceCacheTest`. **Two defects found later the same day when the full suite was finally re-run, both shipped by the original change:** (1) the cache broke read-after-write and took `AdminMetricsEndpointsTest` down with it — those two tests guard IST bucketing and the Monday-bucket bug, not cache timing, so the TTL is now `punenest.metrics.series-cache-ttl-ms` and the test profile sets it to `0`; (2) the key contains the caller's date range and expired entries were only evicted on a repeat lookup, so any staff account could grow the map without bound by walking `from` — now capped at 64 entries with a prune-then-clear on pressure, proven by `theCacheDoesNotGrowWithoutBound`. The class javadoc still claimed "nothing is cached"; corrected | performance | — | done in this wave; revisit if multi-node consistency or shared-cache invalidation becomes required |
| D70 | **A poster's only record of who answered is the notification stream** — the contract has no "who replied to my ad" endpoint and `share_flat_interests` is not readable over the API, so the sender's name and number exist for the poster only as a notification. Dismiss it and the lead is gone, with the row still sitting in the table | design | Med | when share-flat gets a second screen — needs a spec addition, so it could not be fixed inside the slice |
| D76 | **The owner-facing edit warning contradicts the server on what triggers re-moderation** — the dormant mirror `LISTING_FOUNDATION_FIELDS` in `lib/store/listings.js` was corrected 2026-08-10 to the server's seven searchable facets (`deal, locality, bhk, type, price, furnishing, construction`, per `ListingFoundationTest`), and its comment now cites that source — but the array has **no live consumer**. The warning the owner actually sees runs through `list-property/editPolicy.js` `classifyChanges`, whose Tier A/B model says the *opposite* of the server: `price`/`furnishing` sit in Tier B ("goes live instantly") while the server reverts both to `pending`, and `floor`/`facing`/`age`/`carpetArea` sit in Tier A ("re-check") which the server does not revert. So the live UI still tells the owner a price edit goes live instantly when it pulls the listing offline — the original defect, in a different file. **Closed 2026-08-10 — by splitting it.** The two halves were not the same kind of question. *Which* fields cost re-moderation is a fact with exactly one answer, the server's, so it is now derived rather than restated: `editPolicy.js` carries `FOUNDATION_FORM_KEYS`, the seven wire fields of `ListingService.apply` mapped onto wizard form keys (`price` maps to two, since the wizard splits sale price from monthly rent), `classifyChanges` returns `remoderation`/`recheck`/`instant` cutting across the tiers, and the banner counts from those — so a price or furnishing edit is now reported as a re-check that takes the listing off search rather than as "goes live now", and the timeline says *Under review — off search* instead of reassuring the owner nothing goes down. `tierA`/`tierB` are returned unchanged because the edit throttle (`recentMaterialEdits`) and the paywall (`IDENTITY_FIELDS`) read them; this is a reporting fix, not a change to either. *Whether* the server should revert at all rather than flag-and-stay-live is the actual product call and is **not** decided here — it is Q14 in `open-questions.md`. The anti-drift half is `npm run check:listing` (`frontend/scripts/check-listing-foundation.mjs`, wired into `check`): it parses the foundation block out of `ListingService.apply`, cross-checks it against `ListingFoundationTest#foundationCases` — an independent oracle, derived from `PropertyController.search`'s facets rather than from `apply`, so agreement is evidence and not a tautology — pins that `update` reverts while `updateAsModerator` deliberately does not, translates both client lists onto the wire set through the one table where the three vocabularies meet, and then runs `classifyChanges` over a real edit for every field. That last assertion is the one with teeth: three lists agreeing while the banner reads a fourth array is precisely what the first fix left behind. **Reopened and re-closed 2026-08-11, when Q14 was answered.** The 2026-08-10 fix made the banner truthful about a rule that was itself too blunt: one flag, seven fields, one outcome. Q14 split it. `apply` now returns an `EditImpact` record rather than a boolean, and the seven fields are two blocks with a comment banner each: `bhk`/`propertyType`/`locality`/`deal` set `remoderationRequired` and still revert (they change *what the listing fundamentally is*, so a stale index entry is a wrong answer — a 2BHK under 3BHK, a rental under sale); `price`/`furnishing`/`possession` set `recheckOnly` and now stay approved and searchable while `Property.requestRecheck(fields)` files the work item (they change *an attribute of a listing that is still the same property*, so the worst case is a briefly stale number on a listing that is genuinely what it claims to be). Remoderation supersedes recheck when one PATCH trips both. The work item needed a home, because the existing one *was* the `pending` status and `flagged` also removes the listing from search: `V62` adds `properties.recheck_requested_at` + `recheck_reason`, deliberately shaped like the existing `flag_reason`-beside-`status` pair, with a partial index on the pending rows; the timestamp is set once and not refreshed by later edits so queue age stays honest, while the reason string accumulates field names. `PropertyResponse` carries `recheckPending`/`recheckReason` (otherwise the two outcomes are indistinguishable to any client) and `GET /admin/properties` gained a tri-state `recheck` filter modelled on the existing `archived` axis. Clearing is `PATCH /properties/{id}/status` with `approved` on an already-approved listing — no new route, so the `SpecCoverageTest` floor is unchanged. Client side, `FOUNDATION_FORM_KEYS` split into `FOUNDATION_OFF_SEARCH_KEYS` + `FOUNDATION_STAYS_LIVE_KEYS`, `classifyChanges` grew a `staysLive` bucket, and the banner shows a green *Live — being re-checked* pill and a "stays live and searchable" note instead of the amber off-search one. The gate grew accordingly: it now parses both flags out of `apply`, asserts the two sets are **disjoint** (a field in both would make the outcome depend on which flag `update` tests first), and compares each half separately against `ListingFoundationTest#OFF_SEARCH`/`#STAYS_LIVE` and against each client map — 45 assertions to 61, because a field silently moving *between* the sets is a new way to drift that the union-only check could not see | frontend | — | done; Q14 closed 2026-08-11. Remaining: ~~the `recheck=true` moderation queue filter has **no admin UI**~~ **Closed 2026-08-11 the same day, because it had to be** — a stays-live re-check that no screen surfaces is not a rebalanced fraud control, it is a loosened one: the listing keeps earning while it waits for a review no human is ever shown. `/admin/properties` now carries a third queue tab, **Re-check Queue**, alongside Verification and Flagged: its own fetch on `?recheck=true` (not a client-side narrowing of the page's `all` — the endpoint pages at 20 and a re-check is by definition an *approved, un-archived* listing, so a partial page would present a backlog as drained), sorted oldest-first and not re-sortable, showing per row which fields changed (`recheckReason`) and how long the listing has been live-but-unreviewed (`recheckRequestedAt`, escalating sky→amber→rose at 24h/72h). The count rides in the tab label and a KPI card so a backlog announces itself without a click. Both outcomes reuse existing transitions and add no route: pass is `PATCH /properties/{id}/status` `approved` on an already-approved listing (`setStatus` clears the re-check unconditionally), fail is the same call with `rejected` plus a mandatory reason. Two wire gaps found and closed on the way: `recheckRequestedAt` was on the entity and the index but **not on `PropertyResponse`**, so the age — the one thing the screen exists to show — never left the server; and `toModerationQuery` never forwarded `recheck`, so the tested server axis was unreachable from the client. The mock modelled none of the three fields, which would have made every e2e assertion vacuous against a permanently empty tab. `listingFoundationChanged`/`revertListingForReview` in `lib/store/listings.js` still have no caller (harmless, and gate-pinned) |
| D79 | ~~**`GET /properties/{propId}/reviews` cannot be paged while the client computes the rating summary** — ruling D8.6 permits the unpaged read because the per-target UNIQUE index bounds it, and the property page computes the star average, the 1–5 distribution and the per-category averages from the full list~~ **Closed 2026-08-10.** The aggregates moved server-side: `GET /properties/{propId}/reviews/summary` (public, `getReviewSummary`) returns `ReviewSummaryResponse` — `avgRating`, `total`, a `ReviewRatingTally` and `ReviewCategoryAverage[]` — in two round trips. The 1–5 tally is JPQL with the empty buckets zero-filled in Java, because `GROUP BY rating` simply omits a star nobody awarded and the histogram would then draw four bars; the per-category averages are native (`jsonb_each` + `avg`) since the categories are jsonb keys, not columns. `avgRating` is **null**, not 0, when there are no reviews — 0 is a legitimate score and "unrated" is not a score. Fixed in passing: `ReviewService.withAuthors` called `names.get(null)` against an immutable `Map.of()`, so any property whose reviews were all anonymous (`author_id IS NULL`) returned 500. Unknown property 404s. 11 tests in `PropertyReviewSummaryTest`, spec parity green. **Client wired the same day, and the wiring is where the interesting failures were.** The seam gained `getPropertyReviewSummary` on both providers and `ReviewsSection` now reads it instead of reducing the list. Four defects surfaced only once someone looked at the wiring rather than the endpoint: (1) the HTTP provider's property routes pointed at `/reviews/property/{id}`, which matches `/reviews/{entityType}/{entityId}` where `entityType ∈ society\|locality\|owner`, so `ReviewTargetKey.resolve` threw and **every live property-review read and write had been 404ing silently** — the page catches a failed read and renders "no reviews yet", so a total outage presented as an empty state on every listing, and it survived because the only live review coverage probes a *locality*; (2) the page chose its empty panel on `!summary.count` while every review card lived in the other branch, so a failed summary read hid reviews that had loaded perfectly well — split into `hasAggregate` (draw the stars) vs `list.length` (draw the cards), which is the honest reading of two reads that can disagree; (3) the eligibility gate compared `v.propertyId` (a UUID) against `p.id` (the slug, via `propertyMapper`), so `myVisit` never matched and **nobody could rate anything live** — the button told visitors with a completed visit to go book one; (4) mock and live disagreed on per-aspect row order (server `order by c.key` is alphabetical, mock inserts in product order), so the suite and every screenshot proved a layout production does not render — now ordered in the mapper, which also removed the third copy of the category key list. Also hardened: the summary and list endpoints applied no visibility floor, letting anyone holding a UUID distinguish a rejected or archived listing from a nonexistent one — both now go through `PropertyStatus.DIRECTLY_REACHABLE`. A **security LOW stands as a constraint**: at n=1 the aggregate is de-anonymising, harmless only because `GET .../reviews` is itself public — if that read is ever gated or paged, the summary silently becomes an unauthenticated read of individual review content | design | Low | done — paging the list is now a free choice, since no visible number depends on the full read |
| D80 | **`FlatmateRoomDto` is 47 fields, by a distance the largest DTO on the platform** — the next largest is `PropertySummary` at 22. It is returned by the room feed, the mixed flatmates feed and now `GET /properties/{id}/rooms`. Not wrong today (rooms per flat is naturally small, and the derived occupancy fields are the point), but a 47-field row is a DTO that has stopped being a view of anything and become the entity with a different name | design | Low | if the flatmates payload ever needs trimming — split the feed shape from the detail shape |
| D84 | **The alert switch is a boolean over a four-state server field** — `SavedSearch.alertFrequency` is `off\|instant\|daily\|weekly`, but the only control is an on/off `Switch`, so the seam derives `alerts = alertFrequency !== 'off'` and writes back `daily` when switched on. Currently unreachable loss (nothing can produce a non-default cadence), but the moment a frequency picker ships, a user holding `instant` who toggles off and on lands on `daily`. The seam carries `alertFrequency` explicitly so the fix is a UI change, not a contract change | design | Low | when a cadence picker is designed |
| D94 | **Notification preferences have no server surface at all** — `getNotifPrefs`/`setNotifPrefs`/`inQuietHours` cover email/sms/whatsapp channels, a master `matchAlerts` switch, quiet hours and language, and every one of them lives only in localStorage. `ProfileTab.jsx` was deliberately not touched by the notification slice for that reason. The practical consequence is that quiet hours suppress *client-derived* alerts only — a server-written notification arrives at 3am regardless, because the server has never been told | design | Low | needs `GET/PUT /me/notification-preferences`; pair it with the D92 writers, which are what would have to honour it |
| D98 | **Two mobile items blocked on a design/product call, not on engineering** — (a) at 200% font scale the raised centre "Post" slot cannot fit a 56px circle plus a 24px label in a 56px bar (needs ~74px), so that one label squeezes; the fix is to drop the redundant text under an already-`aria-label`led FAB, which is a design decision. (b) "B7" bottom-nav density: 7 targets and 265px of painted control in a 360px row, with the account pill ending at exactly x=360 — real and measured, but the item's suggested escape hatch does not exist, so it needs a call on what leaves the bar | design | Low | needs a design/product decision before any code |
| D99 | **Swipe-to-remove on Saved deferred** — *narrowed 2026-08-08: the sheet half already shipped.* `useSwipeDismiss` is wired into `Select.jsx` ("drag the grab handle down to dismiss"), so sheets are done; the register was still recording them as deferred. What remains is the **destructive** gesture: removing a shortlisted property by swipe. That needs an undo affordance to be safe, which is real state machinery for a P2 win, so it stays deferred deliberately | design | Low | only alongside an undo affordance |
| D120 | **Service-request documents are multipart to the vault and do not resolve in dev** — draft and final documents are `multipart/form-data` returning **signed URLs** that a dev backend does not serve, and there is no customer upload surface behind the tracker (sharing a draft and uploading a final are staff transitions). The seam projects `draft`/`finalDoc` from `documents[]` by category for when they exist, but a customer-created request carries neither, and the per-request **document checklist** (six named items in the mock) has no read representation on the wire. So the tracker's document column and inline `draft.dataUrl` preview are mock-only; live mode shows the thread and the status, not the paperwork | design | Med | a dev-resolvable document surface (or a checklist read endpoint) before the tracker's document UI can flip to http |
| D121 | **Co-fill service requests have no counterparty endpoint** — the mock splits a rent agreement across two mobiles (`serviceFlow.createCoFill`, `listForParty`), keyed on the *other* party's `ownerMobile`; the customer API scopes every request to its requester, so there is no party view to fetch. `listPartyServiceRequests` returns `[]` in http mode (never `undefined` — the tracker spreads it) and `useRentAgreement.js` create stays on `serviceFlow.create` because its create is a co-fill. Also here: `markServiceRequestRead` is a no-op (no read-receipt endpoint) and `changes_requested` collapses server-side to `in-progress` (the maker-checker has `approve`/`reject` only), so a rejection is unrecoverable from the read shape | design | Med | a co-fill invite endpoint (and a distinct `changes_requested` state) if the two-sided rent-agreement flow ships live |
| D123 | **The document seam is the owner's side only — the buyer's half has no faithful server mapping** — `documentService.js` covers the vault (list/upload/delete a listing's files) and the owner's request inbox (list/respond, `/me/documents/{propId}` and `/me/documents/requests`). The buyer's half cannot follow: the server is **token-mediated** (`POST /documents/requests` then `GET /documents/shared?token=`), giving a buyer no way to poll a request's status, so `DocumentsSection`/`ViewDocuments` keep reading `lib/data/documents.js` cross-user localStorage directly (never through the service). Also staying on `lib/`: the cross-user grant notification (`notifyBuyerDocsGranted`), the shared-doc count (`countSharedDocs`), the owner dashboard's per-listing doc-count badge, **rent agreements** (they overlap the already-live tenancy flow — created as a side effect in `lib/data/tenancy.js`), and every presentation helper (`DOC_CATEGORIES`, `DOC_INFO`, `docInfo`, `formatSize`, `docIcon`, checklist progress). The vault's signed-URL preview does not resolve in dev (the D120 pattern), so `toDoc` carries `url` for a viewer but the bytes only exist behind the mock's `dataUrl` | design | Med | a buyer-facing request-status read (`GET /me/document-requests` scoped to the requester) would let the buyer half join the seam; until then it is deliberately mock-only |
| D125 | **The flipped document vault degrades badly on failure** (item 2 resolved 2026-08-08) — Slice D moved the owner vault's list/upload/delete and the owner request inbox onto `documentService`, which is correct but surfaced four more rough edges the synchronous store never had, all resolved 2026-08-08 (see below). **(1)** every load `.catch`es to `[]`, so a failed `GET /me/documents/requests` does not degrade the buyer-request panel, it **removes** it (the whole card is gated on `docReqs.length > 0`) along with its pending badge — an owner is told they have no requests when the truth is the read broke; the same shape makes a failed vault read render a confident, wrong `0/10` loan checklist. **(1) — RESOLVED 2026-08-08:** each of the three lists now carries a `loading`/`error`/`ready` status, so a failed read shows a retry affordance instead of removing the panel, and a failed vault read no longer paints a confident wrong checklist (the checklist is hidden until the vault is `ready`). **(2) — RESOLVED:** `useDashboardData` now reads the inbox through `listDocRequests` and grants through `respondDocRequest` (the seam), the same service `DocumentsTab` uses, so the Documents tab, the `leads` badge and the Action Center share one source of truth and a grant issued from the dashboard reaches the server in http mode; `countSharedDocs`/`notifyBuyerDocsGranted` stay mock-only behind `!isHttpDomain('document')`. Covered by `consumer/account/doc-requests-grant.spec.js`. **(3)** there is no loading state: all three lists start `[]`, so a full vault first paints as an empty one (ring `0/26`, every tile in Upload) and then snaps — the late reflow is what destabilised `doc-info.spec.js`. **(3) — RESOLVED 2026-08-08:** a loading vault renders a skeleton and an indeterminate ring instead of a false `0/N` that snaps. **(4)** `uploadForCategory` captures `targetProp` but `setTick` refetches the *current* property, so uploading while the selector moves toasts success next to an unchanged tile; and `tick` is one scalar across all three effects, so one grant costs four round trips while the provider's own post-mutation return value is discarded. **(4) — RESOLVED 2026-08-08:** `tick` is gone; each mutation applies its provider return value to the one list it belongs to, guarded by a ref so an upload never lands on the wrong flat when the picker moves, and a grant patches only the answered row. **(5)** the http grant branch always toasts success, losing the mock's honest `approvedNoDocToast` for a category with no file behind it; and `openDocUrl` opens **any** `https?://` the API returns, so a dev signed URL (D120) yields a blank tab with a DNS error instead of the `noPreviewToast` it would have shown. **(5) — RESOLVED 2026-08-08 (viewer half):** the viewer treats the non-resolving dev storage-stub URL as non-previewable and shows `noPreviewToast`; the http grant toast is intentionally left as success — live mode has no client-side doc count to flag an empty grant, and the server is authoritative there | design | Med | all five items are resolved (item 2 by the dashboard-seam slice, items 1/3/4/5 by the `DocumentsTab` cleanup); delete this entry once both land on the same branch. The only deliberate residue is the live http grant toast noted in (5) |
| D129 | **Two application-data files are still eagerly bundled onto the critical path** — the locale third of this item is done (2026-08-09: English split into a 6-namespace eager shell and 14 route-deferred chunks loaded inside the existing `lazy()` boundary; critical path 591.9 → **533.0 KB gzip**, and a new `check-i18n-route-namespaces.mjs` gate proves from the import graph that every route declares what it can reach, so a new namespace can no longer silently join the shell). **2026-08-10: the `societies-rera.js` half is done, and closing it exposed a live data bug.** `data/societies.js` had already been converted to a memoised async accessor (`ensureSocietyCatalogue`/`societyCatalogueLoaded`/`invalidateCatalogue`) — but **nothing ever called it**, so all eight consumers read the catalogue before it loaded and silently saw an empty one. That was not a perf issue: `useSocietyHub` fabricated a plausible page for all 320 RERA slugs, and `SocietySelect` **minted duplicate societies** because the name it was given never matched an existing row. New `lib/useSocietyCatalogue.js` gates all eight (`useSocietyHub`, `Societies`, `SocietySelect`, `SocietyFinder`, `AdminSocieties` ×2, `Listings`, `Locality`). **A follow-up review found that first pass incomplete, and its two "deliberately ungated with proof" decisions wrong; both were corrected the same day.** The duplicate-mint bug was *not* actually fixed by gating the memo: `canCreate` still only tested `!exact`, so during the load window the "Add '<name>'" row rendered and was clickable for all 320 RERA names. Gating alone is also a guard on four separate surfaces that only has to be forgotten once, and a mint is unrecoverable — so `addCommunitySociety` now refuses a name that matches a canonical row (by slug **or** by name, since `slugifySociety` folds the locality in) and returns that row instead, with `SocietySelect`/`SocietyFinder`/`Societies` gating their affordance on top. Three more real defects: `ensureSocietyCatalogue` cached a **rejected** promise (still truthy, so `if (!reraPromise)` was false forever — one flaky chunk fetch meant a permanently partial catalogue, no retry, plus unhandled rejections from the fire-and-forget call in `catalogue()`); `AdminSocieties`' `reload()` effect was keyed on `[bump]` alone although `getSocietyCandidates()` → `suggestDuplicates()` reads `allSocieties()`, so the ops Candidates queue scanned 28 rows, reported `dupes: []` for a textbook duplicate and opened the merge dialog with no target — the operator then verified the junk row; and `SocietySelect.onType` read the catalogue *again*, ungated, in an event handler, so typing an exact RERA name early bound `societyId: ''` permanently and `societyForListing` handed the flat to a **different** society via the hash pool while still displaying the right name. The two ungated call sites were justified by claims that do not hold and are now gated: `catalogue()` calls `ensureSocietyCatalogue()` on every read, so `SocietiesSection` was already pulling the 182 KB on Home — the real win is "off the critical path", not "off the route", and gating costs one re-render and zero bytes (the hook's docblock said otherwise and has been corrected); RERA rows also *do* compete in that strip (`tier: 'verified'` with `registration`/`conveyance` both true), and its output was stable only by the accident that they tie at `homes: 0`, decided by which of two promises resolved first. `searchEntities`' "0 of 80 listings carry a `societyId`" is true of seed data only — `submit.js` persists real ids into the same store, so `HeroSearch`'s index is gated too. `LoadError.jsx` now reads `t('common.retry')` instead of `t('dash.retry')`, and `dashboard` is off the `Listings`/`Flatmates` route declarations, so the 5.08 KB namespace no longer follows them. What remains is `db.json` (236 KB), and it is not a bundling problem: 64 `rawLoad()` + 36 `rawDb()` sites across 11 mockApi modules, with `AppFlagsContext` calling `rawDb()` **during render** — that one blocks the async conversion outright. **2026-08-10 (later): the `db.json` third is done, and that framing was wrong.** `db.json` is a *seed*, not the store: the store is `localStorage['puneNestDB_v5']`, and after the first run the seed is read by nothing except the five one-shot migrations and `resetDb()`. Converting ~100 synchronous call sites would have made a hundred reads async to defer a file that ninety-nine of them never touch. Instead one `import()` sits behind a new `ensureMockDb()` in `mockApi/core.js`, awaited once in `main.jsx` between the module graph and `createRoot().render()`. Every `rawLoad()`/`rawDb()` site is **unchanged and still synchronous** — and `AppFlagsContext`'s render-time read stops being a blocker rather than having to move, because the store is guaranteed populated before the first render. A `seedNeeded()` short-circuit means a returning visitor (storage seeded, migrations stamped) never fetches the chunk at all. The invariant is defended at the bottom, not at the call sites: `reseed()` **throws** instead of returning `{}`, because `mutateDb()` is read-then-write and an empty read would become a permanent empty write — the same silent-and-unrecoverable class as the societies half. `hydrateFromDisk()` was a fire-and-forget IIFE racing the seed write; it is now awaited inside the same gate. Critical path 512.2 → **490.8 KB gzip**; the seed is now `assets/db-*.js` (169.0 kB raw / 22.3 KB gzip), proven to be the only chunk containing its rows and absent from `dist/index.html`. A 666-module / 2814-function AST scan found 20 module-scope calls that reach the store, all safe: 19 are false positives (curried getters in `mockApi/collections.js`, and `lib/store/internals.js`'s pure-`localStorage` `get` merged by name), and the one real read — `Refer.jsx:10` `fee('rentAgreementPlatform')` — is inside a `lazy()` route and falls back to `FEE_DEFAULTS` | frontend | Med | done — `db.json` still ships as a lazy chunk until the http seam is live, but it is off the critical path and no longer costs a cold visitor anything before first paint |
| D133 | **No aggregate/BFF endpoints** — `/me/dashboard` returns zero matches in both the Java tree and the OpenAPI spec, so the dashboard fans out to many small calls (the D-series has already caught one 4× duplicate fetch there). Recorded as an explicitly unresolved proposal rather than a decision | design | Med | confirm against the real dashboard call count before adding a bespoke aggregate |
| D134 | ~~**No enforced minimum text size, and nothing that could enforce one** — the blanket 12 px floor was never applied (a large diff across ~40 files), and there is no legibility spec~~ **Closed 2026-08-10.** The register's "~40 files" was an undercount by an order of magnitude: the real surface was **698 declarations across 182 files** (53 in source CSS, 645 `text-[<12px]` utilities), which is precisely why the hand-edit was never attempted. So the floor is enforced at **build time** instead — `styles/min-font-size.js`, a PostCSS plugin running after Tailwind, rewrites any `font-size` under 12 px (`postcss.config.js` moved to array form to order it). Built CSS now carries 206 `font-size` declarations, **0 under 12 px**, 83 at exactly 12 px; a mutation probe confirms `11px`→`12px` and `.625rem`→`12px` while `2em` and `24px` pass through untouched. `e2e/tests/mobile/text-legibility.spec.js` sweeps computed sizes across 14 routes × 2 viewports (22/22 green) and is the thing that would catch a regression the plugin cannot see — exemptions live there, not in the plugin: `.sr-only`, `sub`, `sup`, `svg`, `[data-text-exempt]` | design | Med | done — the plugin has no exemption hatch by design; anything that genuinely needs sub-12 px must opt out in markup and justify itself to the spec |
| D136 | ~~**The tap-target sweep does not walk the routes with the worst offenders**~~ **Closed 2026-08-10.** The sweep now covers 14 public routes plus `/dashboard`, `/messages`, `/admin` and `/ops` — the authenticated fixture that was blocking the admin half already existed (`loginAsAdmin`/`loginAsStaff` in `helpers/auth.js`). Component fixes: `routes/messages.css`, `routes/flatmates.css` (`.badge-seeker` 11→12 px), `Society.jsx`, `HelpHome.jsx`, `OverviewPanel.jsx`, `SavedPanel.jsx`, `AdminTopbarTools.jsx`, `AdminLayout.jsx`. The extended sweep immediately earned its keep: it caught the account drawer's "Log out" at 275×40, and chasing that turned up the **real** defect underneath — the drawer's header and footer mount unconditionally so the panel keeps its shape mid-transition, which left live buttons focusable inside an `aria-hidden` subtree whenever it was shut (`pointer-events-none` stops a mouse, not the Tab key). Now `inert`, with `min-h-[44px]` on the menu rows | e2e | Med | done — a control the sweep can measure while the user cannot see it is itself the bug |
| D138 | ~~**No Lighthouse or performance run in CI**~~ **Closed 2026-08-10.** A third `lighthouse` job in `ci.yml` runs `.github/lighthouse/run-lighthouse.mjs` over `/`, `/listings` and `/property/P5000`. **Record-only by decision** — it exits 0 unless *every* route fails, so it reports FCP/LCP without becoming a merge gate on a bundle that is still 511.7 KB against a 540 budget; a budget that fails today would only be routed around. Lighthouse is pinned to 13.4.1 via `npx --yes` rather than added to `frontend/package.json`, deliberately: it is a CI-only tool and putting it in the manifest would slow every `npm ci` | build | Low | done — turn the budgets on once D129's `db.json` half lands and the critical path actually moves |
| D139 | ~~**No pull-to-refresh on any list surface** — zero matches for `pull-to-refresh`/`usePullToRefresh`. Distinct from D99: that gesture is destructive and needs undo, this one is not~~ **Closed 2026-08-10.** `lib/usePullToRefresh.js`, on `/listings`, `/saved`, `/notifications`, `/messages`. Gated on `(hover:none) and (pointer:coarse)` rather than on width, since a refreshable list exists at every size; binds `touchmove` by hand because React registers it passively at the root and the one legitimate `preventDefault` would be a no-op plus a console warning. A gesture travelling more sideways than down when it crosses the slop is left alone, so `SwipeCard` on Saved and the chip rail on Notifications keep working, and the Listings map carries `data-no-ptr` because a non-scrolling region truthfully answers "yes, at the top" and would otherwise eat the pan. The Listings pull goes through a new `useAsyncList` `refresh()` so it is sequenced against the effect's own read — an outside `loader().then(setList)` can be overtaken by an earlier load, or wiped by one that fails later. `prefers-reduced-motion` keeps the affordance and drops the travel. **2026-08-10 — the "Playwright cannot test this" claim was wrong and is withdrawn.** `e2e/tests/mobile/pull-to-refresh.spec.js` drives real trusted touch through CDP `Input.dispatchTouchEvent` (touchStart / 12 × touchMove / touchEnd): 6 tests × 2 mobile projects covering the coarse-pointer gate, a past-threshold pull, a short pull that snaps back, the sideways-drag carve-out, the `data-no-ptr` opt-out and the scrolled-away-from-top case. Every negative case is paired with a positive control pull, so "nothing happened" can never pass for the wrong reason. Also fixed here: the effect cleanup reset `alive` and the timer but not `isRefreshing`/`pullDistance`, which are state and outlive the effect — a change to `enabled`/`threshold` between `touchend` and `settle` stranded the spinner on screen with nothing left to clear it | frontend | Low | done |
| D140 | ~~**The service landings' hero imagery is still eagerly loaded full-size** — the actual 1.26 MB heroes are CSS `background-image` divs rendered through the shared `components/ServiceLanding.jsx`, which `srcSetFor` cannot touch~~ **Closed 2026-08-10.** `ServiceLanding`'s hero is a real `<img>` now — `srcSetFor(heroImage, [640, 960, 1280, 1600])` with `sizes="100vw"`, `fetchPriority="high"`, `decoding="async"`, intrinsic `width`/`height`, and `object-cover object-center` reproducing what `bg-cover bg-center` drew. `alt=""` because the hero is decorative and every landing states its subject in the `<h1>` on top of it. All three callers (`PackersMovers`, `HomeLoans`, `PropertyLegal`) pass through unchanged; build and the 540 KB size gate green. **Correction (2026-08-10):** the original note justified `width`/`height` as protection against layout shift — that reasoning is wrong. The hero is `absolute inset-0 w-full h-full`, so it is out of flow and the explicit sizes cannot reserve space; `w-full h-full` overrides the intrinsic aspect ratio anyway. The attributes are harmless and worth keeping as an accurate record of the asset, but they buy no CLS here. Covered by `e2e/tests/consumer/services/hero-image.spec.js` across all three landings — note the routes are `/services/packers-movers`, `/home-loans` (**not** under `/services`) and `/services/property-legal` | frontend | Low | done |
| D142 | ~~**The mobile home fold has no search entry point** — the hero search panel is `hidden lg:block` by deliberate design (it cost 286 px) and no compact tap-to-search pill replaced it~~ **Closed 2026-08-10 as won't-do.** The panel's absence is the design, not an oversight: it cost 286 px of the first fold, and the bottom-nav Search tab is a persistent, thumb-reachable entry point present on every route — a second entry point in the fold would duplicate it while re-spending the space that was deliberately reclaimed. Re-open only if analytics show mobile visitors leaving the home route without ever reaching search | design | Low | won't-do — revisit on evidence, not on symmetry with desktop |
| D143 | ~~**The Saved tab strip is still a horizontally-scrolling pill row** — the signed-out shortlist state shipped, but the strip itself hides tabs off-screen on a 360 px viewport with no affordance that more exist~~ **Closed 2026-08-10.** New `pages/consumer/saved/CategorySwitcher.jsx`, a phone-only (`sm:hidden`) bottom sheet reusing `dashboard/MobileNav.jsx`'s geometry verbatim rather than inventing a second sheet idiom; the pill strip becomes `hidden sm:flex` above it. It carries `data-no-ptr` on the overlay because the sheet is a DOM descendant of the pull-to-refresh root, and a non-scrolling region that truthfully answers "yes, at the top" would otherwise eat the gesture | design | Low | done |
| D158 | **The write rate limit still counts in memory — the Redis path is written but cannot be built** — the seam landed 2026-08-12: `WriteRateLimitStore` with `InMemoryWriteRateLimitStore` (default) and `RedisWriteRateLimitStore`, selected by `punenest.ratelimit.store`, `WriteRateLimiter`'s own logic untouched. The counter is a single Lua script (`INCR`, `PTTL`, `PEXPIRE` only when the TTL is missing) rather than `INCR` + conditional `EXPIRE`, because a crash between those two strands a key at 1 with no expiry and refuses that caller **forever**. **What blocks it is dependency resolution, not design:** the offline build resolves `spring-boot-starter-data-redis` at 4.1.0 while `~/.m2` caches 3.3.5, and pinning 3.3.5 drags in `spring-data-keyvalue` 4.1.0 / `spring-oxm` 7.0.8; excluding those drags in `netty` 4.2.15 and `reactor-core` 3.8.6. Same wall the pom's AWS-SDK comment documents. So `RedisEval` is deliberately unimplemented and `store=redis` **fails fast at boot** rather than silently counting in memory. A Redis *outage* at runtime does the opposite — falls back to in-memory and logs ERROR — because refusing every write during a cache blip is worse than a temporarily generous limit; the asymmetry is between a misconfiguration (loud, at boot, fixable) and an incident (degrade, keep serving) | security | Med | implement `RedisEval` (~5 lines over `RedisTemplate.execute`) the first time the build runs online, or when a second instance is provisioned — whichever comes first |
| D176 | **`V27__flatmates.sql` still describes its unique index backwards** — the dead-index half is closed (V54 dropped `idx_transactions_owner`; `owner_id` is the index's *leading* column and no query shape constrains it, `Transaction.getOwnerId()` has zero call sites, and ownership resolves through `properties.findByIdAndOwner_Id` — the index was unreachable, not merely unpopular). What is left is prose: V27 calls the interest endpoints "idempotent" (they refuse a duplicate with 409) and says the index "is the reason the service does not check-then-insert" (both write paths do exactly that, and did before D175 moved the read behind the advisory lock). Only its third claim survives, and only as the residual case — the lock closes the race under READ COMMITTED, so the index is the backstop for a higher isolation level and the reason a double press yields 409 rather than 500. Flyway runs with the default `validateOnMigrate=true` and V27 is applied to both databases, so editing it moves its checksum and breaks every applied database on next boot. The correction therefore ships in three places instead: V54's header, a `COMMENT ON INDEX uq_flatmate_requests_target_requester` that travels *with the schema* and shows in `\d+`, and `FlatmateRequestRepository`'s javadoc | quality | Low | rewrite V27's comment the next time the databases are rebuilt from empty, and delete the then-duplicate section from V54's header |
| D180 | **~~`rent-agreement.spec.js:116` looks like a real product bug~~ — it was another lane's `npm run dev`. Closed 2026-08-10** — the register was wrong on all three counts: wrong line, wrong locator, wrong cause. The real failure is a **10 s action** timeout in `fillTerms` on `.step-panel.active .pn-datefield`, not a 30 s test timeout on the review checkbox, and nothing about the review step is unstable. Cause: Vite broadcasts a full **`page reload`** to every connected browser page whenever a non-HMR-able module changes — any `src/i18n/locales/**/*.json`, most `src/context/*.jsx`. When a parallel lane edits `frontend/src/**` mid-suite, the wizard remounts, `useFormDraft` replays the last 400 ms-debounced autosave, the step just typed comes back blank, `Next` silently refuses, and the run falls over several helpers later on a locator with nothing to do with it. The same reload explains the sibling `referral-rewards.spec.js` *"Execution context was destroyed"* failures. **Resolved:** `clickNext` now asserts the wizard actually advanced and `submitFromReview` asserts the request was actually created, so this class of failure names itself at the transition instead of misdirecting — assertions, not retries; a `Next` that genuinely refuses still fails. See detail below | e2e | — | closed by evidence, not by hardening; the standing rule it produced is **never run mock e2e while another lane edits `frontend/src/**`** |

| D183 | **On a second device the duplicate-409 path shows a done state for a thread that is not there** — the benign `already_interested` branch flips the card to its done state, which is true of the *server*. But the notification and the chat thread are written client-side on the success path only, so on the device that did not make the original ask Messages is empty. Pre-existing consequence of threads living in `localStorage`. The *copy* half is closed — the three strings were renamed (`interestAlreadyRecorded`, `enquiryAlreadyRecorded`, `joinRequestAlreadyRecorded`) and reworded across en/hi/mr to state only what the server actually knows ("already has your earlier interest — there's nothing more to send"), so nothing now promises a conversation. The renames are deliberate: an in-place edit would have let a stale locale pass `check-i18n-locales`, which compares key sets | frontend | Low | closes when the host-inbox/threads endpoint lands and Messages reads the seam instead of `localStorage`. Until then: **do not restore a "continue in Messages" wording on the 409 path** before that seam exists |

### Detail on the ones that need it

**D2 — rate limiting on authenticated writes. Closed 2026-08-09.** Raised by `security-reviewer`
during slice 3 and deferred every slice since as platform-wide, while every slice added more
authenticated write surface — contact requests, offers, visits, reviews, saved searches, service
requests. Only `POST /auth/login` was ever limited.

Closed by `WriteRateLimitFilter`, which sits after `JwtAuthFilter` in the security chain and counts
every mutating request against a fixed window keyed on the principal id, falling back to the client
address when there is none. The shape matters more than the number: a filter that limits writes *by
default* cannot be forgotten at a new endpoint, whereas the per-controller annotation this could have
been would have been forgotten at exactly the endpoint where it mattered. Reads are untouched — they
are cheap, cacheable and mostly anonymous, and `spring.data.web.pageable.max-page-size` is what bounds
their cost — with the single exception of `GET /documents/shared`, the anonymous token-guessable read
the register named alongside the writes, which is included by path because its risk is enumeration
rather than volume. The signed Cashfree callbacks are exempt: they are HMAC-authenticated, they all
arrive from a handful of addresses that would share one bucket, and a refused callback is a customer
who paid and was not credited.

Two things it does not do, both deliberate and both recorded rather than assumed. It counts in memory
and per instance (**D158**). And it does not close **D73** — that is a check-then-insert race inside a
transaction, which a request-level limiter narrows but cannot fix; the two were expected to share one
principal-keyed counter and it turns out they cannot, because D73's counter has to be atomic with the
insert it guards. (D73 was closed separately on 2026-08-09 by a transaction-scoped advisory lock; see
below. This paragraph is why the two limiters remain distinct rather than one.)

A `security-reviewer` pass over the finished slice found four ways the first version could be walked
past, all now fixed and all worth recording because they rhyme. Three were the same mistake in
different clothes — *the identifier the filter compares is not the identifier the dispatcher uses.*
The client address was the proxy's address, because nothing set `server.forward-headers-strategy`, so
every anonymous caller on the internet shared one bucket and a single host could have 429'd every
anonymous write on the platform: the limiter becoming the outage it was added to prevent. The prod
profile now sets `native` with the balancer's CIDR pinned in `server.tomcat.remoteip.internal-proxies`
as a `${INTERNAL_PROXIES}` lookup with no default, so a deploy that has not decided its topology fails
to start rather than silently keying every user together. The path was the raw URI, so
`GET /documents/share%64` reached the protected handler without being counted; it is now decoded and
cleaned before matching. And the method check was `GET`, but Spring dispatches `HEAD` to `@GetMapping`
handlers and only drops the body — the status code answers the enumeration question perfectly well —
so limited reads are now matched on path alone.

The fourth was worse in kind. The map was bounded by refusing to track new keys past a ceiling and
letting them through unlimited, which is the protection failing in precisely the attacker's direction:
50,000 addresses — one routed IPv6 /64 — would have switched the limit off for everyone arriving
afterwards, while the flood's own traffic held the map full. It is now bounded by *eviction* instead
(an access-ordered `LinkedHashMap` with `removeEldestEntry`), which deletes the fail-open, the O(n)
inline sweep it needed, and a race where the sweep could drop a counter between lookup and increment
and silently discard the request it had just counted. Both misconfigurations of the knobs — a zero
window, which limits nothing while looking healthy, and a zero budget, which refuses every write — are
now rejected at construction rather than clamped.

One bound it still does not have: the per-user budget is not the platform ceiling, because signup is
passwordless and self-service, so an attacker can mint accounts and with them fresh buckets. The real
ceiling on that path is set by the OTP send limits, which is why those are re-pinned in the prod
profile rather than inherited from the loosened local defaults.

A second review pass over those fixes found four more, and the pattern in them is worth more than the
individual bugs: **a security control expressed as configuration is only as reliable as the weakest
way that configuration can go missing.** The proxy fix had been written as two `server.*` lines in the
prod profile, which fails silently in two ways — a staging or preview deploy simply never loads that
file (the magic-string trap already recorded as D147), and Boot's `@ConfigurationProperties` binder
resolves placeholders with `ignoreUnresolvablePlaceholders`, so an unset `INTERNAL_PROXIES` binds the
literal text rather than failing, while the far more common declared-but-empty case makes Tomcat trust
nothing, rewrite nothing and log nothing. The topology is now a property this code owns
(`punenest.security.trusted-proxies`), in the base file so every profile has an answer, read through
`@Value` — which genuinely throws — and validated by `TrustedProxyConfig` before the server starts.
`none` is a legitimate answer for a directly exposed instance; saying nothing is not. And because a
wrong answer is still possible, `WriteRateLimitFilter` logs once, loudly, if a request ever arrives
carrying `X-Forwarded-For` while the declaration says nothing is in front — which is proof the
declaration is wrong, obtained from traffic rather than from trust.

The other three: anonymous callers are now keyed on the IPv6 **/64** rather than the address, because
a single host is routinely assigned a whole /64 and would otherwise have 2⁶⁴ free budgets — the
cheapest possible defeat of the limit, and the thing that made the tracked-key ceiling meaningless in
principle. Path parameters are stripped before matching, since Spring routes `/documents/shared;x=1`
to the protected handler; `StrictHttpFirewall` rejects that shape today, which is a reason to expect
the code to be exercised rarely and no reason to depend on it. And the provider callbacks are no
longer *exempt* — they now have their own budget at fifty times the ordinary one, plus a 64 KB body
ceiling. A blanket exemption had made them the only completely unthrottled writes on the platform, and
they are `permitAll`: the HMAC does reject an unsigned body, but only after the container has buffered
it and the handler has materialised it as a string, and nothing else in this stack bounds a JSON body
at all.
A third pass found two more, and both were the same species as everything above: **a guard that is
correct about the value it examines and wrong about which value that is.** The path helper cut at the
first `;` and *then* decoded, while Spring decodes first and strips afterwards — so `%3B` reinstated
the bypass, this time taking the new body ceiling with it, since a callback path that fails to match
`PROVIDER_CALLBACKS` never reaches the size check. And the size check itself read
`getContentLengthLong()` and tested only whether it *exceeded* the cap; an unknown length reports as
`-1`, which exceeds nothing, so `Transfer-Encoding: chunked` streamed an unbounded body onto the heap
through an unauthenticated route in a single request. Both are now closed: decode-then-strip, and an
undeclared length is refused outright, which costs nothing because the provider always sends one.
`StrictHttpFirewall` happened to block the first today — which is exactly why the second matters, as
it demonstrates what depending on that would have been worth.

The through-line across all three passes is worth stating plainly, because it will apply to the next
control as much as this one: **every finding but one was a mismatch between the string the security
code compared and the string the framework acted on** — the address, the path, the method, the
encoding, the declared length. None was a mistake in the rate-limiting logic itself.
The pre-existing local guards remain and remain worth having: `ServiceRequestService` still caps a
principal at one *outstanding unpaid* request per desk, which is a cap on open orders rather than a
rate, and `OtpService`/`SocietyLeadService` still hold their own DB-backed per-mobile limits — keyed
on something more meaningful than an address, and therefore not superseded by this.

**D73 — the count-then-insert race inside those limits. Closed 2026-08-09.** Four call sites, not the
three the row named: `ShareFlatService` had been retired by V28, and the cap it was raised against
now lives in the flatmates domain behind *two* entrances — `FlatmateSeekerService.express` and
`FlatmateSupplyService.record` — which count the same rows of `flatmate_requests` against the same
ceiling of ten an hour. Locking those two separately would have left the burst a second door, so they
share one lock namespace; `OtpService` (per mobile *and* purpose) and `SocietyLeadService` (per
mobile) have one each.

**The interesting part is what did not transfer.** D153, D160 and D170 closed the same shape three
times with a partial unique index plus `ConstraintViolations.isOn` — the database refuses the second
row and the catch translates the named collision. That works because "at most one" has a key. "At
most N in the last hour" has none: there is no column, and no expression over columns, whose
uniqueness is a count over a moving window. Two further candidates fail for reasons worth writing
down, because both look right.

*The one-statement conditional insert* — `INSERT … SELECT … WHERE (SELECT count(…)) < N` — is the
tempting one, and it is a worse bug than the one it replaces. Under `READ COMMITTED` the subquery
reads the statement's own snapshot, so two concurrent inserts both see the pre-insert count and both
proceed. It is atomic in the sense that it is one statement and not atomic in the sense that matters,
and it would have looked like a fix in review.

*`SELECT … FOR UPDATE` over the counted rows* fails on the phantom. Locking the rows already in the
window does make two writers queue, but the loser resumes holding the snapshot it took before it
blocked: it never sees the row the winner inserted, because that row did not exist when the loser
looked and nothing the loser locked was modified. The cap stays exactly as leaky. Locking an
*owning* row would work, and two of the four sites have no owning row to lock — a society-lead submit
is anonymous, and an OTP send names a mobile that usually has no account.

What is left is a transaction-scoped advisory lock, which is what `common.persistence.RateLimitLock`
takes: no row required, an arbitrary key, and released by Postgres itself when the transaction ends,
so no path — thrown exception, dropped connection — leaks it. It depends on `READ COMMITTED`, which
is the default and is not overridden anywhere, and the dependency is not incidental: the whole point
is that the count issued *after* the lock is a new statement with a new snapshot. Raise the isolation
level on these paths and the lock would be held, paid for, and useless.

**Reusing `WriteRateLimiter` was considered and rejected.** It counts in memory, per instance, and
resets on deploy (D158) — and two of these limits explicitly refused an in-memory counter when they
were written. An OTP budget that resets on deploy is not a budget, and a public form with no session
has nothing to hang a bucket off. Folding them in would have deleted three limiters and the guarantee
they exist for. The two controls remain complementary and neither subsumes the other: D2 bounds how
fast one caller can arrive at the API at all; this bounds what a burst can spend once it has.

**The tests had to leave the harness.** `AbstractApiTest` is `@Transactional` and rolls back, so its
writes are never visible to another connection — and this bug is, by definition, a second writer's
count failing to include a first writer's *committed* row. A rolling-back test would pass identically
before and after the fix, which is D90 exactly. `RateLimitRaceTest` and `FlatmateInterestRaceTest` are
therefore plain `@SpringBootTest` classes with real threads, real commits and their own cleanup, and
they assert on both the number of refusals and the number of rows that survived — a limiter that
refused the right number of callers and still wrote the wrong number of rows would be just as broken.
The one cost is that they are bounded by the deliberately small test connection pool: the flatmate
race runs two threads rather than three because the winner needs a second connection for its
`REQUIRES_NEW` audit write.

**D151 — the identity numbers the drafting desk drafts from. Closed 2026-08-09.** A Leave & License
names each party by PAN and Aadhaar; the paid-L&L security pass stopped both from reaching the server
at all, correctly (`details` is plaintext `jsonb` echoed verbatim to every staff read, so carrying
them there made the ops queue's first page a bulk identity dump), and left the desk with nothing.
`PUT|GET /service-requests/{id}/identities` is the channel that was missing, over a new
`service_request_identities` table (V47) that is outside `details` in every sense —
`rejectIdentityNumbers` is untouched and the wizard still redacts.

**The register named the document vault and this went elsewhere, deliberately.** The vault's read
model is `FileStorage.signedDownloadUrl(key)`: a URL that carries its own authority, that nobody can
be excluded from, and whose use never reaches our server — so neither of the two requirements that
define this item ("only the assigned operator", "every access recorded") is expressible against it.
`FileStorage` has no authenticated read at all, only a signer. Two further facts settled it:
`DocumentUploads.validate` admits PDF/JPEG/PNG/HEIC/WebP proved by magic bytes, so a set of numbers
is not something the vault can hold without weakening the allowlist that keeps non-documents out; and
`DocumentService.delete` deliberately leaves the stored object behind, which is a defensible trade
for a sale deed and an indefensible one for an Aadhaar number. The vault would have made this a
permanent, un-revocable, un-auditable copy of the most sensitive field the platform touches.
**The deviation was put to the owner and signed off on 2026-08-10**, so the dedicated table is the
agreed design rather than an unreviewed departure: `service_request_identities` stands, and any
later proposal to move these numbers into the vault has to answer the three objections above first.

**Only the assignee reads, and that is enforced by a refusal rather than by an omission.** Not
"staff", not "admin" — `request.assigneeId == caller.userId()`, or 403. An unassigned request refuses
everyone, because "whoever asked first" is not a name. An admin who needs the numbers assigns the
request to themselves first, which costs two visible moves when somebody else holds it (there is no
`assigned → assigned` edge, so it goes back through `in-progress`) and writes a timeline entry the
customer can read plus an audit row naming them. The control is accountability, not prohibition. Both
reads and refusals go to `audit_log`; the refusal is the more interesting entry, because it is
somebody reaching for a matter that is not theirs. The numbers themselves are never in the audit
row — counts and roles only, keyed by request id, so the one table that must be trusted does not
become the second place they are held.

**Retention is bounded by the work, not by a policy document.** `purgeFor` runs from
`ServiceRequestService.transition` on every move into a terminal status: `completed` because the
registered document now carries the numbers, `cancelled` because nothing will be drafted from them.
The rows survive with their names and a `purged_at` stamp — "recorded, and since discarded" and
"never recorded" are different facts about a matter. Nothing else on the platform holds a raw
Aadhaar (`identity.kyc` stores masks by design), so this exception is time-boxed as well as narrow,
which is what Aadhaar Act s.29 asks for.

**Only the requester writes**, staff and admin included in the refusal: a desk that could write the
parties' identity numbers could also invent them, and the agreement would then name somebody the
customer never identified.

**Still open after this.** The ops back-office has no live client for service requests at all — it
runs entirely on `lib/serviceFlow.js` against `localStorage` — so the `GET` half has a tested server
and no screen yet. That is the ops seam's slice, not this one; recorded as D173.

**D180 — the rent-agreement "product bug" that was another lane's dev server. Closed 2026-08-10.**
The register said: 30 s timeout at `:116` inside `submitFromReview`, on the review-declaration
checkbox, *"element is not stable"*, *"outside of the viewport"* — a scroll/animation fight. Every
part of that is wrong, and the run in isolation says so first: **15/15 pass**, repeatedly. It only
fails under parallel load.

**What actually fails.** `TimeoutError: locator.click: Timeout 10000ms exceeded — waiting for
locator('.step-panel.active').locator('.pn-datefield')`. A **10 s action** timeout, raised in
`fillTerms`, four helpers earlier than the reported line. `test.slow()` would not have helped; the
review step is not involved at all.

**The tell is in the failure screenshot.** The *"We saved your progress. Your earlier answers were
restored…"* banner is visible **mid-test**, with the wizard sitting on Tenant and the tenant fields
blank. `useFormDraft`'s restore effect has dependency array `[key]` — it is mount-only. A banner
mid-test can therefore only mean one thing: **the page remounted.**

**Why it remounted.** Vite's HMR channel broadcasts a full `page reload` to every connected browser
page whenever a changed module cannot be hot-swapped — `src/i18n/locales/**/*.json` and most
`src/context/*.jsx` qualify. Captured from the dev-server log during a failing run:

```
8:51:20 AM [vite] (client) page reload src/i18n/locales/hi/dashboard.json
9:04:42 AM [vite] (client) page reload src/context/AuthContext.jsx   (+6 more)
```

Those edits were **other lanes of this same tech-debt wave**, working in `frontend/src/**` while the
suite ran. The reload remounts the page, `useFormDraft` replays the last 400 ms-debounced autosave,
the step just typed comes back blank, `next()` fails `validateStep` and returns silently, and the
run continues typing tenant answers into the owner panel — the three panels share every placeholder
— until it finally trips over `.pn-datefield` ten seconds later. Reproduced deterministically with a
throwaway probe: fill three steps, `page.reload()`, `Next` → banner shown, heading back on *Owner /
Licensor Details*, zero date fields. Exactly the observed failure. The same reload explains the
sibling `referral-rewards.spec.js` *"Execution context was destroyed, most likely because of a
navigation"* failures.

**Two things hid this from everyone.** `playwright.config.js` sets `webServer.stdout: 'ignore'`, so
Vite's log — which was announcing the cause, by name, with a timestamp — was thrown away. And the
spec's helpers never checked their own postconditions, so the failure surfaced far from its cause.

**What was changed, and why it is not hardening around a defect.** `clickNext(page, expectStep)` now
asserts the step dot for the expected step went `active`; `submitFromReview` now asserts the done
panel appeared. No retry, no re-fill, no loosened selector: a `Next` that genuinely refuses to
advance still fails the test, it just fails at the transition saying *"wizard did not advance to
step 4"* instead of three helpers downstream. Both assertions were mutation-tested (drop the owner
PAN → `clickNext` goes red; drop the declaration checkbox → `submitFromReview` goes red).

**The standing rule.** Never run mock e2e while another lane edits `frontend/src/**`. To attribute a
suspected reload, start Vite yourself with the log captured and point Playwright at it via
`BASE_URL` (`reuseExistingServer` does the rest), then grep the log for `page reload` across the run
window.

**D22 — Maps key rotation.** A real key was committed in `.env.example` and is in git history across
two commits. A placeholder is committed now, but **history exposure means the key must be rotated** —
replacing the file does not undo the leak. This is the highest-urgency item in the register and the
only one that is externally exploitable today.

**D26 — client-side trust derivation. Closed 2026-08-11, and the register had it half wrong.** The
row read as one item; it was two, with opposite answers. `applyVerifiedBadgeToListings` is **not** a
trust derivation at all — its only caller is `mock/verificationProvider.js`, so it *is* the mock half
of the seam doing exactly its job, and the http half correctly has no counterpart. It was left
untouched with a comment saying so. `isSeriousBuyer(mobile)` was the real defect, and worse than
recorded: it computed `Number(last two digits of mobile) % 3 === 0` — a fabricated trust claim about
a stranger, deterministically forgeable by choosing a SIM — and it was **not** mock-only. On the
owner's Leads inbox `item.contactMobile` is populated once the owner approves a contact request, so
in `http` mode an owner saw "Serious Buyer" beside a real person's real name and number, on the
strength of digit arithmetic, one time in three. It is deleted; the badge now reads D114's batch
`POST /tenant-profiles/verified` (one request per inbox) and **fails closed** — absence of an answer
renders no badge. The visible consequence in mock mode is that the tick now appears only for known
tenant profiles rather than for a third of enquirers, which is the point. The residue is D185.

**D28/D29 — e2e.** `expect(errors).toEqual([])` treats *any* console error as a failure, including
ones from third-party network calls, so the suite is green only on a good connection — it will fail
in CI for reasons unrelated to the code under test. `services-loans-team.spec.js` was failing before
the work that recorded it and has not been re-triaged since. Neither should be confused with the
`qa-location-search`/`admin-*` cluster, which **was** root-caused and fixed.

**D30 — owner-scoped mock stores keyed on mobile.** Deals/visits mock stores key on the owner's
mobile. A masked number (`98XXXXX210`) strips to a short-but-plausible digit string, so two owners
can collapse onto the same key. The durable fix is re-keying on `ownerId`, which the API already
returns (`propertyMapper.js:125`) and which the mock fixtures lack. Backend slices 4+ made
deals/offers/visits server-authoritative, so the remaining exposure is mock-mode only.

---

## 2. Standing rulings — do not re-litigate

The only permanent section. Everything here is a settled **"no"** or a deliberate choice that looks
like an oversight, so each entry exists to stop the same question being re-raised by the next
reviewer. Unlike a finished debt item (rule 3), a ruling has no artifact to be deleted into —
its whole value is that it is still written down.

**The delivered-work archive that used to sit here was removed on 2026-08-07.** It had grown to 22
long entries and was longer than the open register above it, which is a real cost: the file people
must read to know what is owed had a majority of text describing what is not. Every entry's
reasoning had already been written into the thing it changed — `VersionedEntity`, `SettingsDocument`,
`common.validation.Formats`, `DocumentUploads`, `Ids`, `NotFoundException`, `ArchitectureBoundaryTest`,
`SharedFormatsTest`, `MigrationChainTest`, and the header of `R__zz_dev_demo_data.sql` — all verified
present before deletion. Git history holds the rest.

| Item | Ruling |
|---|---|
| Records vs Lombok for DTOs | **Records.** Immutability is load-bearing for the trust model: the contact gate rests on "the mapper decides what to reveal, and that decision is final", and a setter on `PropertyResponse` turns that decision into a default. A record is also already shorter than the Lombok equivalent, so there is nothing to save. Full policy in `backend/lombok.config`. |
| A central `ValidationService` / rule engine | **No.** Consider what `OfferService.respond()` enforces: caller is buyer-or-owner (needs the JWT plus two repositories), accept/decline is owner-only, `counterAmount` is required when countering, plus the status transition table. Centralising that means injecting `Offer`, `Property`, `Deal`, `ContactRequest` and `User` repositories into the validation package — then repeating it for 32 other services. It inverts the dependency graph into a god-package and separates each rule from the transaction that makes it atomic. **A service enforcing its own invariants is not scattered validation; that is what a service is.** Format rules may move (`common.validation.Formats`); invariants stay. |
| Centralising status/vocabulary constants in one package | **No.** §7.1 stands; a shared vocabulary package would import every feature's concepts. |
| `role`/`status` as `String` not `enum` | Deliberate (`api-standards.md` §7.1). Feature-owned constants mirror the DB `CHECK` and the spec vocabulary; enums add a mapping layer and break on a spec-side value addition. |
| Duplicated `maskMobile` in two mappers | Deliberate. Both are `private` so MapStruct cannot adopt them as implicit `String→String` converters. A security rule readable in place beats one you have to go find. |
| `reportMapper` / `reviewMapper` duplicating the page-envelope unwrap | Deliberate. Every other unwrap site now calls `unwrapPage` from `services/http.js`, but those two mappers are the only modules in the seam with **no imports at all**. Pulling the transport module into a pure mapper to save four lines makes every consumer of a mapper transitively depend on `API_BASE` and token storage. The duplication is four lines with identical semantics; the coupling would be permanent. |
| `tools.jackson` import "is wrong" | It is correct. Spring Boot 4 ships Jackson 3. |
| **Should we hold deposits, or become a party to the tenancy?** | **No — and this is the one to not re-litigate.** Rescued 2026-08-08 from `docs/feature review/02` before deletion; it existed in exactly one place while an open register row (D115) asked the question it answers. Taking custody of deposits or standing between landlord and tenant converts a marketplace into a balance sheet: it makes us the principal in every dispute, exposes us to defaults we do not price, and drags in NBFC-adjacent regulation we have no licence for. That is the failure mode that broke Nestaway. **We stay the venue, never the counterparty.** Consequence for D115: deposit financing is only shippable as a *referral* to a lender who carries the risk — if it cannot be built that way, it comes off the page. |
| `pendingContactCount` / `pendingOfferCount` etc. | **Superseded 2026-08-08 — this ruling was false.** It read "client-derived from the list responses; no endpoint, no spec addition", but `GET /me/contact-requests/pending-count` shipped as D78 precisely because a client-side count over one page is wrong past the first page. Kept as a correction rather than deleted: a stale "do not re-litigate" is worse than no ruling, and someone will otherwise re-derive the client-side version. |
| Server mask format `98XXXXX210` vs mock `+91 98••• ••10` | Server format stands; the prettier rendering is client-side presentation. |
| `GET /reels` declares `page`/`size` but returns a bare array | Correct for an infinite-scroll feed — bounded input, no total needed. |
| `ReviewResponse.categories` empty-not-null | Deliberate, so clients iterate without a guard. |
| Mojibake / BOM "should just be a grep for the bad sequences" | **No — detect by round-trip.** A pattern list only ever catches damage somebody already noticed: the first repair pass used one and missed two whole families. `SourceTreeHygieneTest.noMojibakeOrBom` re-encodes each non-ASCII run to CP1252 and decodes it as UTF-8, accepting only a valid, strictly shorter result — which also leaves genuine `Café` / `पुणे` / `₹` alone. |
| Collapsing the 5 bespoke exception subclasses into one parameterised type (C5) | **No.** `AadhaarAlreadyRegistered`, `ReviewNotEligible` and friends each carry a distinct wire error code the React client branches on. Named types are greppable, testable, and impossible to get subtly wrong at the call site; collapsing them saves ~5 files and costs the one property that matters. |
| Splitting `common/web/Routes.java` because it is 591 lines | **No.** Its length is the feature — one greppable registry of every route (`api-standards.md` §2.1). Splitting it by feature reintroduces the scatter the rule exists to prevent. |
| "Classes are too long, we need to split them" | **Measured and false.** Only 2 main files exceed 500 lines, and one of those should stay long (`Routes.java`, above). The measured waste was elsewhere: a 42.7% comment ratio and test duplication — which is what D33 and the `AbstractApiTest` consolidation addressed. |
| "Just upgrade Checkstyle to the latest and turn it on" | **No — Checkstyle was deleted instead (D36, closed 2026-08-10), and the version was never the real problem.** The reassessment measured 1,307 violations from 60 rule modules, 93% of them from two rules, one of which (`SingleLineJavadoc`, 468) contradicts §10 outright. Then it ran Checkstyle again over a Spotless-formatted copy of the same tree and violations went **up, 1,307 → 1,440** — the formatter and this config disagree about braces (`RightCurlyAlone` 6 → 141) and continuation indent (`Indentation` 8 → 51). Two tools that fight is worse than one tool that decides. The residual genuinely-semantic findings were 25, across four organisation/naming rules, catching zero bugs; every real bug-catcher already reported zero. Layout is now Spotless's job, boundaries stay `ArchitectureBoundaryTest`'s. The config is deleted — `git log -- backend/config/checkstyle/` recovers it and its provenance README if this is ever revisited. |

### A note on `tasks/todo.md` unchecked boxes

A sweep of that file found **34 unticked `- [ ]` items**; the majority are *stale plan checkboxes*
from slices that subsequently shipped, not outstanding work. They were verified individually against
the source, spec and migrations before landing here. **This register — not the checkbox state in
`todo.md` — is authoritative for what remains.**
