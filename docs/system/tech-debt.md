# PuneNest Backend — Tech-Debt Register

**Single source of truth for outstanding backend work.** Items needing a *human decision* rather than
engineering time live in [`open-questions.md`](./open-questions.md) — mixing "we know what to do,
we haven't done it" with "we don't know what we want" makes a register unactionable.

## Rules

1. **An entry needs a reason, an owner and a trigger.** "Deferred" with none of those is forgetting
   with extra steps.
2. **Debt is deliberate.** A bug is not debt — fix it. Debt is a shortcut we would take again given
   the same information.
3. **A finished item is deleted, not archived.** Once the reasoning lives somewhere it will be read —
   a Javadoc, a comment beside the code, a test that fails if the rule is broken — the entry has no
   job left. Git history and `tasks/todo.md` are the archive. Two exceptions, never deleted: a
   **ruling** (§2), and a lesson with no home in code (§3).
4. **Numbers are never reused.** Deleting D19 does not free D19, so an id in an old commit can never
   resolve to a different item than it meant.
5. **This file does not schedule work.** When a slice picks an item up it goes to `tasks/todo.md`;
   the entry here is deleted when that work lands.
6. **If it needs a decision, it is not debt yet** — it goes to `open-questions.md` and returns once
   answered.

---

## 1. Open register

**19 open — 1 High, 11 Med, 7 Low.** Clusters: `design` 6, `security` 4, `quality` 3, `frontend` 2,
`payments` 1, `build` 1, `storage` 1, `style` 1. Highest number ever issued: **D218**.
Reconciliation: 19 open + 199 deleted/closed = D218.

> Recounted 2026-08-13 with the snippet below. The previous header said *17 open — 1 High, 9 Med, 7
> Low* and had been wrong since at least D216: three rows (D216, D217 and the `quality` reclassing
> that came with them) were added to the table without the arithmetic being re-run — the exact
> failure the warning underneath this paragraph was written to prevent, committed by the passes that
> came after it. Left here rather than quietly corrected, because "the header drifts" is a claim the
> file makes about itself and this is the evidence.

> **Recount before editing that header — do not assert it.** It was wrong for months because every
> pass edited the prose and not the arithmetic. Three traps, all live:
> `Get-Content` decodes this UTF-8 file as cp1252 so em-dashes never compare equal (read it with
> `[IO.File]::ReadAllLines(p, [Text.Encoding]::UTF8)`); rows contain escaped pipes
> (`buyer\|owner\|staff`) so an un-unescaped split mis-reads the priority column; and **the priority
> cell is the machine-readable field, the strike-through is decoration** — when you close a row,
> delete it (rule 3) rather than striking it and leaving the priority behind.
>
> ```powershell
> $l = [IO.File]::ReadAllLines("docs/system/tech-debt.md", [Text.Encoding]::UTF8)
> $rows = $l | Where-Object { $_ -match '^\| D\d+ \|' } | ForEach-Object { $_ -replace '\\\|','/' }
> "rows=$($rows.Count)"
> $rows | ForEach-Object { ($_ -split '\|')[-3].Trim() -replace '\*','' } | Group-Object | Sort-Object Count -Descending
> $rows | ForEach-Object { ($_ -split '\|')[-4].Trim() } | Group-Object | Sort-Object Count -Descending
> ```

**A new control re-prices every gap around it.** The sole High, D206, arrived that way: D192 shipped
per-account scoping, which made "a narrowed admin can widen itself back" mean something for the first
time; D200 closed that with maker-checker; reviewing *that* found the checker signs for a masked
record rather than a person (D206). Expect the next High to come out of whatever D206 resolves into,
rather than assuming a security feature can only move the count down.

`P` = rough priority. The trigger column is what unblocks it.

| # | Item | Area | P | Owner / Trigger |
|---|---|---|---|---|
| D4 | `CashfreePaymentGateway.createOrder` live HTTP | payments | Low | whoever obtains the merchant account |
| D11 | ~~`role` / `status` as `String`, not enums~~ **Narrowed 2026-08-12 — measured first, one vocabulary converted, the rest deliberately left.** The row said "style, Low" with no number behind it, so the first work done was to count. **30 status/role constant holders, 1,126 references across `backend/src`**; `Roles` alone is 414, and 223 `String status`/`role` declarations sit across 152 main-source files (55 in 36 entities, 84 in 62 DTO records, 84 in services and controllers), with **40 `@Query` annotations** naming a status and **zero** existing `AttributeConverter`, `@Converter` or `@Enumerated` anywhere in the tree. Converting all thirty is not a style pass, and `api-standards.md` §7.1 already says how to spend the budget — *"Revisit per-vocabulary, not wholesale."* **Converted: `ServiceRequestStatuses` → `ServiceRequestStatus`** (38 refs, 10 files), chosen because it was the one holder that had already grown behaviour a constant cannot hold: a nine-state transition table, a staff-settable subset and a terminal test, all written as static helpers taking the `String` they should have been methods on. It is now an enum with a `wire()` value, a nested `AttributeConverter` named on the column, `@JsonValue`/`@JsonCreator` on the wire, and `canTransitionTo`/`isStaffSettable`/`isTerminal` as instance methods; `toString()` returns the wire form so every interpolated message and log line is byte-identical. The old holder is **deleted** — one vocabulary, not two. `ServiceRequestStatusWireTest` pins the mapping **through the database in both directions**, and a deliberate `wire()` break was proved to turn it, plus the pre-existing `ServiceRequestFlowTest.rejectionReopensTheWork`, red. **Not converted, with reasons:** `Roles` (414) *cannot* be — §7.1 documents that the upper-case authority form is interpolated into `@PreAuthorize`, which needs a compile-time `String` constant, so an enum would leave the constants in place and add a second vocabulary beside them. `Teams` (114) and the `*Types` holders are labels with no behaviour and nothing to gain. The other **27 status holders are eligible on exactly the terms this one met — 12 of them already carry a state machine as static helpers** — but each brings its own JPQL literals and its own tests, and doing them in one batch is the wholesale pass §7.1 warns against. | style | Low | one holder per wave, when that feature is next edited; take the ones with a transition table first, and copy the `ServiceRequestStatus` shape |
| D33 | **822 `@param` lines remain, after a whole-tree hand pass on 2026-08-13 read all 844 of them and deleted 22.** Lineage: the 2026-08-07 pass amended `api-standards.md` §10 first (the durable half: without it they grow straight back) and deleted the **42** that were provably empty. **The 2026-08-09 re-measurement previously recorded in this row was wrong, and is corrected here rather than quietly replaced.** It claimed *"of the 670 remaining `@param` lines, **zero** are provably empty"*. Both halves were false: the real population on 2026-08-13 was **844 lines across 222 of 694 main-source files**, and by the row's own definition `@param furnishing   furnishing level, nullable` occurred **5 times**, `@param amenities    amenity labels, nullable` **3 times**, and `@param entityId  affected entity id` **3 times**. The lesson is the one the row was already trying to teach: a count asserted without the dump behind it is not a measurement. **What the hand pass actually did:** every one of the 844 lines was dumped with its file and line number and judged individually — deliberately no bulk regex. A line was deleted only when a competent reader could recover its description verbatim from the parameter name and it added no fact; it was kept whenever it carried units or scale, an enum vocabulary, a default or the meaning of `null`, a mapping to an external system's field, a `{@link}` worth not hunting for, a constraint the annotation does not state, or any *why*. Doubt resolved as keep. **Result: 22 deleted across 14 files, 822 kept.** *The dominant keep-reason, by a wide margin, is a single qualifier that changes the meaning of an otherwise-restated line* — `built` area (not carpet), `security` deposit (not booking), `display` locality (the slug is the filter key, not this), `opaque` id (do not parse it), `currently` rented, `server-resolved` actor, and `affected entity **type**` (not the entity). Those look like restatements at a glance and are not, which is exactly why a regex must not be pointed at this. Representative deletions: `@param furnishing furnishing level, nullable`; `@param entityId affected entity id`; `@param closedAt when the deal was closed`; `@param id row id`; and `@param lat listing latitude, nullable` — §10's own banned example, on a method whose summary already reads "with optional coordinates". Representative keeps: `@param areaUnit area unit, defaults sqft when null` (default), `@param possession possession state ({@link PropertyPossession}), nullable = not stated` (link plus the meaning of null), `@param reraId MahaRERA id` (external system), `@param joinedAt first sign-up time` next to `@param createdAt row creation time` (the *pair* is the information). **Still open, deliberately.** 822 is not a claim that all 822 lines earn their place; it is the narrower, falsifiable claim that none of them is a *provable* name-restatement any more. Verified: the diff is comments-only (0 insertions, every changed line inside a javadoc block), no BOM / no `U+FFFD` / no cp1252 mojibake in any of the 14 files, and the suite is `1480 / 0 failures / 0 errors / 3 skipped`, unchanged | quality | Low | by hand, per file, when that file is next edited — §10 now forbids adding more |
| D68 | **The Spotless whole-tree reformat has not been applied** — `spotless:apply` over `backend/src` is **612 of 755 files, 12,411 changed lines** (6,609 insertions / 5,802 deletions), broad but shallow: median 8 changed lines per file, 227 files at ≤5 lines, only 66 above 50. It is not enormous, but it is enough to bury every code review for a week, and it must not be smuggled into a feature branch alongside logic. Land it as a **standalone commit that touches nothing else**, and in that same commit add the `<executions>` block binding `spotless:check` to `validate` — the gate and the tree it requires have to arrive together or the build breaks for everyone. *Why Palantir:* measured against the two google-java-format styles on scratch copies of the same tree — `palantirJavaFormat` (4-space/120) 612 files / 12,411 lines, `googleJavaFormat` AOSP (4-space/100) 716 files / 37,859 lines, `googleJavaFormat` GOOGLE (2-space/100) 753 files / 126,407 lines. The tree is already 4-space (leading-whitespace histogram peaks at 4/8/12/16/20/24, zero tabs) and already wraps near 110 (68 lines over 110 columns, 20 over 120), so Palantir is the style this code is *already written in*. Note the reformat also deletes unused imports, so it is not purely cosmetic and needs a compile | build | Med | a quiet moment with no other backend lane in flight |
| D41 | Deleting a document leaves its object in the store — needs a bucket lifecycle rule | storage | Low | with the real object store |
| D70 | **A poster's only record of who answered is the notification stream** — the contract has no "who replied to my ad" endpoint and `share_flat_interests` is not readable over the API, so the sender's name and number exist for the poster only as a notification. Dismiss it and the lead is gone, with the row still sitting in the table | design | Med | when share-flat gets a second screen — needs a spec addition, so it could not be fixed inside the slice |
| D94 | ~~**Notification preferences have no server surface at all** — `getNotifPrefs`/`setNotifPrefs`/`inQuietHours` cover email/sms/whatsapp channels, a master `matchAlerts` switch, quiet hours and language, and every one of them lives only in localStorage. `ProfileTab.jsx` was deliberately not touched by the notification slice for that reason. The practical consequence is that quiet hours suppress *client-derived* alerts only — a server-written notification arrives at 3am regardless, because the server has never been told~~ **Closed 2026-08-12 (server half).** `notification_preferences` (V73, PK = `user_id`) + `GET/PUT /me/notification-preferences`, and the D92 writers now honour it — **at the `Notifier` port rather than in the eight writers**, so the ninth writer inherits the rule instead of having to remember it. Three decisions worth reading before changing any of this. (1) **Quiet hours defer, they do not suppress.** The inbox is the only place a notification is ever read, so dropping one at 03:00 permanently loses an offer the owner never asked to lose; the row is written with its true `created_at` and `notifications.deliver_after` carries the instant the window closes, which the inbox read compares against `now()`. No scheduler, therefore no lag, no double-fire, no leader election and no window in which a row is due but unreleased — the same argument V63 makes for deriving the verification badge rather than sweeping it. `QuietHours` owns the three edges (windows that wrap midnight, `start == end` meaning *never*, and the close being a future instant rather than a time of day), reckoned in `PlatformTime.IST` because this platform has no per-user timezone — an explicit assumption, not an oversight. (2) **An absent row is today's defaults, never silence.** Every existing account has no row, so reading missing as "all off" would mute the platform for the entire user base on deploy. (3) **`matchAlerts` is narrow on purpose** — it drops proactive `match.*`/`price.*` alerts (there is no later moment at which a user wants an alert they switched off) and does *not* touch events the user is a party to; an offer on your own listing is not an alert you opted into. **Deliberately still open:** `email`/`sms`/`whatsapp`/`language` are stored and returned but consulted by nothing, because nothing on this server sends email, SMS or WhatsApp — gating the in-app row on a switch labelled "Email" would stop a user seeing notifications in the app. They are persisted so the first real channel sender inherits an answer rather than a migration. **Also still open: the client half** — `ProfileTab.jsx` and `Notifications.jsx` still read and write localStorage through `lib/store/notifications.js` and have not been pointed at the endpoint | design | Low | closed server-side; the client wiring and a real channel sender are the two remaining halves |
| D120 | **Service-request documents are multipart to the vault and do not resolve in dev** — draft and final documents are `multipart/form-data` returning **signed URLs** that a dev backend does not serve, and there is no customer upload surface behind the tracker (sharing a draft and uploading a final are staff transitions). The seam projects `draft`/`finalDoc` from `documents[]` by category for when they exist, but a customer-created request carries neither, and the per-request **document checklist** (six named items in the mock) has no read representation on the wire. So the tracker's document column and inline `draft.dataUrl` preview are mock-only; live mode shows the thread and the status, not the paperwork | design | Med | a dev-resolvable document surface (or a checklist read endpoint) before the tracker's document UI can flip to http |
| D121 | **Co-fill service requests have no counterparty endpoint** — the mock splits a rent agreement across two mobiles (`serviceFlow.createCoFill`, `listForParty`), keyed on the *other* party's `ownerMobile`; the customer API scopes every request to its requester, so there is no party view to fetch. `listPartyServiceRequests` returns `[]` in http mode (never `undefined` — the tracker spreads it) and `useRentAgreement.js` create stays on `serviceFlow.create` because its create is a co-fill. Also here: `markServiceRequestRead` is a no-op (no read-receipt endpoint) and `changes_requested` collapses server-side to `in-progress` (the maker-checker has `approve`/`reject` only), so a rejection is unrecoverable from the read shape | design | Med | a co-fill invite endpoint (and a distinct `changes_requested` state) if the two-sided rent-agreement flow ships live |
| D123 | **The document seam is the owner's side only — the buyer's half has no faithful server mapping** — `documentService.js` covers the vault (list/upload/delete a listing's files) and the owner's request inbox (list/respond, `/me/documents/{propId}` and `/me/documents/requests`). The buyer's half cannot follow: the server is **token-mediated** (`POST /documents/requests` then `GET /documents/shared?token=`), giving a buyer no way to poll a request's status, so `DocumentsSection`/`ViewDocuments` keep reading `lib/data/documents.js` cross-user localStorage directly (never through the service). Also staying on `lib/`: the cross-user grant notification (`notifyBuyerDocsGranted`), the shared-doc count (`countSharedDocs`), the owner dashboard's per-listing doc-count badge, **rent agreements** (they overlap the already-live tenancy flow — created as a side effect in `lib/data/tenancy.js`), and every presentation helper (`DOC_CATEGORIES`, `DOC_INFO`, `docInfo`, `formatSize`, `docIcon`, checklist progress). The vault's signed-URL preview does not resolve in dev (the D120 pattern), so `toDoc` carries `url` for a viewer but the bytes only exist behind the mock's `dataUrl` | design | Med | a buyer-facing request-status read (`GET /me/document-requests` scoped to the requester) would let the buyer half join the seam; until then it is deliberately mock-only |
| D158 | **The write rate limit still counts in memory — the Redis path is written but cannot be built** — the seam landed 2026-08-12: `WriteRateLimitStore` with `InMemoryWriteRateLimitStore` (default) and `RedisWriteRateLimitStore`, selected by `punenest.ratelimit.store`, `WriteRateLimiter`'s own logic untouched. The counter is a single Lua script (`INCR`, `PTTL`, `PEXPIRE` only when the TTL is missing) rather than `INCR` + conditional `EXPIRE`, because a crash between those two strands a key at 1 with no expiry and refuses that caller **forever**. **What blocks it is dependency resolution, not design:** the offline build resolves `spring-boot-starter-data-redis` at 4.1.0 while `~/.m2` caches 3.3.5, and pinning 3.3.5 drags in `spring-data-keyvalue` 4.1.0 / `spring-oxm` 7.0.8; excluding those drags in `netty` 4.2.15 and `reactor-core` 3.8.6. Same wall the pom's AWS-SDK comment documents. So `RedisEval` is deliberately unimplemented and `store=redis` **fails fast at boot** rather than silently counting in memory. A Redis *outage* at runtime does the opposite — falls back to in-memory and logs ERROR — because refusing every write during a cache blip is worse than a temporarily generous limit; the asymmetry is between a misconfiguration (loud, at boot, fixable) and an incident (degrade, keep serving). **Re-verified 2026-08-12 and still blocked — but two specifics above are now wrong, which is the reason this note exists.** (1) `netty` 4.2.15.Final **is** cached; it stopped being a blocker at some point and the row never noticed, so anyone re-testing this by re-reading the prose would chase a wall that has moved. (2) The live blockers are three artifacts, each confirmed absent by `mvnw -o dependency:get`: `org.springframework.boot:spring-boot-starter-data-redis:4.1.0`, `io.lettuce:lettuce-core:7.5.2.RELEASE` (Boot 4.1.0's managed version; cache has 6.3.2.RELEASE) and `io.projectreactor:reactor-core:3.8.6` (via `reactor-bom` 2025.0.6; cache tops out at 3.8.1). `redis.clients:jedis` is absent entirely, so there is no second client to reach for either. The **online** trigger has also not fired: the same `dependency:get` without `-o` fails with a connect timeout to the configured mirror, so "the first time the build runs online" has not happened yet. Nothing was faked to get past this — no pom pin, no hand-rolled RESP client on a security control — and the trigger below is now a command rather than a paragraph | security | Med | run `mvnw dependency:get -Dartifact=org.springframework.boot:spring-boot-starter-data-redis:4.1.0`; when that succeeds, `RedisEval` is ~5 lines over `RedisTemplate.execute`. Re-test with the command, not by re-reading the prose — one of its claims has already gone stale |
| D176 | **`V27__flatmates.sql` still describes its unique index backwards — and the correction has already shipped; only V27's own text is outstanding.** Verified 2026-08-11 against both live databases with `\d+`: `V54__drop_dead_transactions_owner_index.sql` carries the argument in its header *and* a live `COMMENT ON INDEX uq_flatmate_requests_target_requester`, and `FlatmateRequestRepository`'s javadoc carries the "V27's comment on that index is wrong; do not believe it" paragraph. So a reader who reaches the index from the schema or from the code is told the truth; only a reader who opens V27 directly is not. The dead-index half was closed by V54 (`owner_id` is the index's *leading* column and no query shape constrains it, `Transaction.getOwnerId()` has zero call sites, ownership resolves through `properties.findByIdAndOwner_Id`). The surviving prose error: V27 calls the interest endpoints "idempotent" (they refuse a duplicate with 409) and says the index "is the reason the service does not check-then-insert" (both write paths do exactly that, and did before D175 moved the read behind the advisory lock). Only its third claim survives, and only as the residual case — the lock closes the race under READ COMMITTED, so the index is the backstop for a higher isolation level and the reason a double press yields 409 rather than 500. **This row cannot be actioned on demand**: Flyway runs with the default `validateOnMigrate=true` and V27 is applied to both databases, so editing it moves its checksum and breaks every applied database on next boot | quality | Low | **not open work — parked until the databases are next rebuilt from empty.** At that moment, and only then: rewrite V27's comment, and delete the then-duplicate section from V54's header |
| D206 | **The second key signs for a record, not a person** — D200 stops one administrator minting a colleague alone, and a security review of it (2026-08-11) found the rule holds mechanically: there is no way to obtain a usable administrator without a second human. What that second human is *attesting* is the weak part. The maker supplies the new account's `mobile` at creation — no longer its password, see below — and the checker is still shown almost nothing to judge by: `pendingApprovals()` returns `masked(user)`, and `UserResponse` carries no `createdBy` at all — so the approval screen offers a name, an email, a role, and a mobile with most of its digits removed, with **no indication of who is asking**. A narrowed admin holding `users:write` therefore mints an admin account on a mobile *they* control, asks any unrelated colleague to clear the queue, and signs in as an account with no grant row and thus the full role baseline — which is D200's original attack, executed with a second signature attached. The audit log reads as ordinary onboarding, because that is exactly what it is, apart from who holds the handset. Two halves, and they are separable. **The credential half shipped 2026-08-12:** `staff_invites` (V71) + `StaffInviteService` + `POST /auth/staff-invite/redeem` — `addStaff` now creates the account with **no usable password** and issues a single-use, time-limited invite (`<id>.<secret>`, stored as `sha256(secret)`, compared with `MessageDigest.isEqual`) to the colleague's own handset; neither administrator ever learns the token, `created_by` is audit-only, and an open invite blocks authentication on **every** login path. That is what makes the approval capable of meaning "this *person* may have access" — though note it does **not** by itself defeat the attack above, because an invite sent to a handset the maker controls is still redeemed by the maker. **The attestation half is still open:** surfacing `createdBy` and the unmasked mobile on the pending-approval response is small, and is the part that would let a checker notice the number belongs to the maker — as of today `UserResponse` still carries no `createdBy` and `pendingApprovals()` still returns `masked(user)`. **That remaining half is not free of a counter-argument**: `pendingApprovals` masks deliberately, and its own note explains why — a queue listing one unmasked number per waiting colleague is "a small bulk-export surface wearing the clothes of a to-do list". So this is a real trade of one privacy property against one integrity property, which is why it is a row and not a commit. It is currently unobservable in the product for the same reason as D205 — there is no approval screen yet — which is the argument for ruling on it **before** one is built rather than retrofitting it | security | High | needs a product ruling — see `open-questions.md`; decide before the D205 console is built |
| D204 | **A revoked stay does not retract the review it authorised** — D194 lets an owner confirm a self-declared tenancy and revoke it again, and `PropertyExperienceService.standingOf` reads the live status, so revocation closes the composer immediately. It does not reach backwards. `ReviewService.createForProperty` freezes the standing into the row's `context` badge at write time, so *confirm → publish a "Tenant" review → revoke* leaves a published review wearing a badge whose evidence has been withdrawn, and the only party who can withdraw it is the one the review is about. Both directions are defensible and neither is obviously right: retracting on revoke hands an owner a delete button for criticism, which is the outcome the contact gate and the eligibility rule exist to prevent; leaving it standing means the badge outlives its proof. A third option — keep the review, drop the badge to `NONE`, and say so on the card — is probably the honest one but is a **product ruling, not an implementation detail**, which is why this is a row and not a commit. Related and deliberately not fixed: nothing detects an owner confirming an alt account they control, so a determined lister can mint one tenant-badged review about themselves. The declaration carries `owner_id`, `declarant_id` and a `decided_at`, and every decision is written to the audit log, so this is *detectable after the fact* by the ops side; it is not *prevented*, and preventing it needs an identity signal the platform does not have | security | Med | needs a product ruling first — see `open-questions.md` |
| D213 | **`flatNumber` on an anonymous room read is a precise door number** — kept by D80 because the reader is real: `flatKeyOf` uses `'addr:' + society + '\|' + flatNumber` as the client-side flat-ledger key. But it is used only as a *grouping key* — nothing renders it — while the wire publishes it unauthenticated alongside `society`, `lat`, `lng`, `gender` and `occupancy`. `PropertySummary`, the platform's other anonymous list, carries coordinates but deliberately no unit number, so "Flat B-1204, Skyline Heights, women-only, currently empty, at these coordinates" is available to any unauthenticated scraper on the flatmate board and nowhere else. For a flat-sharing product that is a physical-safety exposure, not only a privacy one. The consumer needs an opaque *identity*, not the address: mint a server-side `flatKey` — `propertyId` when present, else a truncated HMAC of the normalised `society\|flatNumber` under a server secret — have `flatKeyOf` prefer it, and keep `flatNumber` on the host's own `FlatmateRoomDto`. Ledger grouping is unchanged and the room stops publishing its door. `lat`/`lng` alone are fine (`PropertySummary` sets that precedent, and split rooms copy them from an already-public parent); it is the combination with the unit number that earns the row. **Re-scoped 2026-08-12 — the frontend half is done, this is now server-only.** The frontend lane confirmed it cannot close this alone and did not try: the exposure is the *server* publishing the field, and dropping `flatNumber` from the client would not unpublish it while breaking the ledger for exactly the posts that need it — tenant-hosted rooms have no `propertyId`, so they would fall back to `'room:' + id`, stand alone, and lose the shared occupancy ceiling. A client-side hash is not a substitute either: society × flat number is a tiny input space, so any digest the client could compute is brute-forceable back to the door. Instead `flatKeyOf` now **prefers** `r.flatKey` ahead of `propertyId` and the `addr:` fallback, and `http/flatmateMapper.js` passes `flatKey` through. Both are inert until the server sends the field, so the server can mint it and drop `flatNumber` from the anonymous read in one deploy with no coordinated frontend change. What the server must mint: a stable opaque `flatKey` on `FlatmateRoomFeedDto` — the `propertyId` string when there is one, else a truncated HMAC of the normalised `society\|flatNumber` under a server-held secret — identical for sibling rooms in one flat and for nobody else, with `flatNumber` kept only on the host's own `FlatmateRoomDto` | security | Med | server-only now: mint `flatKey`, then drop `flatNumber` from the anonymous read |
| D214 | **The bundle-size ratchet has gone slack and should be re-tightened.** `check-bundle-size.mjs` sets `BUDGET_KB = 497`, chosen by D203 against a measured 492.9 KB — a deliberately uncomfortable ~4 KB of headroom, on that file's own stated principle that *"a budget that is never tightened is a budget that has stopped working"*. D208 then made `config.js`'s provider glob lazy, which removed 58 providers (and the mappers and mock data they reach) from the entry preload set the gate measures, and the number fell to **437.6 KB**. So the ratchet now permits ~59 KB of unnoticed growth — enough to re-land the whole eager-glob regression without the gate saying a word. Not folded into D208 because tightening a shared budget while the tree carries several lanes' uncommitted work would fail *their* builds for something none of them did | frontend | Med | re-measure on a clean tree, then set `BUDGET_KB` to the new number plus a few KB |
| D216 | **An archived user reports `status: "active"` over the API, and the wire carries no archival flag at all.** Two separate facts that only bite in combination. (1) `SoftDeleteEntity.archive(reason)` sets `archived`, `archivedAt` and the reason; it never touches the `status` column, which is a different field with its own vocabulary (`active`/`suspended`) and stays wherever it was. The single place `status` is ever set to `"archived"` is inside `User.erasePersonalData(...)`, which is DPDP erasure, not archival. (2) `UserResponse` publishes `status` but has **no `archived` field**, so a client holding a `UserResponse` cannot tell an archived account from a live one — it is told, positively and wrongly, that the account is active. Confirmed against `punenest_e2e` on 2026-08-13: across 84 rows, `status = 'archived'` occurs **zero** times while two rows carry `archived = t`, both of them reporting `active`. The guard itself is sound — the D200 restore-collision refusal works, and the archived row stays archived — so this is a *reporting* defect, not an authorisation one; nothing archived is reachable, it just does not look archived. The operator-visible consequence is the one that matters: a back office reading `status` shows an archived staffer as active, which is precisely the screen an administrator would use to confirm the archive took. The read path that *is* correct is `GET /users?archived=true`, which filters on the real column and is what `live-user-restore-email-collision.spec.js` now asserts through — that spec previously asserted `status === 'archived'` and was asserting on this bug. Two candidate fixes and they are not equivalent: adding `archived` to `UserResponse` is additive and safe; moving `status` inside `archive()` changes a column with a CHECK constraint and an existing vocabulary, and would need V-migration + a sweep of every `status` reader | design | Med | with the admin slice — the same lane that owns `/staff-login`, since both are the back office reading a half-migrated contract |
| D215 | **The lazy provider glob costs one extra round-trip in front of each domain's first request, unmeasured.** `vite.config.js`'s `manualChunks` names only vendor libraries, so nothing groups the providers and Rollup emits roughly one chunk each — 44 of them in the D208 build. On a cold route the sequence is now `import()` the provider chunk *then* `fetch()`, on top of an already-lazy route chunk. The shell got 57.9 KB cheaper and the data path got a serialised hop; on mobile 4G that trade may be net-negative for TTI even though the byte count improved. Raised by `code-reviewer` on D208 as a trade to make deliberately rather than by omission. Grouping the providers into one or two chunks, or firing `provider()` unawaited when the route chunk loads, are both cheap — but neither should be done before the hop is measured, or it is guesswork replacing guesswork | frontend | Low | measure first: cold-route TTI on a throttled profile, before vs after grouping |

| D217 | **`punenest_test` is not empty between runs — `audit_log` had accumulated 12,267 rows, and the cleanups written to prevent it clean nothing.** Two compounding facts. (1) Audit writes run `REQUIRES_NEW` and therefore commit, so `AbstractApiTest`'s class-level `@Transactional` rollback never reaches them — which `AbstractApiTest`'s own javadoc already documents as the standing exception. (2) The `@AfterEach` cleanups written in response to that are **silent no-ops wherever the class is `@Transactional`**: Spring's `TransactionalTestExecutionListener` ends the test transaction *after* `@AfterEach` runs, so a plain `jdbc.update` there joins the doomed transaction and is rolled back with everything else. The two classes whose deletes genuinely work — `FlatmateInterestRaceTest` and `FlatmateDuplicateInterestRaceTest` — work only because they deliberately are *not* `@Transactional`, for an unrelated reason (a second connection must see their writes). So `AdminSettingsDeadKeyTest`, `FlatmateModerationGateTest` and `ServiceRequestIdentityTest` each carry a cleanup that has never removed a row. **Per-class patching is not the fix**: 12k rows is far more than three classes produce, because most audit writes across the 1483-test suite have no cleanup at all. Note the trap for anyone attempting one — making the delete commit means its own `REQUIRES_NEW`, which means a second connection, which then blocks forever on the row locks the still-open test transaction holds; Postgres sets no lock timeout, so the symptom is Maven going silent mid-suite, not an error. `AuthEndpointsTest` hit exactly this and now uses a static `@AfterAll` with a `DataSource` captured from `@BeforeEach`, which is the only per-class shape that works. Truncated by hand 2026-08-13; the `users` half is closed (that was `provisionBuyer`'s `REQUIRES_NEW` insert, four fixed mobiles, fixed at source) | quality | Med | one suite-wide sweep — a `TestExecutionListener` registered via `META-INF/services` truncating `audit_log` on `testPlanExecutionFinished` — rather than N per-class teardowns; then delete the three no-op `@AfterEach` blocks so they stop reading as protection |

**Closed and deleted (rule 3).** Ids never return (rule 4). Recent closures and their reasoning are in
`tasks/todo.md` under **Shipped**; everything older is in git history.

### Lessons kept because they have no home in code

These outlived their entries and belong to no single file, so they stay here rather than being lost
with the row that taught them.

- **A `@Transactional` test harness cannot see a bug that happens at commit time.** D90 survived 750
  tests because every HTTP test rolls back; the guard had to be written outside the harness that hid it.
- **A mock more permissive than its server silently passes tests the real thing would fail** (D97d).
  Worth re-checking the other mock providers against their repositories for the same asymmetry.
- **An assertion that cannot fail is worse than no test** — it reads as coverage. D95's one-shot-perk
  test passed with the guard deliberately disabled.
- **Consolidating filters must merge them first.** The local console-noise lists were not subsets of
  the shared one, so pointing everything at it would have silently *tightened* several specs.
- **Measure before scheduling, re-measure before deleting.** Four of nine items in the 2026-08-07 pass
  were filed with the wrong size or cause; the register's own numbers were the least reliable thing in it.

---

## 2. Standing rulings — do not re-litigate

The only permanent section. Each entry is a settled **"no"** or a deliberate choice that looks like an
oversight, and exists to stop the same question being re-raised. Unlike a finished debt item (rule 3),
a ruling has no artifact to be deleted into — its whole value is that it is still written down.

| Item | Ruling |
|---|---|
| Records vs Lombok for DTOs | **Records.** Immutability is load-bearing for the trust model: the contact gate rests on "the mapper decides what to reveal, and that decision is final", and a setter on `PropertyResponse` turns that decision into a default. A record is also already shorter than the Lombok equivalent, so there is nothing to save. Full policy in `backend/lombok.config`. |
| A central `ValidationService` / rule engine | **No.** Consider what `OfferService.respond()` enforces: caller is buyer-or-owner (needs the JWT plus two repositories), accept/decline is owner-only, `counterAmount` is required when countering, plus the status transition table. Centralising that means injecting `Offer`, `Property`, `Deal`, `ContactRequest` and `User` repositories into the validation package — then repeating it for 32 other services. It inverts the dependency graph into a god-package and separates each rule from the transaction that makes it atomic. **A service enforcing its own invariants is not scattered validation; that is what a service is.** Format rules may move (`common.validation.Formats`); invariants stay. |
| Centralising status/vocabulary constants in one package | **No.** §7.1 stands; a shared vocabulary package would import every feature's concepts. |
| `role`/`status` as `String` not `enum` | **Amended 2026-08-12 (D11) — still the default, now with one stated exception.** For a vocabulary that is *only* a vocabulary the original ruling stands: feature-owned constants are simultaneously the DB `CHECK` values and the API surface, and an enum adds a mapping layer that breaks on a spec-side value addition. **The exception is behaviour.** When a vocabulary grows a transition table, a settable subset or a terminal test, that behaviour has to live somewhere, and static helpers taking a `String` are an enum written badly — the compiler cannot stop you passing the wrong `String` to any of them. `ServiceRequestStatus` is the worked example and the shape to copy: a `wire()` value keeps the column and the JSON byte-identical, an `AttributeConverter` named on the field does the mapping (**never `@Enumerated`** — it writes `IN_PROGRESS` into a column whose CHECK wants `in-progress`), `@JsonValue`/`@JsonCreator` do the same at the wire, `toString()` returns the wire form so interpolated messages do not drift, and the transition table becomes instance methods. Convert one vocabulary at a time and delete the old holder in the same change. `Roles` is the standing counter-example and stays constants whatever behaviour it grows: the upper-case authority form goes inside `@PreAuthorize`, which needs a compile-time constant. |
| Duplicated `maskMobile` in two mappers | Deliberate. Both are `private` so MapStruct cannot adopt them as implicit `String→String` converters. A security rule readable in place beats one you have to go find. |
| `reportMapper` / `reviewMapper` duplicating the page-envelope unwrap | Deliberate. Every other unwrap site now calls `unwrapPage` from `services/http.js`, but those two mappers are the only modules in the seam with **no imports at all**. Pulling the transport module into a pure mapper to save four lines makes every consumer of a mapper transitively depend on `API_BASE` and token storage. The duplication is four lines with identical semantics; the coupling would be permanent. |
| `tools.jackson` import "is wrong" | It is correct. Spring Boot 4 ships Jackson 3. |
| **Should we hold deposits, or become a party to the tenancy?** | **No — and this is the one to not re-litigate.** Rescued 2026-08-08 from `docs/feature review/02` before deletion; it existed in exactly one place while an open register row (D115) asked the question it answers. Taking custody of deposits or standing between landlord and tenant converts a marketplace into a balance sheet: it makes us the principal in every dispute, exposes us to defaults we do not price, and drags in NBFC-adjacent regulation we have no licence for. That is the failure mode that broke Nestaway. **We stay the venue, never the counterparty.** Consequence for D115: deposit financing is only shippable as a *referral* to a lender who carries the risk — if it cannot be built that way, it comes off the page. |
| `pendingContactCount` / `pendingOfferCount` etc. | **Superseded 2026-08-08 — this ruling was false.** It read "client-derived from the list responses; no endpoint, no spec addition", but `GET /me/contact-requests/pending-count` shipped as D78 precisely because a client-side count over one page is wrong past the first page. Kept as a correction rather than deleted: a stale "do not re-litigate" is worse than no ruling, and someone will otherwise re-derive the client-side version. |
| Server mask format `98XXXXX210` vs mock `+91 98••• ••10` | Server format stands; the prettier rendering is client-side presentation. |
| `GET /reels` declares `page`/`size` but returns a bare array | Correct for an infinite-scroll feed — bounded input, no total needed. |
| `ReviewResponse.categories` empty-not-null | Deliberate, so clients iterate without a guard. |
| Mojibake / BOM "should just be a grep for the bad sequences" | **No — detect by round-trip.** A pattern list only ever catches damage somebody already noticed: the first repair pass used one and missed two whole families. `SourceTreeHygieneTest.noMojibakeOrBom` re-encodes each non-ASCII run to CP1252 and decodes it as UTF-8, accepting only a valid, strictly shorter result — which also leaves genuine `Café` / `पुणे` / `₹` alone. |
| Collapsing the 5 bespoke exception subclasses into one parameterised type (C5) | **No.** `AadhaarAlreadyRegistered`, `ReviewNotEligible` and friends each carry a distinct wire error code the React client branches on. Named types are greppable, testable, and impossible to get subtly wrong at the call site; collapsing them saves ~5 files and costs the one property that matters. |
| Splitting `common/web/Routes.java` because it is 591 lines | **No.** Its length is the feature — one greppable registry of every route (`api-standards.md` §2.1). Splitting it by feature reintroduces the scatter the rule exists to prevent. |
| "Classes are too long, we need to split them" | **Measured and false.** Only 2 main files exceed 500 lines, and one of those should stay long (`Routes.java`, above). The measured waste was elsewhere: a 42.7% comment ratio and test duplication — which is what D33 and the `AbstractApiTest` consolidation addressed. |
| "Just upgrade Checkstyle to the latest and turn it on" | **No — Checkstyle was deleted instead (D36, closed 2026-08-10), and the version was never the real problem.** The reassessment measured 1,307 violations from 60 rule modules, 93% of them from two rules, one of which (`SingleLineJavadoc`, 468) contradicts §10 outright. Then it ran Checkstyle again over a Spotless-formatted copy of the same tree and violations went **up, 1,307 → 1,440** — the formatter and this config disagree about braces (`RightCurlyAlone` 6 → 141) and continuation indent (`Indentation` 8 → 51). Two tools that fight is worse than one tool that decides. The residual genuinely-semantic findings were 25, across four organisation/naming rules, catching zero bugs; every real bug-catcher already reported zero. Layout is now Spotless's job, boundaries stay `ArchitectureBoundaryTest`'s. The config is deleted — `git log -- backend/config/checkstyle/` recovers it and its provenance README if this is ever revisited. |

### A note on `tasks/todo.md` unchecked boxes

Unticked `- [ ]` items there are mostly *stale plan checkboxes* from slices that subsequently shipped.
**This register — not the checkbox state in `todo.md` — is authoritative for what remains.**
