# Flow: Enquiries & Deals Funnel

> The admin demand desk: every buyer/tenant enquiry, scheduled site visit, and closed deal, plus a
> conversion funnel (Enquiry -> Visit -> Deal) with GMV and per-locality breakdown.
> **Status:** documented from React source - **Primary role(s):** admin (atom `enquiries:read`)

---

## 1. Purpose & user problem
- **Persona:** an operations / growth lead watching demand health and conversion.
- **Job-to-be-done:** "Track incoming leads, move them through their stages, and see how many
  enquiries become visits and deals - and where the revenue comes from."
- **Why it matters:** this is the funnel view of the whole marketplace - top-of-funnel demand
  (enquiries) through the mid-funnel (visits) to the bottom (closed deals / GMV). It quantifies the
  conversion that [`property-verification.md`](./property-verification.md) supply feeds.

## 2. Entry points
- **Routes:** `/admin/enquiries`. Tabs: `enquiries`, `visits`, `deals`, `funnel`.
- **Tiles / triggers:** four KPI tiles (Enquiries, Open leads, Site visits, Deal GMV) that jump to
  their tab; per-row actions; the Funnel tab visualisation.
- **Source components:**
  - `src/pages/admin/AdminEnquiries.jsx` - tabs, KPIs, tables, row actions, reschedule modal.
  - `src/pages/admin/enquiries/FunnelView.jsx` - funnel + conversion metrics + locality breakdown.
  - `src/pages/admin/enquiries/helpers.js` (`updateCollection`) and `constants.js` (status/type opts).

## 3. Actors & roles
- **Operator = admin.** `ModuleRoute moduleKey="enquiries"` (atom `enquiries:read`) inside
  `RoleRoute roles={['admin']}` (`src/App.jsx`). `manager` was retired with the custom roles that
  labelled it (D209); ops staff hold `enquiries:read` too, but it widens their API reach rather than
  opening the admin shell.
- **Subjects** are records generated by consumer activity (buyers/tenants enquiring, scheduling
  visits; deals closed on properties). The control is `@PreAuthorize` on each route, not the guard.

## 4. Entities touched
- [`enquiries`](../../system/data-model.md) - **read** and **updated** (`status`).
- [`visits`](../../system/data-model.md) - **read** and **updated** (`status`, `when`). Gated by
  the `enquiries.visits` flag.
- [`deals`](../../system/data-model.md) (analytics/admin feed) - **read** only (value + status).
  Gated by the `enquiries.deals` flag.
- [`audit_log`](../../system/data-model.md) - **created** on each enquiry/visit mutation.

## 5. Business rules & logic  *(the meat)*

### 5.1 Loading & tab data
`reload()` fetches `listEnquiries()` always, and `listVisits()` / `listDeals()` only when their flags
are enabled (`src/lib/mockApi/collections.js`). The active tab picks the base list; `rows` applies
status filter, type filter (`kind` for enquiries, `deal` for deals), free-text search
(`JSON.stringify(r).includes(q)`), and a date-range cutoff over `at`/`when`.

### 5.2 KPIs
`kpis` memo:
- **Enquiries** = total enquiry count.
- **Open leads** = enquiries with `status` in {`new`, `open`}.
- **Site visits** = visit count.
- **Deal GMV** = sum of `deal.value`.

### 5.3 Funnel stages & status vocab
Records progress through operational statuses (`enquiries/constants.js`):
- **Enquiry `status`:** `new -> open -> responded -> closed`. Type (`kind`): `call`, `chat`, `visit`
  (seed also uses `contact`).
- **Visit `status`:** `scheduled -> completed | cancelled` (with reschedule keeping it `scheduled`).
- **Deal `status`:** `in_progress -> closed`.

### 5.4 Row actions (state transitions)
`act(col, id, patch, msg, type)` = `updateCollection(col,id,patch)` + `logAudit('Enquiries', msg)` +
`reload()` + toast.
| Tab | Action | Guard | Patch |
|-----|--------|-------|-------|
| enquiries | Mark responded | status `new`/`open` | `{ status: 'responded' }` |
| enquiries | Close | status `new`/`open` | `{ status: 'closed' }` |
| visits | Completed | not completed/cancelled | `{ status: 'completed' }` |
| visits | Reschedule | not completed/cancelled | `{ when: formatWhen(date, time, mode) }` |
| visits | Cancel | not completed/cancelled | `{ status: 'cancelled' }` |
| deals | View | - | read-only detail modal |

### 5.5 Assignment (current state)
- The admin enquiries desk has **no assignee field today** - enquiries/visits are triaged by status,
  not routed to a named owner within this page. This is an honest gap to close server-side.
- The nearest existing "assignment"-like feature is the **owner-side** lead desk
  (`src/lib/leadNotes.js`): per-lead private `note` + `followUpAt`, keyed by owner digits in
  `puneNestLeadNotes:<ownerDigits>`. That is a consumer/owner CRM affordance, not admin routing.
- Service tickets (a different flow) carry a `team`; enquiries do not. A future backend should add
  `assignedTo` / `owner` to enquiries for a real lead-assignment funnel.

### 5.6 Conversion metrics (`FunnelView.jsx`)
Computed client-side over the (deal + date) filtered sets:
- `enqCount`, `visitCount`, `dealCount` (deals with `status === 'closed'`), `dealGMV` (sum of value).
- **Rates:** `enqToVisit = round(visit/enq * 100)`, `visitToDeal = round(deal/visit * 100)`,
  `enqToDeal = round(deal/enq * 100)`; each guards divide-by-zero (0 when denominator is 0).
- **Revenue per enquiry** = `round(dealGMV / enqCount)`.
- **Locality breakdown:** locality is parsed from the listing title via `split(' in ')[1]` (falls
  back to "Unknown"); rows aggregate enquiries/visits/deals/GMV and a per-locality conversion %,
  sorted by enquiries, top 10.
- **Filters:** deal (`buy`/`rent`, matched on `deal` or `kind`) and a date-range cutoff (`inRange`).

### 5.7 What MUST move server-side
- Funnel aggregation, conversion rates, GMV, and locality rollups (currently recomputed in the browser
  over full in-memory lists).
- Lead assignment/routing (not modeled yet).
- The fragile locality parse (`split(' in ')`) should be replaced by the real `localitySlug` join.

## 6. Maker-checker / approval
- **Applicable: no** for the admin funnel itself - enquiry/visit status updates are single-actor
  operational tracking, not a propose/approve gate.
- The upstream **visit request** is a maker-checker elsewhere (buyer proposes a slot, owner confirms /
  cancels via `PATCH /visit-requests/:id/status`); by the time a visit appears in this admin desk it
  is already a confirmed record the operator maintains. See
  [`../../system/cross-cutting.md`](../../system/cross-cutting.md) section 2 for the shared pattern and
  [`../consumer/schedule-visit.md`](../consumer/schedule-visit.md) for the maker-checker on visits.

## 7. State machine
- **Enquiry:** `new -> open -> responded -> closed` (close is terminal from the desk; actions only
  offered while `new`/`open`).
- **Visit:** `scheduled -> completed | cancelled`; reschedule updates `when` and keeps it
  `scheduled`. `completed`/`cancelled` are terminal (no further actions).
- **Deal:** `in_progress -> closed` (read-only here; closed deals feed GMV and the funnel).

## 8. Edge cases, validation & error states
- **Loading:** `<Loading />` until enquiries resolve.
- **Empty:** table empty state ("Nothing matches this filter.").
- **Flag-gated tabs/KPIs:** Visits and Deals tabs + their KPIs are hidden unless `enquiries.visits` /
  `enquiries.deals` are enabled; `reload` returns empty arrays for disabled domains.
- **Reschedule validation:** Save is disabled until both a date and time are chosen; the date field
  enforces `min = today`.
- **Divide-by-zero:** every conversion rate returns 0 when its denominator is 0.
- **Locality "Unknown":** titles without " in <locality>" bucket into "Unknown".
- **Concurrency:** shared store, last write wins.
