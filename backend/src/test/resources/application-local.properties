# The only place the demo seed can be turned off for the suite: a profile-specific file outranks a
# plain one regardless of source set, and 126 assertions are exact row counts.
spring.flyway.locations=classpath:db/migration
