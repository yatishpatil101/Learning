/**
 * Identity &amp; Access — authentication, sessions, user profiles, RBAC, identity verification badge.
 * Rank-0 boundary: may import only {@code common}, {@code security}, {@code provider}; no feature contexts.
 */
package com.draazy.api.identity;
