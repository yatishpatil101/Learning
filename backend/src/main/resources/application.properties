spring.application.name=punenest-api

# Cloud Run (platform-architecture.md §6.1) injects PORT and requires the container to listen on it;
# a container that ignores it fails its health check and the revision never receives traffic. Cloud
# Run's own default happens to be 8080, so a hardcoded 8080 worked by coincidence — right up until
# someone sets a container port, at which point the deploy fails with a healthy-looking process
# listening on the wrong socket. The default keeps `mvn spring-boot:run` and the Vite proxy on 8080.
server.port=${PORT:8080}

# Every route in Routes.java is written relative to this prefix, and both api-standards.md §2 and
# Routes.java's own header have always said so. Until now nothing set it: the backend actually served
# `/auth/login`, and the arrangement only worked because the Vite dev proxy stripped `/api` before
# forwarding. That made the mismatch invisible in dev and fatal at deploy — pointing VITE_API_BASE at
# a real host would have requested `https://host/api/auth/login` against a server mapped at
# `/auth/login`, 404ing every call with nothing in the logs to explain it.
#
# Note this moves the infrastructure paths too: health is `/api/actuator/health` and Swagger UI is
# `/api/docs`. Deployment probes must use the prefixed paths.
server.servlet.context-path=/api

# Actuator: expose health + info for readiness/liveness probes.
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

# Local-development datasource. Every value is an env-overridable default so a developer can clone,
# start Postgres and boot — while CI/deployments override via DB_URL/DB_USER/DB_PASSWORD. The prod
# profile re-declares these as bare ${ENV} lookups so a missing secret fails the boot rather than
# silently falling back to these well-known local values.
#
# Deliberately NOT `punenest_test`: that database belongs to the test suite, which asserts exact
# row counts and so requires a schema Flyway built from empty. Sharing one database makes seeded
# dev data fail the tests. Dev data lives here; `punenest_test` stays throwaway.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/punenest}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

# Flyway owns the schema; Hibernate only validates the mapping against it, so a green boot proves
# the entities still match the migrations.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate

# Open Session In View, off (D54).
#
# Spring leaves this ON by default and logs a warning on every boot asking to be told explicitly,
# which is a warning worth reading rather than silencing: with OSIV the persistence context stays
# open for the whole request, so a lazy association touched during serialisation quietly issues a
# query from the view layer. That is how a mapper that looks like field copying turns into an N+1
# nobody can see in the service, and it only ever fires in the shape the JSON happens to take.
#
# Off is safe here because no controller returns an entity: every handler maps to a DTO inside the
# service's transaction, which is where the fetch decision belongs and can be reviewed. The cost of
# being wrong is loud rather than silent — a missed fetch is a LazyInitializationException on the
# endpoint that needs it, not a slow page in production — and the whole API suite runs against this
# setting, so the endpoints are the proof.
spring.jpa.open-in-view=false

# Flyway locations. Migration-only here (D147/D155).
#
# `db/migration` is the schema and runs everywhere. `db/seed` holds the demo data and is declared in
# application-dev.properties, not here.
#
# Both overrides that narrow this are load-bearing and both are enforced:
#   * application-prod.properties resets it to migration-only -- prod must never see demo data, and
#     profiles are not exclusive, so a `dev,prod` activation has to resolve there too.
#     ProdProfileContractTest fails if that slips.
#   * src/test/resources/application-dev.properties does the same for the test run, where 126
#     assertions are exact counts. TestDatabaseIsolationTest fails if that slips.
#
# The demo seed used to be listed on this line, because a local run activated no profile at all and
# the seed would otherwise never load. That is no longer true — the dev-only beans are opted into by
# the `dev` profile, so every documented local launch names it — and leaving the seed here made this
# a denylist: only `prod` narrowed it, so a container called `staging`, `preview` or nothing at all
# would run R__zz_dev_demo_data.sql and put 78 fabricated users and 38 fabricated listings into a
# live catalogue, indistinguishable from real inventory to a buyer. `R__` is repeatable, so it would
# do it again on every deploy where the checksum moved. The seed now lives in
# application-dev.properties, where naming the profile is the act that asks for it.
#
# Listing `classpath:db/migration` explicitly is required, not decorative: setting this property
# replaces Flyway's default location rather than adding to it.
spring.flyway.locations=classpath:db/migration

# Page-size ceiling, matching the contract's `Size` parameter (`maximum: 100`).
#
# Spring's own default is 2000, which on a public unauthenticated endpoint such as `GET /properties`,
# `/societies` or `/reels` is a free amplification: one request, 2000 rows plus their joins. The
# contract already says 100, so the server was simply not enforcing a limit it had published.
# Set globally rather than per-controller — a cap that has to be remembered at every call site is a
# cap that will eventually be forgotten at one.
spring.data.web.pageable.max-page-size=100
spring.data.web.pageable.default-page-size=20

# Dev-only HS256 signing key (must be >= 32 bytes). Prod overrides with a bare ${JWT_SECRET}.
punenest.security.jwt.secret=${JWT_SECRET:dev-only-hs256-secret-key-that-is-long-enough-32b}

# Static OpenAPI contract is served from classpath:/static/openapi/punenest-api.yaml
# Swagger UI renders it at /docs (see OpenApiDocsConfig).

# --- Container access log (tech-debt D42) ----------------------------------------------------
# The access log is OFF (Spring Boot's default), and this line only takes effect if someone turns
# it on. It is set now precisely because that is the moment the mistake gets made: Tomcat's stock
# `common`/`combined` patterns log `%r`, the raw first line of the request — which for
# `GET /documents/shared?token=…` writes a live bearer credential to a file, one that grants an
# anonymous reader somebody's title deeds for the next seven days. Whoever enables the log will be
# debugging something at the time and will not be thinking about that.
#
# `%m %U %H` is `%r` minus the query string: same method, same route, same protocol, no parameters.
# Losing the query string costs some debugging detail on filtered reads; the alternative costs a
# leaked secret in a file that is by design copied to a log pipeline and kept for months.
#
# Application-level logging has the same obligation and no pattern to enforce it — nothing in this
# codebase logs a request URI today, and anything that starts must go through
# common/web/LogSafeUri.redact().
server.tomcat.accesslog.pattern=%h %l %u %t "%m %U %H" %s %b %D

# Cashfree webhook HMAC secret. Env-supplied; the fallback exists only so the mock DigiLocker flow
# is demoable with zero vendor keys. Prod overrides with a bare ${CASHFREE_WEBHOOK_SECRET} so a
# missing secret fails the boot instead of running on a publicly-known one.
punenest.webhooks.cashfree.secret=${CASHFREE_WEBHOOK_SECRET:dev-webhook-secret}


# ---------------------------------------------------------------------------------------------
# Cashfree providers (KYC today; the payment rail lands in slice 6).
#
# Off by default, and deliberately not tied to the `prod` profile: "which environment is this"
# and "do we hold vendor credentials" are independent questions, and coupling them meant a
# developer with sandbox keys could never exercise the real client while production could never
# fall back to mocks. With the flag off, MockKycProvider / MockPaymentGateway are wired and no
# external call is possible - CashfreeClient is not even instantiated.
#
# The keys have NO committed default: unlike the webhook secret there is no demo value that could
# stand in, and a blank key would surface as a puzzling vendor 401 instead of an obvious local
# misconfiguration. CashfreeClient refuses to start if the flag is on and either key is missing.
punenest.providers.cashfree.enabled=${CASHFREE_ENABLED:false}
punenest.providers.cashfree.base-url=${CASHFREE_BASE_URL:https://sandbox.cashfree.com}
punenest.providers.cashfree.app-id=${CASHFREE_APP_ID:}
punenest.providers.cashfree.secret-key=${CASHFREE_SECRET_KEY:}
punenest.providers.cashfree.kyc-redirect-url=${CASHFREE_KYC_REDIRECT_URL:https://punenest.in/verify/complete}

# --- Document storage (slice 10) -------------------------------------------------------------
# Where MockFileStorage writes uploaded document bytes. A real deployment replaces the whole
# FileStorage bean with an object store; this exists so that in dev an upload is a real side
# effect rather than a URL-shaped fiction.
punenest.storage.dir=${STORAGE_DIR:${java.io.tmpdir}/punenest-storage}

# Cloudflare R2 object storage (ADR-013). Off by default: with the flag false the local-disk
# mock (dev) or the throwing stub (elsewhere) is wired and no R2 call is ever made. Turn on by
# setting STORAGE_ENABLED=true plus the R2_* credentials in the environment (never committed).
punenest.providers.storage.enabled=${STORAGE_ENABLED:false}
punenest.providers.storage.endpoint=${R2_ENDPOINT:}
punenest.providers.storage.access-key-id=${R2_ACCESS_KEY_ID:}
punenest.providers.storage.secret-access-key=${R2_SECRET_ACCESS_KEY:}
punenest.providers.storage.public-bucket=${R2_BUCKET_PUBLIC:}
punenest.providers.storage.private-bucket=${R2_BUCKET_PRIVATE:}
punenest.providers.storage.public-base-url=${R2_PUBLIC_BASE_URL:}

# The container's multipart ceiling. Deliberately a little above DocumentUploads.MAX_BYTES (10 MB):
# our own check produces a 413 naming the limit, and we want that message rather than the
# container's, which trips before any of our code runs. The request cap leaves room for the
# category field and the multipart framing on top of one max-size file.
spring.servlet.multipart.max-file-size=12MB
spring.servlet.multipart.max-request-size=13MB

# --- Write rate limit (tech-debt D2) ---------------------------------------------------------
# WriteRateLimitFilter caps mutating requests per caller — keyed on user id when a bearer token is
# present, on client address otherwise. The default budget is deliberately far above what a person
# can produce (a determined user filling forms manages single-digit writes a minute) and far below
# what a script wants, so a legitimate 429 from here should never happen; if one does, the number is
# wrong, not the user.
#
# Left at the production defaults here rather than loosened for dev, unlike the OTP budget above:
# the OTP limits exist to protect an SMS gateway that does not exist locally, whereas this one
# protects the database and the queues, which do. A local run should feel exactly like production.
punenest.security.rate-limit.enabled=true
punenest.security.rate-limit.writes-per-window=120
punenest.security.rate-limit.window-seconds=60

# Where the counter lives (tech-debt D158). `memory` is correct while exactly one instance runs and
# is the default for that reason. The moment a second one starts, each keeps its own tally and the
# effective budget silently multiplies by the instance count — the failure is invisible, because
# every instance believes it is enforcing the number above.
#
# `redis` selects the shared counter instead. It refuses to start rather than falling back to
# memory: a silent fallback would hand the operator exactly the multiplied budget they switched to
# Redis to eliminate, and they would have no way to tell. Note the transport is not yet buildable
# offline (the cached Spring Data Redis generation does not match this Boot version), so `redis` is
# not selectable today; the store and its tests exist so that turning it on is a one-class change.
punenest.ratelimit.store=${RATELIMIT_STORE:memory}

# --- Bot defence on public writes (tech-debt D130) --------------------------------------------
# Cloudflare Turnstile, verified server-side over its siteverify endpoint. Applies only to writes a
# stranger can reach — login, city waitlist, society leads — because a challenge on an authenticated
# endpoint inconveniences the users who already proved who they are and stops no script that has a
# token.
#
# Off by default, and the asymmetry is the point: disabled means no HTTP call and every request
# passes, while enabled means a missing, rejected or unverifiable token is refused. Failing open
# when configured would make an attacker's job "break the verifier", which is easier than solving
# the challenge. The secret has no committed default and the app refuses to start without one when
# enabled — a blank secret would fail every verification and read as a broken site, not a
# misconfigured one. Put it in backend/.env.local, never here.
punenest.security.turnstile.enabled=${TURNSTILE_ENABLED:false}
punenest.security.turnstile.secret-key=${TURNSTILE_SECRET_KEY:}
punenest.security.turnstile.verify-url=${TURNSTILE_VERIFY_URL:https://challenges.cloudflare.com/turnstile/v0/siteverify}
punenest.security.turnstile.connect-timeout-seconds=3
punenest.security.turnstile.read-timeout-seconds=5

# Who, if anyone, sits in front of this instance. `none` means "nothing does, so the socket address
# is the client's address" — true for a local run and for an app on a public port. Any deploy behind
# a load balancer must replace this with a regex matching that balancer's addresses, or every
# anonymous caller is keyed on the balancer and one host can rate-limit the whole platform.
#
# Declared here rather than only in the prod profile on purpose: a profile name is a magic string,
# and a staging environment or a container with a mistyped SPRING_PROFILES_ACTIVE would otherwise
# inherit no answer at all. TrustedProxyConfig refuses to start on a blank or unparseable value, and
# WriteRateLimitFilter logs loudly if a request arrives carrying X-Forwarded-For while this says
# `none` — which is proof that a proxy is in front and this line is wrong.
punenest.security.trusted-proxies=none

