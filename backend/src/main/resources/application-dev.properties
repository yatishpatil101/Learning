# Dev profile: keyless mock providers (selected by @Profile("!prod")), readable console logs, and a
# little extra visibility into our own package. Datasource/JWT secret fall back to the local
# defaults in application.properties.
logging.level.com.punenest.api=DEBUG

# Handy during local development; never enabled in prod.
management.endpoints.web.exposure.include=health,info
