/* Re-export kept as the import path the filtersPanel/* sections use.
   Prefer components/ui/FilterGroup.jsx in new code. */
export { FilterGroup, Divider, Cb, Rb } from '../../../components/ui/FilterGroup.jsx';
